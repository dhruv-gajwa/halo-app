---
phase: 03-authenticated-shell-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/storage/keys.ts
requirements:
  - DASH-03
autonomous: true
must_haves:
  truths:
    - "Implements CONTEXT.md decisions D-03 (faker as runtime dependency, ships with demo bundle), D-08 (K.tasks key shape: halo:v${SCHEMA_VERSION}:tasks:${workspaceId}), D-27 (SCHEMA_VERSION stays 1, no migration handler added)"
    - "recharts dependency resolves and a minimal <AreaChart> renders SVG (not canvas) at runtime under React 19"
    - "@faker-js/faker dependency resolves and faker.lorem.sentence() returns a string in a Node smoke check"
    - "K.tasks(workspaceId) returns the literal string 'halo:v1:tasks:<workspaceId>' for any workspaceId argument"
  artifacts:
    - path: "package.json"
      provides: "recharts and @faker-js/faker pinned in dependencies (not devDependencies)"
      contains: "recharts"
    - path: "src/storage/keys.ts"
      provides: "K.tasks(workspaceId) scoped key builder"
      contains: "tasks:"
  key_links:
    - from: "src/storage/keys.ts"
      to: "package.json scripts.typecheck"
      via: "K object literal must still satisfy `as const` with new method"
      pattern: "typecheck exit 0"
---

<objective>
Land the two runtime dependencies (`recharts`, `@faker-js/faker`) and the per-workspace storage key builder `K.tasks(workspaceId)` that every downstream Phase 3 plan depends on. This is the foundation wave — no UI, no data layer, just dependencies + the one key.

Purpose: Plans 03-02, 03-03, 03-04, 03-05, 03-06 all import either recharts, faker, or `K.tasks(...)` (or all three). Installing them up front in their own plan keeps wave 1 cheap and lets the rest of the phase run in parallel where possible.
Output: Updated `package.json` + `package-lock.json` with pinned `recharts` and `@faker-js/faker` versions, plus one new line in `src/storage/keys.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-authenticated-shell-dashboard/03-CONTEXT.md
@.planning/phases/03-authenticated-shell-dashboard/03-PATTERNS.md
@CLAUDE.md
@package.json
@src/storage/keys.ts
@src/storage/index.ts

<interfaces>
<!-- Existing K builder shape (src/storage/keys.ts:34-53) — Phase 3 adds one entry following the same shape -->

```ts
export const SCHEMA_VERSION = 1

export const K = {
  meta: (): string => `halo:v${SCHEMA_VERSION}:meta`,
  pendoAnonId: (): string => `halo:v${SCHEMA_VERSION}:pendo:anonId`,
  visitors: (): string => `halo:v${SCHEMA_VERSION}:visitors`,
  workspaces: (): string => `halo:v${SCHEMA_VERSION}:workspaces`,
  session: (): string => `halo:v${SCHEMA_VERSION}:session`,
  signupDraft: (): string => `halo:v${SCHEMA_VERSION}:signup:draft`,
} as const
```

<!-- The header note at lines 19-25 pre-declares the scoped-key pattern Phase 3 uses verbatim. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts + @faker-js/faker as runtime dependencies (pinned)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency versions, React 19 + Mantine 9 line)
    - CLAUDE.md §"Recommended Stack" (recharts 2.x HIGH confidence, @faker-js/faker 8.x HIGH confidence)
    - CLAUDE.md §"What NOT to Use" (no Chart.js, no canvas charts — recharts SVG mandate)
    - .planning/STATE.md (Phase 1 blocker: "Pin exact npm package versions via `npm view <pkg> version` at install time")
    - .planning/phases/03-authenticated-shell-dashboard/03-CONTEXT.md §"Decisions" D-03 + canonical_refs note on recharts React 19 peer-dep
  </read_first>
  <action>
    Resolve latest stable versions at install time via `npm view recharts version` and `npm view @faker-js/faker version`. Run `npm install --save recharts @faker-js/faker` (NOT `--save-dev`) per D-03 and CLAUDE.md (faker ships with the demo bundle so a fresh workspace seeds in-browser). After install, verify both packages appear under the top-level `dependencies` key in `package.json` (NOT under `devDependencies`). Run `npm run typecheck` to confirm React 19 peer-dep resolves without compile errors. Run `npm run build` once to confirm Vite can statically analyze a recharts import — create a one-off scratch file `src/__recharts-smoke.tsx` that imports `{ AreaChart, Area, XAxis, YAxis, CartesianGrid } from 'recharts'` and exports a renderable component, then build, then DELETE the scratch file before completing the task. If recharts emits a React 19 peer-dep warning during install, document the warning text in the plan SUMMARY but proceed — recharts 2.13.0+ supports React 19. Do NOT install `dayjs` (CLAUDE.md MEDIUM-confidence; the activity-feed relative-time helper is hand-rolled in Plan 03-06 per S6 analog).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.recharts) process.exit(1); if(!p.dependencies['@faker-js/faker']) process.exit(2); if(p.devDependencies && p.devDependencies.recharts) process.exit(3); console.log('OK', p.dependencies.recharts, p.dependencies['@faker-js/faker']);" && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"recharts"\s*:' package.json` matches inside the `dependencies` block (not `devDependencies`).
    - `grep -E '"@faker-js/faker"\s*:' package.json` matches inside the `dependencies` block.
    - Pinned version strings start with `^` and are non-empty (e.g. `^2.x.x`, `^9.x.x`).
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
    - File `src/__recharts-smoke.tsx` does NOT exist after the task (was created, used to verify, then deleted).
    - `node -e "import('recharts').then(m => console.log(typeof m.AreaChart))"` (or equivalent CJS resolution check) prints `function` or `object` — recharts module loads.
  </acceptance_criteria>
  <done>
    Two new runtime deps pinned in `dependencies` (not devDependencies), typecheck + build green, no leftover smoke files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add K.tasks(workspaceId) scoped key builder</name>
  <files>src/storage/keys.ts</files>
  <read_first>
    - src/storage/keys.ts (full file — the existing K object literal at lines 34-53, the header note at lines 19-25 pre-declaring the `tasks(accountId)` pattern, the comment at line 23 referring to Phase 4 — we honor the same shape with workspaceId instead of accountId per D-08)
    - src/storage/index.ts (the barrel — `export * from './keys'` means `K.tasks` becomes available via `import { K } from '../storage'`)
    - .planning/phases/03-authenticated-shell-dashboard/03-CONTEXT.md §"Decisions" D-08 (key template literal `halo:v${SCHEMA_VERSION}:tasks:${workspaceId}`) and D-27 (SCHEMA_VERSION stays 1 — do NOT bump)
    - .planning/phases/03-authenticated-shell-dashboard/03-PATTERNS.md §"src/storage/keys.ts (modified — add K.tasks(workspaceId))"
  </read_first>
  <action>
    Add one method to the `K` object literal in `src/storage/keys.ts` between `workspaces` and `session` (keep persistence-related keys grouped before session/draft keys): name the method `tasks`, signature `(workspaceId: string): string`, body `\`halo:v${SCHEMA_VERSION}:tasks:${workspaceId}\``. Mirror the existing JSDoc shape — one-line `/** ... */` comment above the entry naming the key template and noting it is per-workspace (Phase 3 seeder + Phase 4 CRUD). Do NOT bump `SCHEMA_VERSION` (per D-27 — this is a non-breaking additive change). Do NOT modify the `as const` assertion. Do NOT touch any other entry in `K`. Do NOT add `K.tasks` re-export anywhere — the storage barrel already does `export * from './keys'`.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "import('./src/storage/keys.ts').then ? null : null;" || true && node --input-type=module -e "const m = await import('./src/storage/keys.js').catch(() => null); /* fallback */" 2>/dev/null; grep -Pq '^\s+tasks:\s*\(workspaceId:\s*string\):\s*string\s*=>\s*`halo:v\$\{SCHEMA_VERSION\}:tasks:\$\{workspaceId\}`,' src/storage/keys.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tasks:' src/storage/keys.ts` >= 1 (new entry present).
    - `grep -E 'tasks:\s*\(workspaceId:\s*string\):\s*string' src/storage/keys.ts` matches at least once (exact signature).
    - `grep -E 'halo:v\$\{SCHEMA_VERSION\}:tasks:\$\{workspaceId\}' src/storage/keys.ts` matches at least once (exact template).
    - `grep -E '^export const SCHEMA_VERSION = 1' src/storage/keys.ts` still matches (NOT bumped to 2 per D-27).
    - `grep -E '} as const' src/storage/keys.ts` still matches (the `as const` on K is preserved).
    - `npm run typecheck` exits 0.
    - In a Node REPL (or via a one-shot eval): the expression `K.tasks('ws_abc')` returns the exact string `halo:v1:tasks:ws_abc`.
  </acceptance_criteria>
  <done>
    `K.tasks(workspaceId)` exists in the `K` object literal, returns `halo:v1:tasks:<workspaceId>`, typecheck passes, SCHEMA_VERSION is still 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party package code is executed at build + runtime; both packages are widely-installed (>40M weekly downloads each) and have no known supply-chain incidents at pinned versions |
| localStorage tampering → app boot | Per-workspace task key namespace; malformed values handled by Plan 03-02's `readWithSchema` fallback (defense-in-depth lives downstream, not here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | npm package integrity | mitigate | `package-lock.json` is committed; versions pinned with `^` allows patch/minor float but lock pins exact resolution. Document installed version strings in plan SUMMARY. |
| T-03-01-02 | Denial of Service | bundle size from faker @ ~5MB | accept | Demo-only surface; CLAUDE.md D-03 mandates faker as a runtime dep so workspaces seed in-browser. Tree-shaking + Vite production minify reduces footprint; if bundle exceeds a future budget, switch faker to a lazy import in Plan 03-03's seeder. No PII-leak risk — faker only generates synthetic data. |
| T-03-01-03 | Information Disclosure | per-workspace key namespace | accept | `K.tasks(workspaceId)` includes the workspaceId in the localStorage key — visible to anyone with DevTools access. Demo app; workspaceIds are nanoid (non-guessable, no PII). No mitigation needed beyond Plan 03-02's `readWithSchema` envelope. |
</threat_model>

<verification>
- `package.json` has both `recharts` and `@faker-js/faker` under `dependencies`.
- `npm run typecheck` and `npm run build` both exit 0.
- `K.tasks(workspaceId)` is callable and returns the expected template-literal output.
- No other `K.*` entry was modified.
- `SCHEMA_VERSION` is still `1`.
</verification>

<success_criteria>
- Both packages resolve at the latest 2.x (recharts) / 8.x or later (@faker-js/faker) stable versions per CLAUDE.md.
- React 19 peer-dep does not block install or build.
- `K.tasks('test_ws')` returns `halo:v1:tasks:test_ws`.
- All downstream Plan 03-02..03-06 imports of `K`, `recharts`, and `@faker-js/faker` will resolve when those plans execute.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authenticated-shell-dashboard/03-01-SUMMARY.md` recording: pinned versions installed, any React 19 peer-dep warnings observed, the exact `K.tasks` line added, and `npm run typecheck` / `npm run build` exit codes.
</output>
