---
phase: 03-authenticated-shell-dashboard
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - src/tasks/schemas.ts
  - src/tasks/types.ts
  - src/tasks/tasksRepo.ts
  - src/tasks/labels.ts
  - src/tasks/index.ts
requirements:
  - DASH-02
autonomous: true
must_haves:
  truths:
    - "Implements CONTEXT.md decisions D-06 (Task field list: id, title, description, status, priority, assignee, createdAt, updatedAt, dueDate, completedAt with exact Zod types), D-07 (schemas.ts layout mirrors src/auth/schemas.ts; types via z.infer), D-09 (TASK_STATUS_LABELS + TASK_PRIORITY_LABELS in src/tasks/labels.ts, UI-layer-only, not in schema), D-10 (tasksRepo exposes listTasks/getTaskById/createTask/updateTask/deleteTask; every read flows through readWithSchema per FND-04)"
    - "TasksArraySchema rejects malformed JSON and listTasks(workspaceId) returns [] instead of throwing"
    - "TaskSchema accepts a faker-shaped task object (id, title, description, status, priority, assignee {id,name}, createdAt, updatedAt, dueDate, completedAt) and rejects missing-required-field objects"
    - "TASK_STATUS_LABELS exhaustively maps every TaskStatus enum value to a human-readable string (compile-time enforced by Record<TaskStatus, string>)"
  artifacts:
    - path: "src/tasks/schemas.ts"
      provides: "TaskSchema, TasksArraySchema, TaskStatusEnum, TaskPriorityEnum, AssigneeSchema"
      contains: "TaskSchema"
    - path: "src/tasks/types.ts"
      provides: "Task, TaskStatus, TaskPriority, Assignee types (z.infer-derived)"
      contains: "z.infer"
    - path: "src/tasks/tasksRepo.ts"
      provides: "listTasks/getTaskById/createTask/updateTask/deleteTask"
      contains: "readWithSchema"
    - path: "src/tasks/labels.ts"
      provides: "TASK_STATUS_LABELS, TASK_PRIORITY_LABELS"
      contains: "Record<TaskStatus, string>"
    - path: "src/tasks/index.ts"
      provides: "Barrel re-export of schemas + types + repo + labels"
      contains: "export *"
  key_links:
    - from: "src/tasks/tasksRepo.ts"
      to: "src/storage (K + readWithSchema + writeJSON)"
      via: "import { K, readWithSchema, writeJSON } from '../storage'"
      pattern: "from '\\.\\./storage'"
    - from: "src/tasks/types.ts"
      to: "src/tasks/schemas.ts"
      via: "import type { TaskSchema } from './schemas'; export type Task = z.infer<typeof TaskSchema>"
      pattern: "z\\.infer<typeof "
---

<objective>
Establish the Task feature module as the schema-first contract that Phase 4 inherits verbatim. Mirrors the `src/auth/{schemas,types,authRepo,index}.ts` layout exactly. Phase 3 only CALLS `listTasks` (for the Dashboard) but the full repo signature (`createTask`/`updateTask`/`deleteTask`) is exported now so Phase 4's LIST CRUD doesn't have to extend the surface.

Purpose: The Task schema is a cross-phase contract. Editing it later is a deliberate change, not a refactor — so lock it now with all the field types Phase 4 needs (D-06 through D-10).
Output: Five files in a new `src/tasks/` directory + a thin barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-authenticated-shell-dashboard/03-CONTEXT.md
@.planning/phases/03-authenticated-shell-dashboard/03-PATTERNS.md
@CLAUDE.md
@src/auth/schemas.ts
@src/auth/types.ts
@src/auth/authRepo.ts
@src/auth/index.ts
@src/storage/codec.ts
@src/storage/keys.ts

<interfaces>
<!-- The pattern Task module must mirror, extracted from src/auth/* -->

```ts
// src/auth/schemas.ts — Zod enum + object pattern
export const RoleEnum = z.enum(['engineer', 'manager', 'designer', /* ... */])

export const VisitorSchema = z.object({
  id: z.string().min(1),
  email: z.string().email(),
  createdAt: z.iso.datetime(),
  // ...
})

// src/auth/types.ts — z.infer derivation
import type { z } from 'zod'
import type { VisitorSchema, RoleEnum } from './schemas'
export type Visitor = z.infer<typeof VisitorSchema>
export type Role = z.infer<typeof RoleEnum>

// src/auth/authRepo.ts — read-helper pattern
import { K, readWithSchema, writeJSON } from '../storage'
import { VisitorsArraySchema } from './schemas'
import type { Visitor } from './types'

export function listVisitors(): Visitor[] {
  return readWithSchema(K.visitors(), VisitorsArraySchema, [] as Visitor[])
}
```

<!-- src/storage/codec.ts:26-36 — readWithSchema contract (never throws, returns fallback on key absent / parse fail / Zod fail) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/tasks/schemas.ts + src/tasks/types.ts + src/tasks/labels.ts</name>
  <files>src/tasks/schemas.ts, src/tasks/types.ts, src/tasks/labels.ts</files>
  <read_first>
    - src/auth/schemas.ts (full file — enum-declaration pattern at the `RoleEnum`/`CompanySizeEnum` declarations; embedded-object schema pattern on `VisitorSchema` for the `assignee` shape; `z.iso.datetime()` idiom on `createdAt` field)
    - src/auth/types.ts (full file — `import type { z } from 'zod'` + `export type X = z.infer<typeof XSchema>` pattern with type-only imports)
    - .planning/phases/03-authenticated-shell-dashboard/03-CONTEXT.md §"Decisions" D-06 (exact field list), D-07 (file layout mirrors src/auth/schemas.ts), D-09 (display-label map values)
    - .planning/phases/03-authenticated-shell-dashboard/03-PATTERNS.md §"src/tasks/schemas.ts", §"src/tasks/types.ts", §"src/tasks/labels.ts" (full pattern excerpts including file-header docblocks)
  </read_first>
  <action>
    Create three files in a new `src/tasks/` directory.

    `src/tasks/schemas.ts` — import `z` from `zod`; declare `TaskStatusEnum = z.enum(['todo', 'in_progress', 'done'])` and `TaskPriorityEnum = z.enum(['low', 'medium', 'high', 'urgent'])` per D-06 (no error-message argument — Phase 3 has no form surface for tasks; Phase 4 LIST-02 may add messages); declare `AssigneeSchema = z.object({ id: z.string().min(1), name: z.string().min(1), avatar: z.string().url().optional() })` per D-06; declare `TaskSchema = z.object({ id, title, description, status, priority, assignee, createdAt, updatedAt, dueDate, completedAt })` with exact field types from D-06: `id: z.string().min(1)`, `title: z.string().min(1)`, `description: z.string()`, `status: TaskStatusEnum`, `priority: TaskPriorityEnum`, `assignee: AssigneeSchema`, `createdAt: z.iso.datetime()`, `updatedAt: z.iso.datetime()`, `dueDate: z.iso.datetime().nullable()`, `completedAt: z.iso.datetime().nullable()`. Use `z.iso.datetime()` (Zod 4 idiom — matches `VisitorSchema.createdAt`), NOT `z.string().datetime()`. Declare `TasksArraySchema = z.array(TaskSchema)`. Include the file-header docblock from PATTERNS.md §"src/tasks/schemas.ts" (Phase 3 single-source-of-truth, schema-lock note for Phase 4, assignee snapshot rationale for Phase 5 TEAM-01).

    `src/tasks/types.ts` — `import type { z } from 'zod'`; `import type { TaskSchema, TaskStatusEnum, TaskPriorityEnum, AssigneeSchema } from './schemas'`; `export type Task = z.infer<typeof TaskSchema>`; `export type TaskStatus = z.infer<typeof TaskStatusEnum>`; `export type TaskPriority = z.infer<typeof TaskPriorityEnum>`; `export type Assignee = z.infer<typeof AssigneeSchema>`. CRITICAL: every import uses `import type` (not `import`) per the auth/types.ts convention. Include the file-header docblock from PATTERNS.md §"src/tasks/types.ts" (Zod-as-single-source-of-truth, do-not-hand-write).

    `src/tasks/labels.ts` — `import type { TaskStatus, TaskPriority } from './types'`; export `TASK_STATUS_LABELS: Record<TaskStatus, string>` with exact mapping per D-09: `todo: 'To do'`, `in_progress: 'In progress'`, `done: 'Done'`; export `TASK_PRIORITY_LABELS: Record<TaskPriority, string>`: `low: 'Low'`, `medium: 'Medium'`, `high: 'High'`, `urgent: 'Urgent'`. Use `Record<TaskStatus, string>` annotation (NOT plain object literal) — forces exhaustiveness so future enum additions trip the type checker (PATTERNS.md §"src/tasks/labels.ts" rationale). Include the file-header docblock from PATTERNS.md §"src/tasks/labels.ts" (UI-layer-only, do-NOT-add-to-schemas).
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "z.iso.datetime" src/tasks/schemas.ts && grep -c "z.infer" src/tasks/types.ts && grep -c "Record<TaskStatus" src/tasks/labels.ts && node --input-type=module -e "import { TaskSchema, TasksArraySchema, TaskStatusEnum } from './src/tasks/schemas.ts'" 2>&1 | grep -v "Cannot use import" || true</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tasks/schemas.ts` exists.
    - `grep -E "^export const TaskStatusEnum = z.enum\\(\\['todo', 'in_progress', 'done'\\]\\)" src/tasks/schemas.ts` matches.
    - `grep -E "^export const TaskPriorityEnum = z.enum\\(\\['low', 'medium', 'high', 'urgent'\\]\\)" src/tasks/schemas.ts` matches.
    - `grep -c "z.iso.datetime" src/tasks/schemas.ts` >= 4 (createdAt, updatedAt, dueDate, completedAt all use `z.iso.datetime()`).
    - `grep -c "z.string().datetime" src/tasks/schemas.ts` == 0 (no Zod-3 idiom).
    - `grep -E "export const AssigneeSchema = z.object" src/tasks/schemas.ts` matches.
    - `grep -E "export const TaskSchema = z.object" src/tasks/schemas.ts` matches.
    - `grep -E "export const TasksArraySchema = z.array\\(TaskSchema\\)" src/tasks/schemas.ts` matches.
    - File `src/tasks/types.ts` exists.
    - `grep -c "^import type" src/tasks/types.ts` >= 2 (z + schemas).
    - `grep -E "export type Task = z.infer<typeof TaskSchema>" src/tasks/types.ts` matches.
    - `grep -E "export type TaskStatus = z.infer<typeof TaskStatusEnum>" src/tasks/types.ts` matches.
    - `grep -E "export type TaskPriority = z.infer<typeof TaskPriorityEnum>" src/tasks/types.ts` matches.
    - `grep -E "export type Assignee = z.infer<typeof AssigneeSchema>" src/tasks/types.ts` matches.
    - File `src/tasks/labels.ts` exists.
    - `grep -E "TASK_STATUS_LABELS: Record<TaskStatus, string>" src/tasks/labels.ts` matches.
    - `grep -E "TASK_PRIORITY_LABELS: Record<TaskPriority, string>" src/tasks/labels.ts` matches.
    - `grep -E "todo: 'To do'" src/tasks/labels.ts` matches.
    - `grep -E "in_progress: 'In progress'" src/tasks/labels.ts` matches.
    - `grep -E "urgent: 'Urgent'" src/tasks/labels.ts` matches.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Schemas, types, and labels are in place. Every type is `z.infer`-derived. Labels enforce exhaustiveness via `Record<TaskStatus, string>`. Typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/tasks/tasksRepo.ts + src/tasks/index.ts barrel</name>
  <files>src/tasks/tasksRepo.ts, src/tasks/index.ts</files>
  <read_first>
    - src/auth/authRepo.ts (full file — read-helper pattern on `listVisitors`/`getVisitorById`, write-helper pattern on `createVisitor` for `createTask` template, the FND-04 contract docblock at lines 1-29, the import block at lines 31-35)
    - src/storage/codec.ts (`readWithSchema(key, Schema, fallback)` signature — never throws; returns fallback on three failure modes)
    - src/storage/keys.ts (`K.tasks(workspaceId)` added in Plan 03-01)
    - src/storage/index.ts (barrel exports: `K`, `readWithSchema`, `writeJSON`)
    - src/auth/index.ts (barrel pattern — `export * from './authRepo'`, `export * from './schemas'`, `export * from './types'`)
    - .planning/phases/03-authenticated-shell-dashboard/03-CONTEXT.md §"Decisions" D-10 (exposed methods + Phase-3-uses-only-listTasks rationale)
    - .planning/phases/03-authenticated-shell-dashboard/03-PATTERNS.md §"src/tasks/tasksRepo.ts" (full code excerpt + the FND-04 contract enforcement note) and §"src/tasks/index.ts" (barrel pattern)
  </read_first>
  <action>
    Create `src/tasks/tasksRepo.ts` mirroring `src/auth/authRepo.ts` line-for-line:
    - File-header docblock per PATTERNS.md §"src/tasks/tasksRepo.ts" (Phase 3 owner of `K.tasks(workspaceId)`; FND-04 contract — every read through `readWithSchema`; non-atomic read-modify-write caveat).
    - Imports: `import { nanoid } from 'nanoid'`; `import { K, readWithSchema, writeJSON } from '../storage'`; `import { TasksArraySchema } from './schemas'`; `import type { Task } from './types'`.
    - Export `function listTasks(workspaceId: string): Task[]` body: `return readWithSchema(K.tasks(workspaceId), TasksArraySchema, [] as Task[])`. Single line, mirrors `authRepo.listVisitors`.
    - Export `function getTaskById(workspaceId: string, id: string): Task | undefined` body: `return listTasks(workspaceId).find((t) => t.id === id)`.
    - Export `type CreateTaskInput = Omit<Task, 'id' | 'createdAt' | 'updatedAt'>`.
    - Export `function createTask(workspaceId: string, input: CreateTaskInput): Task` body: build `now = new Date().toISOString()`; construct `task: Task = { id: nanoid(), createdAt: now, updatedAt: now, ...input }`; read existing via `listTasks(workspaceId)`; write `writeJSON(K.tasks(workspaceId), [...existing, task])`; return task.
    - Export `function updateTask(workspaceId: string, id: string, patch: Partial<Omit<Task, 'id' | 'createdAt'>>): Task | undefined` body: read existing via listTasks; find index by id; if -1 return undefined; construct `updated: Task = { ...existing[idx], ...patch, updatedAt: new Date().toISOString() }`; clone array, replace index, writeJSON, return updated.
    - Export `function deleteTask(workspaceId: string, id: string): boolean` body: read existing via listTasks; filter out by id; if length unchanged return false; writeJSON the filtered array; return true.
    - NO direct `localStorage.*` calls anywhere — only `readWithSchema` and `writeJSON` via the storage barrel (FND-04 contract — verifiable by `grep -c "localStorage\." src/tasks/tasksRepo.ts` returning 0).

    Create `src/tasks/index.ts` barrel:
    - File-header docblock per PATTERNS.md §"src/tasks/index.ts" (single import target; `export *` for schemas/types/repo/labels; named export for seeder which is added in Plan 03-03 — for now, do NOT add the seeder export; Plan 03-03 will append it).
    - Body (exactly these lines, in this order, NO seeder export yet — Plan 03-03 appends it):
      - `export * from './schemas'`
      - `export * from './types'`
      - `export * from './tasksRepo'`
      - `export * from './labels'`

    Do NOT add `seedIfNeeded` re-export here — Plan 03-03 owns the seeder file and will append its own re-export line.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "localStorage\." src/tasks/tasksRepo.ts && grep -c "from '../storage'" src/tasks/tasksRepo.ts && grep -c "readWithSchema(K.tasks(workspaceId)" src/tasks/tasksRepo.ts && grep -c "^export \*" src/tasks/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tasks/tasksRepo.ts` exists.
    - `grep -c "localStorage\\." src/tasks/tasksRepo.ts` == 0 (NO direct localStorage access — FND-04).
    - `grep -E "from '\\.\\./storage'" src/tasks/tasksRepo.ts` matches at least once.
    - `grep -E "readWithSchema\\(K\\.tasks\\(workspaceId\\)" src/tasks/tasksRepo.ts` matches at least once (the canonical read path).
    - `grep -E "^export function listTasks\\(workspaceId: string\\): Task\\[\\]" src/tasks/tasksRepo.ts` matches.
    - `grep -E "^export function getTaskById\\(workspaceId: string, id: string\\): Task \\| undefined" src/tasks/tasksRepo.ts` matches.
    - `grep -E "^export function createTask\\(workspaceId: string, input: CreateTaskInput\\): Task" src/tasks/tasksRepo.ts` matches.
    - `grep -E "^export function updateTask" src/tasks/tasksRepo.ts` matches.
    - `grep -E "^export function deleteTask\\(workspaceId: string, id: string\\): boolean" src/tasks/tasksRepo.ts` matches.
    - `grep -E "export type CreateTaskInput" src/tasks/tasksRepo.ts` matches.
    - `grep -E "import \\{ nanoid \\} from 'nanoid'" src/tasks/tasksRepo.ts` matches.
    - File `src/tasks/index.ts` exists with 4 `export *` lines (schemas, types, tasksRepo, labels) — NO `seedIfNeeded` named export (deferred to Plan 03-03).
    - `grep -c "^export \\* from './" src/tasks/index.ts` == 4.
    - `grep -c "seedIfNeeded" src/tasks/index.ts` == 0 (added in Plan 03-03).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Repository module owns `K.tasks(workspaceId)`, exposes the five-method API (Phase 3 calls only `listTasks`; Phase 4 inherits the rest), and routes every read through `readWithSchema`. Barrel re-exports the public surface; the seeder slot is reserved for Plan 03-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → repo read path | Untrusted persisted blob enters here; Zod safeParse rejects malformed shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | localStorage value for `halo:v1:tasks:{wsId}` | mitigate | Every read goes through `readWithSchema(K.tasks(wsId), TasksArraySchema, [])` (FND-04). DevTools-injected malformed JSON, prototype-pollution payloads, or stale schemas all fall through to `[]` instead of throwing or executing. Verified by `grep -c localStorage src/tasks/tasksRepo.ts == 0`. |
| T-03-02-02 | Information Disclosure | embedded `assignee.name` in task record | accept | Faker generates synthetic names (no PII). Demo app; no real user data flows through the task schema. Phase 5 TEAM-01 introduces the canonical teammates store and may rewrite the embedded slot to a reference, narrowing this further. |
| T-03-02-03 | Tampering | non-atomic read-modify-write in updateTask/deleteTask | accept | Two browser tabs racing on the same workspace can drop a write. Same WR-04 caveat as `authRepo` (CONTEXT.md §code_context analog). Demo surface — no mitigation budget; documented in the repo header docblock. |
</threat_model>

<verification>
- All five `src/tasks/*` files exist.
- `npm run typecheck` exits 0.
- `tasksRepo` is the only file that imports `K.tasks` — verified by `grep -rn "K.tasks" src/` returning matches only inside `src/tasks/` (excluding `src/storage/keys.ts` definition).
- `tasksRepo.ts` contains zero `localStorage.*` calls.
- The barrel exports cover schemas, types, repo, and labels (4 `export *` lines).
</verification>

<success_criteria>
- The Task schema is the cross-phase contract — every field type matches D-06 exactly.
- `listTasks(workspaceId)` returns `Task[]` and never throws.
- `TasksArraySchema.safeParse(garbageInput).success === false` (Zod rejects malformed payloads).
- TASK_STATUS_LABELS enumerates all three TaskStatus values; TypeScript flags missing entries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authenticated-shell-dashboard/03-02-SUMMARY.md` recording: the five files created, the exact `TaskSchema` field-by-field shape (so Phase 4 can confirm inherited contract), and verification that `grep -c localStorage src/tasks/` returns 0 across all Phase 3 task files.
</output>
