---
phase: 05-team-help-polish
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - src/routes/app/dashboard/Dashboard.tsx
  - src/routes/app/lists/ListsPage.tsx
  - src/routes/app/settings/SettingsPage.tsx
  - src/routes/app/reports/ReportsPage.tsx
  - src/routes/app/team/TeamPage.tsx
  - src/routes/app/help/HelpPage.tsx
  - src/routes/app/help/HelpArticlePage.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Every /app/* page title is `<Title order={3}>` — no rogue `<Title order={2}>` page titles (UI-04)"
    - "No `fw={700}` anywhere in src/ — Phase 3/4/5 two-weight typography contract holds (UI-04)"
    - "No `gap=\"xs\"` (10px) anywhere in Phase 5-touched files — Phase 5 xs-token exclusion (UI-04)"
    - "All `<Paper>` surfaces on /app/* are `withBorder` (no `shadow` props on non-AppShell surfaces) (UI-04)"
    - "No raw px values in CSS props anywhere in src/team/ or src/help/ (UI-04)"
    - "No hardcoded hex color values in src/team/ or src/help/ component bodies (UI-04 + D-18 dark-mode)"
    - "Reports filtered-empty state exists and renders compact in-flow per Phase 4 D-06 (UI-01 verify)"
    - "Toasts emit on: task create, task save, task delete, profile save, workspace save, invite sent, role updated — completeness audit passes (UI-02)"
    - "No new destructive flows; Delete-task and Reset-demo-data confirmations remain intact (UI-03)"
    - "Grep for `<canvas` across src/ returns ZERO matches — SVG-only chart rule (PEN-08 verify, no regression)"
    - "PasswordInput primitive applies `.pendo-sr-ignore` class (PEN-09 verify, no regression)"
    - "Every Phase 5 interactive element references PENDO_IDS.<namespace>.<key> — no hand-typed `data-pendo-id` strings in src/team/, src/help/, src/seed/, src/routes/app/team/, src/routes/app/help/ (PEN-07 verify)"
    - "Every /app/* route deep-links cleanly on refresh (SHELL-04 verify — no regression from Phase 5 router edit)"
    - "Polish scope is verification + targeted fix only across UI-01 (empty states), UI-02 (toast completeness), UI-03 (destructive confirms — no new destructive flows in Phase 5), and UI-04 (typography/spacing/color consistency); NOT a redesign (D-16)"
    - "After Reset demo data is invoked, the existing halo:v* prefix sweep clears the new K.teammates(workspaceId) key automatically — no edit to Reset code is required, and on next sign-in seedDemoData runs both teammate + task seeders fresh (D-13)"
    - "Verification is manual via /gsd-verify-phase plus an inline 05-VERIFICATION.md capturing the Success Criteria #5 walkthrough and UAT items per the Phase 4 commit 5195b3b precedent — no DEMO-READY-AUDIT.md artifact is generated (D-17)"
  artifacts:
    - path: ".planning/phases/05-team-help-polish/05-VERIFICATION.md"
      provides: "Audit findings — pass/fail per Success Criteria #5 check"
      contains: "Pendo-readiness checklist"
  key_links:
    - from: "Polish audit"
      to: "ROADMAP Phase 5 Success Criteria #5"
      via: "Manual /gsd-verify-phase walk + automated grep gates"
      pattern: "Success Criteria"
---

<objective>
Cross-page polish + Pendo-readiness audit. Walk every /app/* page, fix UI-04 inconsistencies (typography/spacing/color/heading-levels), confirm UI-01/02/03 completeness across the surface, and run the automated Pendo-readiness gates (no canvas, .pendo-sr-ignore on password, PENDO_IDS-only data-pendo-id, deep-link integrity). Human-verify the user-eye items the automated gates cannot conclude.

Purpose: ROADMAP Phase 5 Success Criteria #5 lock — "could this pass for a real B2B SaaS in a screenshot?" + Pendo-readiness checklist. Per D-17, no DEMO-READY-AUDIT.md artifact is generated; verification is manual + an inline 05-VERIFICATION.md capturing gate results and UAT items.

Output: Targeted polish edits ONLY where the audit finds drift (most pages should pass clean; this plan ships small fixes if any). One VERIFICATION.md artifact capturing the audit walkthrough. Per D-16, polish is verification + targeted-fix, NOT a redesign.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-team-help-polish/05-CONTEXT.md
@.planning/phases/05-team-help-polish/05-UI-SPEC.md
@.planning/phases/05-team-help-polish/05-01-SUMMARY.md
@.planning/phases/05-team-help-polish/05-02-SUMMARY.md
@.planning/phases/05-team-help-polish/05-03-SUMMARY.md
@.planning/phases/05-team-help-polish/05-04-SUMMARY.md
@.planning/phases/05-team-help-polish/05-05-SUMMARY.md

<interfaces>
<!-- Phase 3/4 typography contract (inherited) -->
- 4 sizes (Title order 2, Title order 3, Text size md, Text size sm)
- 2 weights (400 default, 600 in Title)
- fw={500} permitted ONLY as sole exception for table headers and certain semi-emphasis labels (Phase 3 lock)
- No fw={700}; no Title order={4}; no size="xs"

<!-- Phase 3/4 spacing contract (inherited) -->
- Mantine named tokens only as string values: sm (12px), md (16px), lg (20px), xl (32px) — xs (10px) excluded per Phase 5
- gap={8} or gap={4} numeric exceptions allowed (multiples of 4 only)
- No raw px values in CSS props

<!-- Pendo-readiness regression gates -->
- src/ui/primitives/PasswordInput.tsx must apply `.pendo-sr-ignore` class to the rendered DOM <input> (PEN-09)
- grep `<canvas` across src/ must be 0 (PEN-08)
- All data-pendo-id values must flow from PENDO_IDS.ts — no hand-typed strings outside PENDO_IDS.ts itself
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Pendo-readiness + UI-04 gates + targeted polish fixes</name>
  <files>src/routes/app/dashboard/Dashboard.tsx, src/routes/app/lists/ListsPage.tsx, src/routes/app/settings/SettingsPage.tsx, src/routes/app/reports/ReportsPage.tsx, src/routes/app/team/TeamPage.tsx, src/routes/app/help/HelpPage.tsx, src/routes/app/help/HelpArticlePage.tsx</files>
  <read_first>
    - src/ui/primitives/PasswordInput.tsx (confirm .pendo-sr-ignore class is on the rendered DOM input element — PEN-09 regression gate)
    - src/pendo/PENDO_IDS.ts (confirm both team and help namespaces are present from Plans 04 + 05)
    - src/routes/app/dashboard/Dashboard.tsx, src/routes/app/lists/ListsPage.tsx, src/routes/app/settings/SettingsPage.tsx, src/routes/app/reports/ReportsPage.tsx — read each for the page header `<Title order=...>` line (must be `order={3}`), any `fw={700}` or `size="xs"` occurrences (must be zero), and any `<Paper shadow>` (must be `withBorder`)
    - src/routes/app/team/TeamPage.tsx, src/routes/app/help/HelpPage.tsx (Plans 04 + 05 outputs — confirm Title order={3}, no rogue spacings)
    - src/routes/app/help/HelpArticlePage.tsx (Plan 05 output — confirm Title order={3} in both branches)
    - .planning/phases/05-team-help-polish/05-UI-SPEC.md sections "Cross-Page Polish (UI-01..04)" (verbatim audit checklist + dark-mode color verification list), "What Phase 5 Does NOT Touch" (locks: do not touch existing PENDO_IDS namespaces, FND-07 provider stack, MetaSchema, SCHEMA_VERSION)
    - .planning/phases/05-team-help-polish/05-CONTEXT.md D-16 (polish scope — verification + targeted-fix only, NOT redesign), D-17 (no DEMO-READY-AUDIT.md artifact; manual /gsd-verify-phase + UAT entries)
  </read_first>
  <action>
    Run the automated audit gates IN ORDER. Document each gate's outcome in `.planning/phases/05-team-help-polish/05-VERIFICATION.md` as a pass/fail list. Apply targeted fixes only where a gate FAILS.

    Gate 1 (PEN-08 SVG-only): `grep -rnE "<canvas" src/` MUST return zero matches. If any match exists, that's a regression that needs investigation (Phase 5 ships zero new charts so this should pass cleanly).

    Gate 2 (PEN-09 Replay mask): `grep -E "pendo-sr-ignore" src/ui/primitives/PasswordInput.tsx` MUST return at least one match. If absent, regression — restore the class.

    Gate 3 (PEN-07 — Phase 5 surfaces use PENDO_IDS only): `grep -rE "data-pendo-id=\"(team|help)\\." src/` MUST return zero matches (the only place these strings live is PENDO_IDS.ts itself — every consumer reads via `PENDO_IDS.team.*` / `PENDO_IDS.help.*`). The dynamic per-row attributes (`data-pendo-teammate-id`, `data-pendo-article-slug`) are EXCLUDED from this gate — they read from data, not registry strings.

    Gate 4 (UI-04 typography — order 3 only for page titles): For each of the seven /app/* pages, the page-level `<Title>` element MUST be `order={3}` per UI-SPEC §"Cross-Page Polish (UI-01..04) UI-04". If a page renders `<Title order={2}>` as the PAGE title (KPI value `order={2}` on Dashboard stat cards is exempt — those are KPI values, not page titles), downgrade it. Run `grep -rnE "Title order=\\{2\\}" src/routes/app/` and inspect each match; fix any that are page-title scope.

    Gate 5 (UI-04 typography — no fw={700}, no size="xs"): `grep -rE "fw=\\{700\\}" src/` MUST be zero. `grep -rE "size=\"xs\"" src/` MUST be zero in any Phase 5-touched file (src/team/, src/help/, src/routes/app/team/, src/routes/app/help/, src/seed/) AND zero in /app/* page-body code. If any violations exist outside of legacy Phase 3/4 files, fix in place — replace with the closest legal value (`fw={500}` semi-emphasis or `fw={600}` Title-internal weight; `size="sm"` for size="xs").

    Gate 6 (UI-04 spacing — no raw px in CSS props or modules): `grep -rE "[0-9]+px\\b" src/team/components/ src/help/components/ src/seed/` MUST return zero matches in CSS modules (raw px in className-driven CSS rules is forbidden — must use Mantine spacing CSS vars). The Tabler icon `size={18}`, `size={16}`, `size={64}` numeric props are EXCLUDED — those are JSX number props, not CSS px strings, and are an inherited exception.

    Gate 7 (UI-04 color — no hardcoded hex): `grep -rE "#[0-9a-fA-F]{3,6}\\b" src/team/ src/help/ src/seed/` MUST return zero matches in component bodies. CSS-var references like `var(--mantine-color-gray-4)` are legal. If any hex strings exist in the new Phase 5 source, replace with `var(--mantine-color-*)` per UI-SPEC §"Color" dark-mode contract.

    Gate 8 (UI-04 surface — no `shadow` on /app/* surfaces): `grep -rE "<Paper[^>]*shadow=" src/routes/app/` MUST return zero matches. AppShell may use shadow internally — only `<Paper>` direct-page surfaces must use `withBorder`. If found, swap to `withBorder`.

    Gate 9 (UI-01 empty states — completeness check): Confirm presence by grep:
    - Dashboard empty state (tasks.length === 0): `grep -E "(EmptyState|No tasks yet)" src/dashboard/Dashboard.tsx` returns at least 1 (Phase 3 lock)
    - Lists empty state: `grep -E "ListsEmptyState" src/routes/app/lists/ListsPage.tsx` returns at least 1 (Phase 4 lock)
    - Lists filtered-empty: `grep -E "FilteredEmptyState" src/routes/app/lists/ListsPage.tsx` returns at least 1 (Phase 4 lock)
    - Reports filtered-empty: `grep -rE "(No tasks match these filters|filtered.*empty)" src/routes/app/reports/ src/reports/` returns at least 1 (Phase 4 D-06 — verify present)
    - Team empty state (Plan 04 output): `grep -E "TeamEmptyState" src/routes/app/team/TeamPage.tsx` returns 1
    - Help no-results (Plan 05 output): `grep -E "HelpNoResultsState" src/routes/app/help/HelpPage.tsx` returns 1
    - Help article-not-found: `grep -E "Article not found" src/routes/app/help/HelpArticlePage.tsx` returns 1
    If Reports filtered-empty is absent, this is a Phase 4 carry-over — flag for resolution (do NOT add new logic in Phase 5; surface to user as UAT item).

    Gate 10 (UI-02 toast completeness — call-site presence check): grep across the codebase for `notifications.show(` and inventory the call sites. Expected coverage (per UI-SPEC §"UI-02 — Toast notifications" table):
    - `src/tasks/components/TaskFormModal.tsx` — Task created / Task saved toast call(s) (Phase 4)
    - `src/routes/app/lists/ListsPage.tsx` or related — Task deleted toast (Phase 4)
    - `src/routes/app/settings/` — Profile saved + Workspace saved toasts (Phase 4)
    - `src/team/components/InviteTeammateModal.tsx` — Invite sent toast (Plan 04)
    - `src/routes/app/team/TeamPage.tsx` — Role updated toast (Plan 04)
    If any expected call site is missing, that's a regression — flag it as a UAT item; do NOT introduce new toast call sites in Phase 5 outside the ones Plan 04 ships.

    Gate 11 (UI-03 destructive confirms — no new flows): Confirm presence of existing Delete-task + Reset-demo-data confirms (Phase 4 locks):
    - `grep -E "DeleteConfirmModal" src/routes/app/lists/ListsPage.tsx` returns at least 1
    - `grep -E "ResetDemoDataModal" src/routes/app/settings/` returns at least 1
    Phase 5 ships NO new destructive flows ("Remove member" is `<deferred>`). If any new destructive modal was accidentally added, remove it (out of scope for Phase 5).

    Gate 12 (SHELL-04 deep-link integrity — implicit; manual): Document in 05-VERIFICATION.md that /app/help/{slug} is a new deep-linkable route landing in Phase 5. The structural deep-link guarantee (Vite SPA fallback + createBrowserRouter) is inherited from Phase 1 — Phase 5 adds no new routing layer; the nested-child edit inherits the same fallback. No automated test required.

    For ANY fix applied, the file MUST end up still passing `npm run typecheck` and `npm run lint`. No fix should alter functional behavior — polish edits are typography/spacing/color/heading-level only. Surface to the user as a checkpoint any fix that requires a behavioral judgment (e.g., a missing toast call site you're tempted to add).

    Generate `.planning/phases/05-team-help-polish/05-VERIFICATION.md` capturing each gate's outcome (Pass / Fail+fix-applied / Fail+UAT-flagged) plus a "User-eye items deferred to /gsd-verify-phase UAT" list per D-17 (e.g., "could this pass for a real B2B SaaS in a screenshot?", dark-mode contrast on Team table cells, dark-mode contrast on Help no-results state, dark-mode contrast on Help article body — these are user-judgment items that grep cannot resolve).
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && npm run typecheck 2>&1 | tail -10 && npm run lint 2>&1 | tail -10 && npm run build 2>&1 | tail -10 && echo "--- GATE 1 (canvas):" && grep -rnE "<canvas" src/ | wc -l && echo "--- GATE 2 (sr-ignore):" && grep -cE "pendo-sr-ignore" src/ui/primitives/PasswordInput.tsx && echo "--- GATE 3 (hand-typed team/help pendo-id):" && grep -rE "data-pendo-id=\"(team|help)\\." src/ | wc -l && echo "--- GATE 5a (fw 700):" && grep -rE "fw=\\{700\\}" src/ | wc -l && echo "--- GATE 6 (raw px in phase 5 CSS):" && grep -rE "[0-9]+px\\b" src/team/components/ src/help/components/ 2>/dev/null | grep -E "\\.css:" | wc -l && echo "--- GATE 7 (hex in phase 5 source):" && grep -rE "#[0-9a-fA-F]{3,6}\\b" src/team/ src/help/ src/seed/ 2>/dev/null | wc -l && echo "--- GATE 8 (Paper shadow):" && grep -rE "<Paper[^>]*shadow=" src/routes/app/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` all exit 0
    - GATE 1: `grep -rnE "<canvas" src/` returns 0 lines
    - GATE 2: `grep -cE "pendo-sr-ignore" src/ui/primitives/PasswordInput.tsx` returns >= 1
    - GATE 3: `grep -rE "data-pendo-id=\"(team|help)\\." src/` returns 0 lines (the team and help namespace string literals exist only inside PENDO_IDS.ts; consumers use the registry)
    - GATE 5a: `grep -rE "fw=\\{700\\}" src/` returns 0 lines
    - GATE 5b: `grep -rE "size=\"xs\"" src/team/ src/help/ src/seed/ src/routes/app/team/ src/routes/app/help/` returns 0 lines (Phase 5-touched surface free of xs)
    - GATE 6: `grep -rE "[0-9]+px\\b" src/team/components/ src/help/components/ src/seed/` filtered to `*.css` files returns 0 lines (no raw px in Phase 5 CSS modules)
    - GATE 7: `grep -rE "#[0-9a-fA-F]{3,6}\\b" src/team/ src/help/ src/seed/` returns 0 matches in component bodies (CSS-var references via `var(--mantine-color-*)` only)
    - GATE 8: `grep -rE "<Paper[^>]*shadow=" src/routes/app/` returns 0 lines
    - GATE 9: All seven expected empty-state grep checks pass; ANY missing state is recorded in 05-VERIFICATION.md as a UAT item (NOT silently fixed)
    - GATE 11: Existing destructive confirms intact; no new destructive flows introduced by Phase 5
    - File `.planning/phases/05-team-help-polish/05-VERIFICATION.md` exists and lists each of Gates 1–12 with Pass/Fail outcomes plus the User-eye UAT items per D-17
    - No functional behavior changed by polish edits — only typography, spacing, color, heading levels, and `<Paper>` shadow→withBorder swaps may be modified
  </acceptance_criteria>
  <done>Automated readiness gates pass; polish drift fixed in place; user-judgment items captured for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual "Looks Done But Isn't" walkthrough + UAT capture</name>
  <files>.planning/phases/05-team-help-polish/05-VERIFICATION.md</files>
  <action>Run the user walkthrough described under `<how-to-verify>`. For each item that cannot be cleanly approved, append a UAT entry to `.planning/phases/05-team-help-polish/05-VERIFICATION.md` per D-17 (NOT a new GSD plan — UAT entries are picked up by `/gsd-verify-phase` per the Phase 4 commit 5195b3b precedent). Wait for the user's resume signal.</action>
  <verify>Manual — `.planning/phases/05-team-help-polish/05-VERIFICATION.md` contains either explicit per-page approval or itemized UAT entries; user types "approved" or specifies fix-ups.</verify>
  <done>User has typed "approved" (Phase 5 polish complete) OR Task 1 of this plan is reopened to apply listed fix-ups.</done>
  <what-built>
    Phase 5 ships the Team page, Help list + detail, idempotent seeding across teammates and tasks (single meta.seededAt gate), assignee-source swap, and a polish pass driven by automated gates. Task 1 has documented gate outcomes in 05-VERIFICATION.md. This checkpoint captures the user-judgment items that automated grep cannot conclude.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` and open http://localhost:5173.
    2. Sign in (or sign up if no account exists) and ensure you land on /app.
    3. Walk every /app/* page in BOTH light and dark color schemes (use the Settings → Preferences theme toggle to switch). For each page, confirm:
       - Page title is `Title order={3}` size+weight (visual: ~22px semi-bold).
       - All cards/surfaces use `<Paper withBorder>` (no drop shadow on page surfaces; AppShell may shadow).
       - Spacing is consistent — no rogue tight or loose gaps.
       - Color: hover/focus indicators are indigo accent; "Invited" badge is yellow; "Owner" Select is disabled.
    4. /app/team specific checks:
       - Owner row is at top of the table.
       - Inline role Select for the Owner is disabled and shows "Owner" only.
       - Non-Owner rows offer Admin/Member/Viewer.
       - Click "Invite teammate" → modal opens; submit a NEW email → row appears with yellow Invited badge + green "Invite sent" toast.
       - Submit a DUPLICATE email → see "{email} is already a teammate." inline error.
       - Change a non-Owner role → green "Role updated" toast; reload page; role persisted.
    5. /app/help specific checks:
       - ≥8 articles grouped under uppercase dimmed topic labels.
       - Type a keyword → list filters live with ~150ms debounce.
       - Type a no-match query → "No articles match \"{q}\". Try a different keyword." compact state with "Clear search" link; click Clear → list returns.
       - Click an article → /app/help/{slug} detail renders topic + title + body paragraphs + "← Back to Help".
       - Navigate to /app/help/this-is-not-a-real-slug → "Article not found" state with working back link.
       - Refresh on /app/help/{slug} → deep-link works (no fallback to root).
    6. Reset demo data check (UI-03 regression):
       - Settings → Reset demo data → confirm → reload.
       - On next sign-in, seedDemoData reruns: teammates re-seeded (including Owner at top) AND tasks re-seeded (assignees drawn from the new teammate list).
    7. Cross-page polish check:
       - Visit Dashboard / Lists / Reports / Settings / Team / Help in sequence. Does the typography feel uniform? Spacing rhythmic? Color palette consistent? Empty states polished?
       - The "could this pass for a real B2B SaaS in a screenshot?" gestalt question per ROADMAP Success Criteria #5.
    8. Pendo-readiness spot-check (DevTools):
       - Right-click a Team row's role Select → Inspect → confirm BOTH `data-pendo-id="team.row.role-select"` AND `data-pendo-teammate-id="..."` on the element.
       - Right-click a Help article row → Inspect → confirm BOTH `data-pendo-id="help.article.row"` AND `data-pendo-article-slug="..."`.
       - Right-click any password input (Sign-in page) → Inspect → confirm the input has class containing `pendo-sr-ignore`.
    9. Any item that fails or feels off → record in `.planning/phases/05-team-help-polish/05-VERIFICATION.md` under a "UAT items" section per D-17 (NOT a new GSD plan — these are user-eye items the orchestrator will surface as UAT to /gsd-verify-phase per the Phase 4 commit 5195b3b precedent).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 polish complete, OR list specific items needing fix-up (planner will reopen Task 1 of this plan for targeted edits).</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run build` all pass
- All 12 automated gates in Task 1 produce documented outcomes in 05-VERIFICATION.md
- `npm run dev` starts cleanly; manual walkthrough in Task 2 captures user-eye UAT items
- No functional regressions (Phase 4 confirms intact; Phase 5 features as defined in Plans 01–05)
</verification>

<success_criteria>
- UI-01: All seven expected empty states verified present (Dashboard, Lists hero, Lists filtered, Reports filtered, Team hero, Help no-results, Help article-not-found)
- UI-02: All seven expected toast call sites verified present (task create/save/delete, profile save, workspace save, invite sent, role updated)
- UI-03: No new destructive flows; existing Delete-task + Reset-demo-data confirms intact
- UI-04: Automated gates 4–8 pass; manual gestalt judgment captured at checkpoint
- PEN-07/08/09: Automated regression gates pass; DevTools spot-check confirms registry-only data-pendo-id values + .pendo-sr-ignore on password inputs + zero canvas
- D-17: No DEMO-READY-AUDIT.md file generated; 05-VERIFICATION.md captures gate outcomes + UAT deferrals
</success_criteria>

<output>
After completion, create `.planning/phases/05-team-help-polish/05-06-SUMMARY.md` documenting:
- Each gate's pass/fail outcome
- Any targeted polish edits applied (file + line)
- The UAT items deferred to /gsd-verify-phase (user-eye gestalt judgments, dark-mode contrast spot-checks)
- Confirmation that no functional behavior was altered by polish edits
</output>
