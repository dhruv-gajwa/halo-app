---
phase: 05-team-help-polish
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - src/team/components/TeamTable.tsx
  - src/team/components/TeamTable.module.css
  - src/team/components/InviteTeammateModal.tsx
  - src/team/components/TeamEmptyState.tsx
  - src/routes/app/team/TeamPage.tsx
  - src/pendo/PENDO_IDS.ts
autonomous: true
requirements: [TEAM-01, TEAM-02, TEAM-03, UI-01, UI-02]

must_haves:
  truths:
    - "/app/team renders the Visitor-as-Owner row at index 0, then status='active' teammates sorted by firstName asc, then status='invited' sorted by invitedAt desc"
    - "The Team table has exactly 4 columns (Name, Email, Role, Last active) and shows an 'Invited' yellow badge in the Name cell when status==='invited'"
    - "Owner cannot be demoted via the inline role dropdown (D-02): the Owner row's Select shows 'Owner' as a disabled option (Mechanism A)"
    - "Non-Owner rows offer Admin/Member/Viewer in the role dropdown — Owner is NEVER in the options for non-Owner rows"
    - "Inline role change calls teamsRepo.updateTeammate, refreshes the table state, and emits a green 'Role updated' toast (D-05, UI-02)"
    - "'Invite teammate' button on the page header AND on the empty-state CTA open the same Mantine Modal"
    - "Invite form rejects empty email ('Enter an email.'), malformed email ('Enter a valid email.'), and duplicate emails (case-insensitive check via teamsRepo.findTeammateByEmail) with '{email} is already a teammate.' (D-03)"
    - "On submit, a teammate row is created with status='invited', invitedAt=ISO now, lastActiveAt=null, firstName derived from email local-part (split on '.'/'_', Title-Case each segment), lastName='', avatar=null; modal closes; green 'Invite sent — Sent to {email}' toast appears"
    - "Owner role is NOT in the Invite modal Role options (D-02 — Owner is not invitable)"
    - "Every interactive element references PENDO_IDS.team.<key> — no hand-typed data-pendo-id strings outside PENDO_IDS.ts; leaf strings follow the dotted kebab-case convention (e.g., team.row.role-select) so Leaves<typeof PENDO_IDS> auto-derives the PendoId union with no type-definition edit (PEN-07, D-15)"
    - "The Team row Select includes data-pendo-teammate-id={teammate.id} (dynamic-list parameterization per CLAUDE.md + D-14)"
  artifacts:
    - path: "src/pendo/PENDO_IDS.ts"
      provides: "team namespace with leaves: header.inviteButton, table.container, row.roleSelect, invite.{modalContainer,modalEmail,modalRole,modalCancel,modalSubmit}, emptyState.{container,cta}"
      contains: "team:"
    - path: "src/team/components/TeamTable.tsx"
      provides: "Mantine native Table render with 4 columns + inline role Select per row"
    - path: "src/team/components/TeamTable.module.css"
      provides: "Cell padding rule using Mantine spacing CSS vars (no raw px)"
    - path: "src/team/components/InviteTeammateModal.tsx"
      provides: "RHF + Zod modal form with InviteFormSchema (email + workspaceRole + dedupe refine)"
    - path: "src/team/components/TeamEmptyState.tsx"
      provides: "Full-page hero empty state with IconUsers + 'No teammates yet' + Invite CTA"
    - path: "src/routes/app/team/TeamPage.tsx"
      provides: "Composer page that wires teamsRepo + components + refresh ticker + toast call sites"
  key_links:
    - from: "src/routes/app/team/TeamPage.tsx"
      to: "src/team/teamsRepo.ts"
      via: "listTeammates / updateTeammate"
      pattern: "listTeammates|updateTeammate"
    - from: "src/team/components/InviteTeammateModal.tsx"
      to: "src/team/teamsRepo.ts"
      via: "createTeammate + findTeammateByEmail (dedupe .superRefine)"
      pattern: "createTeammate|findTeammateByEmail"
    - from: "All Team components"
      to: "src/pendo/PENDO_IDS.ts (team namespace)"
      via: "pendoId={PENDO_IDS.team.<key>} on every interactive element"
      pattern: "PENDO_IDS\\.team\\."
    - from: "Team row Select"
      to: "DOM attribute"
      via: "data-pendo-teammate-id={teammate.id} dynamic-list parameterization"
      pattern: "data-pendo-teammate-id"
---

<objective>
Build the /app/team page: PENDO_IDS team namespace, three components (TeamTable, InviteTeammateModal, TeamEmptyState), and the composer page. Lands TEAM-01 (list teammates), TEAM-02 (invite modal + toast), TEAM-03 (inline role change + persist), UI-01 (empty state), UI-02 (toasts).

Purpose: Replace the existing ComingSoonCard placeholder body of TeamPage.tsx with a real implementation that consumes the data layer from Plans 01 + 03. Every Phase 5 Team page decision (D-01 through D-05, D-14) lands here.

Output: 4 new files (3 components + 1 CSS module), 2 modifications (TeamPage body + PENDO_IDS append).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-team-help-polish/05-CONTEXT.md
@.planning/phases/05-team-help-polish/05-UI-SPEC.md
@.planning/phases/05-team-help-polish/05-PATTERNS.md
@.planning/phases/05-team-help-polish/05-01-SUMMARY.md
@.planning/phases/05-team-help-polish/05-03-SUMMARY.md

<interfaces>
<!-- Plan 01 data layer outputs -->
From src/team/index.ts:
```typescript
export type Teammate = { id, firstName, lastName, email, workspaceRole, status, lastActiveAt, invitedAt, avatar }
export type WorkspaceRole = 'Owner' | 'Admin' | 'Member' | 'Viewer'
export function listTeammates(workspaceId: string): Teammate[]
export function findTeammateByEmail(workspaceId: string, email: string): Teammate | undefined
export function createTeammate(workspaceId: string, input: Omit<Teammate, 'id'>): Teammate
export function updateTeammate(workspaceId: string, id: string, patch: Partial<Omit<Teammate, 'id'>>): Teammate | undefined
```

<!-- Auth store selectors consumed by TeamPage -->
From src/auth/authStore.ts:
```typescript
useAuthStore((s) => s.currentVisitor): Visitor | null
useAuthStore((s) => s.currentWorkspace): Workspace | null
```

<!-- Wrapped UI primitives (PENDO_IDS forwarding contract) -->
From src/ui/primitives:
```typescript
export function Button(props: { pendoId: PendoId, ... })
export function TextInput(props: { pendoId: PendoId, ... })
export function Select(props: { pendoId: PendoId, ... })
```

<!-- Phase 3 relative-time helper for lastActive cell -->
From src/dashboard/relative-time.ts:
```typescript
export function formatRelative(iso: string, nowRefIso: string): string
```

<!-- Phase 4 now-ref helper -->
From src/tasks/now-ref.ts:
```typescript
export function computeNowRef(): string
```

<!-- @mantine/notifications + IconCheck pattern (Phase 4 lock) -->
```typescript
import { notifications } from '@mantine/notifications'
import { IconCheck } from '@tabler/icons-react'
notifications.show({ title, message, color: 'green', icon: <IconCheck size={18} />, autoClose: 3000 })
```

<!-- RHF + Zod resolver + mode: 'onSubmit' (Phase 2/4 lock) -->
```typescript
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
const form = useForm<InviteFormValues>({ resolver: zodResolver(InviteFormSchema), mode: 'onSubmit', defaultValues })
```

<!-- Existing TeamPage placeholder -->
From src/routes/app/team/TeamPage.tsx:
```typescript
// Currently renders a ComingSoonCard. The entire body of this component is replaced.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PENDO_IDS team namespace + TeamEmptyState + InviteTeammateModal</name>
  <files>src/pendo/PENDO_IDS.ts, src/team/components/TeamEmptyState.tsx, src/team/components/InviteTeammateModal.tsx</files>
  <read_first>
    - src/pendo/PENDO_IDS.ts (entire file — confirm append site between reports namespace and the closing `} as const`; understand the `Leaves<T>` type derivation that picks up new namespaces automatically)
    - src/tasks/components/ListsEmptyState.tsx (verbatim analog for TeamEmptyState — copy the structure, swap icon to IconUsers, swap copy and pendoId references)
    - src/tasks/components/TaskFormModal.tsx (analog for InviteTeammateModal — RHF setup, zodResolver, Modal markup, footer button order; the InviteTeammateModal is structurally simpler — no edit mode, no DatePickerInput, just 2 fields)
    - src/team/teamsRepo.ts (Plan 01 output — createTeammate + findTeammateByEmail signatures consumed by InviteTeammateModal)
    - src/ui/primitives/Button.tsx, src/ui/primitives/TextInput.tsx, src/ui/primitives/Select.tsx (confirm pendoId prop is required and forwards to data-pendo-id)
    - .planning/phases/05-team-help-polish/05-UI-SPEC.md sections "Team empty state", "Invite teammate modal", and "Pendo ID Registry — Phase 5 Extensions" (verbatim PENDO_IDS shape)
    - .planning/phases/05-team-help-polish/05-PATTERNS.md sections "src/team/components/InviteTeammateModal.tsx" and "src/team/components/TeamEmptyState.tsx" (verbatim code shape including the .superRefine email dedupe)
    - .planning/phases/05-team-help-polish/05-CONTEXT.md D-02 (Owner not invitable — role options are Admin/Member/Viewer only), D-03 (modal behavior, firstName derivation, toast copy), D-14 (PENDO_IDS leaves)
  </read_first>
  <action>
    Modify src/pendo/PENDO_IDS.ts — append the `team` namespace verbatim between the existing `reports` namespace and the closing `} as const` (per D-14, UI-SPEC §"Pendo ID Registry"). Leaves to add:
    - header.inviteButton → 'team.header.invite-button'
    - table.container → 'team.table.container'
    - row.roleSelect → 'team.row.role-select'
    - invite.modalContainer → 'team.invite.modal-container'
    - invite.modalEmail → 'team.invite.modal-email'
    - invite.modalRole → 'team.invite.modal-role'
    - invite.modalCancel → 'team.invite.modal-cancel'
    - invite.modalSubmit → 'team.invite.modal-submit'
    - emptyState.container → 'team.empty-state.container'
    - emptyState.cta → 'team.empty-state.cta'
    Per D-14 dynamic-list rule, leave a comment above `row` noting "consumers add data-pendo-teammate-id={teammate.id}". Do NOT touch any existing namespace. Do NOT add an `rc` namespace (D-10 no-op). The `PendoId` type derivation via `Leaves<typeof PENDO_IDS>` updates automatically.

    Create src/team/components/TeamEmptyState.tsx (mirrors src/tasks/components/ListsEmptyState.tsx verbatim shape):
    - Imports: `Center, Stack, Title, Text` from '@mantine/core'; `IconUsers` from '@tabler/icons-react'; `Button` from '../../ui/primitives'; `PENDO_IDS` from '../../pendo/PENDO_IDS'.
    - Props type `TeamEmptyStateProps = { workspaceName: string; onInviteClick: () => void }`.
    - Render `<Center mih={400}>` (full-page hero idiom per UI-SPEC §"Team empty state") with `data-pendo-id={PENDO_IDS.team.emptyState.container}`.
    - Inside: `<Stack align="center" gap="md">` containing:
      - `<IconUsers size={64} stroke={1.2} color="var(--mantine-color-gray-4)" />` (structural empty-state hero icon idiom inherited from Phase 3/4)
      - `<Title order={3}>No teammates yet</Title>` (Sub-heading role per UI-SPEC Typography)
      - `<Text c="dimmed" ta="center" maw={420}>Invite your first teammate to start collaborating on {workspaceName}.</Text>` (copy verbatim per UI-SPEC §"Empty-state copy")
      - `<Button variant="filled" pendoId={PENDO_IDS.team.emptyState.cta} onClick={onInviteClick}>Invite teammate</Button>` (label verbatim per UI-SPEC §"CTA labels"; no period)
    - Spacing values are locked: do NOT modify `mih={400}`, `gap="md"`, `maw={420}`, icon `size={64}` `stroke={1.2}`.

    Create src/team/components/InviteTeammateModal.tsx (mirrors src/tasks/components/TaskFormModal.tsx structurally; simpler — 2 fields, no edit mode):
    - Imports: `useForm` from 'react-hook-form'; `zodResolver` from '@hookform/resolvers/zod'; `z` from 'zod'; `Modal, Stack, Group, Title` from '@mantine/core'; `notifications` from '@mantine/notifications'; `IconCheck` from '@tabler/icons-react'; `TextInput, Select, Button` from '../../ui/primitives'; `PENDO_IDS` from '../../pendo/PENDO_IDS'; `createTeammate, findTeammateByEmail` from '../teamsRepo'; `WorkspaceRole` (type-only) from '../types'.
    - Define local `InviteFormSchema` (Phase 4 D-15 local-form-schema pattern): `z.object({ email: z.string().min(1, 'Enter an email.').email('Enter a valid email.'), workspaceRole: z.enum(['Admin', 'Member', 'Viewer'], { message: 'Pick a role.' }) }).superRefine((data, ctx) => { if (findTeammateByEmail(workspaceId, data.email)) ctx.addIssue({ code: 'custom', path: ['email'], message: `${data.email} is already a teammate.` }) })`. NOTE: `workspaceId` is captured from the component closure — define InviteFormSchema inside the component body so it has access to props.
    - Component props: `{ opened: boolean; onClose: () => void; onSuccess: () => void; workspaceId: string }`.
    - Inside the component, derive `InviteFormSchema` via a `useMemo` (so a stable reference is passed to zodResolver but the closure captures the current workspaceId), then `type InviteFormValues = z.infer<typeof InviteFormSchema>`.
    - `const form = useForm<InviteFormValues>({ resolver: zodResolver(InviteFormSchema), mode: 'onSubmit', defaultValues: { email: '', workspaceRole: 'Member' } })` — defaultValues role is 'Member' per UI-SPEC §"Field order" table.
    - `onSubmit = form.handleSubmit((values) => { ... })`:
      - Derive firstName from email local-part (D-03): `const localPart = values.email.split('@')[0]; const firstName = localPart.split(/[._]/).map(seg => seg.charAt(0).toUpperCase() + seg.slice(1).toLowerCase()).join(' ')` (split on `.` or `_`, Title-Case each segment — planner picked this approach per CONTEXT discretion because it produces 'Alex Chen' from 'alex.chen@acme.com').
      - Call `createTeammate(workspaceId, { firstName, lastName: '', email: values.email.toLowerCase(), workspaceRole: values.workspaceRole, status: 'invited', lastActiveAt: null, invitedAt: new Date().toISOString(), avatar: null })`.
      - Emit toast: `notifications.show({ title: 'Invite sent', message: \`Sent to ${values.email}\`, color: 'green', icon: <IconCheck size={18} />, autoClose: 3000 })` (copy verbatim per UI-SPEC §"Toast notifications").
      - Call `onSuccess()` and `onClose()`. Reset form via `form.reset()` AFTER onClose (or rely on `keepMounted={false}` to drop state — mirror src/tasks/components/TaskFormModal.tsx Phase 04-07 D-26 idiom).
    - Render `<Modal opened={opened} onClose={onClose} title={<Title order={3}>Invite teammate</Title>} size="md" centered keepMounted={false} data-pendo-id={PENDO_IDS.team.invite.modalContainer}>`.
    - Inside Modal: `<form onSubmit={onSubmit} noValidate>` wrapping:
      - `<Stack gap="md">` with two fields:
        - `<TextInput {...form.register('email')} label="Email" type="email" required error={form.formState.errors.email?.message} pendoId={PENDO_IDS.team.invite.modalEmail} />`
        - `<Select label="Role" data={[{ value: 'Admin', label: 'Admin' }, { value: 'Member', label: 'Member' }, { value: 'Viewer', label: 'Viewer' }]} value={form.watch('workspaceRole') ?? null} onChange={(value) => { if (value && (['Admin','Member','Viewer'] as const).includes(value as WorkspaceRole)) form.setValue('workspaceRole', value as WorkspaceRole, { shouldDirty: true }) }} allowDeselect={false} error={form.formState.errors.workspaceRole?.message} pendoId={PENDO_IDS.team.invite.modalRole} />` — Owner is NEVER in the options (D-02).
      - `<Group justify="flex-end" mt="lg" gap="md">` containing:
        - `<Button variant="default" type="button" pendoId={PENDO_IDS.team.invite.modalCancel} onClick={onClose}>Cancel</Button>` (left)
        - `<Button variant="filled" type="submit" pendoId={PENDO_IDS.team.invite.modalSubmit}>Send invite</Button>` (right; primary action; label verbatim per UI-SPEC)
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && npm run typecheck 2>&1 | tail -20 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/pendo/PENDO_IDS.ts` contains a `team:` object with all 10 leaves listed in D-14 (grep: `grep -E "(team\\.header\\.invite-button|team\\.table\\.container|team\\.row\\.role-select|team\\.invite\\.modal-container|team\\.invite\\.modal-email|team\\.invite\\.modal-role|team\\.invite\\.modal-cancel|team\\.invite\\.modal-submit|team\\.empty-state\\.container|team\\.empty-state\\.cta)" src/pendo/PENDO_IDS.ts | wc -l` is >= 10)
    - No new `rc` namespace added (grep: `grep -E "^\\s*rc:\\s*\\{" src/pendo/PENDO_IDS.ts | wc -l` is 0 — D-10 no-op verified)
    - Existing namespaces still intact — file still contains `lists:`, `settings:`, `reports:` (grep: `grep -cE "^\\s*(lists|settings|reports):\\s*\\{" src/pendo/PENDO_IDS.ts` is >= 3)
    - File `src/team/components/TeamEmptyState.tsx` exists; renders IconUsers + Title with 'No teammates yet' (grep: `grep -E "No teammates yet" src/team/components/TeamEmptyState.tsx` returns 1 match; `grep -E "IconUsers" src/team/components/TeamEmptyState.tsx` returns at least 1 match)
    - TeamEmptyState references PENDO_IDS.team.emptyState.* via the registry — NOT hand-typed (grep: `grep -E "PENDO_IDS\\.team\\.emptyState\\.(container|cta)" src/team/components/TeamEmptyState.tsx` returns >= 2 matches; `grep -E "data-pendo-id=\"team\\." src/team/components/TeamEmptyState.tsx | wc -l` is 0 — no hand-typed string)
    - File `src/team/components/InviteTeammateModal.tsx` exists; uses RHF + zodResolver + mode 'onSubmit' (grep: `grep -E "zodResolver\\(InviteFormSchema\\)|mode: 'onSubmit'" src/team/components/InviteTeammateModal.tsx | wc -l` is >= 2)
    - InviteFormSchema includes .superRefine that calls findTeammateByEmail (grep: `grep -cE "(superRefine|findTeammateByEmail)" src/team/components/InviteTeammateModal.tsx` is >= 2)
    - Duplicate-email error copy matches D-03 (grep: `grep -E "is already a teammate" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - Owner is NOT in the Role Select options — only Admin/Member/Viewer (grep: `grep -E "value: 'Owner'" src/team/components/InviteTeammateModal.tsx | wc -l` is 0; `grep -cE "value: '(Admin|Member|Viewer)'" src/team/components/InviteTeammateModal.tsx` is 3)
    - onSubmit derives firstName from email local-part by splitting on `.` or `_` (grep: `grep -E "split\\(/\\[\\._\\]/\\)" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - onSubmit creates teammate with status: 'invited' and invitedAt set (grep: `grep -cE "(status: 'invited'|invitedAt:)" src/team/components/InviteTeammateModal.tsx` is >= 2)
    - Toast emits 'Invite sent' with 'Sent to ' message (grep: `grep -E "'Invite sent'" src/team/components/InviteTeammateModal.tsx` returns 1 match; `grep -E "Sent to \\$\\{" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - Cancel button left, Send invite button right inside `<Group justify="flex-end">` footer (grep: `grep -E "justify=\"flex-end\"" src/team/components/InviteTeammateModal.tsx` returns 1 match; `grep -E "Send invite" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - `npm run typecheck` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>PENDO_IDS team namespace appended; TeamEmptyState + InviteTeammateModal land with locked copy + locked validators + locked toast.</done>
</task>

<task type="auto">
  <name>Task 2: TeamTable + TeamTable.module.css + TeamPage composer</name>
  <files>src/team/components/TeamTable.tsx, src/team/components/TeamTable.module.css, src/routes/app/team/TeamPage.tsx</files>
  <read_first>
    - src/tasks/components/TaskTable.tsx (analog — Paper wrap idiom, Table headers, row component pattern; UI-SPEC chose Mantine native `<Table>` for TeamTable, NOT TanStack — read for the Paper wrap shape, cell rendering, dynamic-list parameterization)
    - src/tasks/components/TaskTable.module.css (analog for cell padding rule using Mantine spacing CSS vars — `padding: var(--mantine-spacing-sm) var(--mantine-spacing-md)`)
    - src/routes/app/lists/ListsPage.tsx (analog composer shape — Group header + table-or-empty + modal stacking + refresh-ticker useState pattern; the Phase 4 'belt-and-suspenders' defensive narrowing line `if (!workspaceId || !visitor || !workspace) return <></>` is the pattern)
    - src/dashboard/relative-time.ts (formatRelative API consumed by the Last active cell)
    - src/tasks/now-ref.ts (computeNowRef helper)
    - src/auth/authStore.ts (currentVisitor, currentWorkspace selectors — workspace.companyName is what gets passed to TeamEmptyState's workspaceName prop)
    - src/team/teamsRepo.ts (Plan 01 output — listTeammates, updateTeammate)
    - .planning/phases/05-team-help-polish/05-UI-SPEC.md sections "Team page (/app/team)", "Team table", "Role cell — two acceptable mechanisms" (Mechanism A is preferred per the UI-SPEC; use it)
    - .planning/phases/05-team-help-polish/05-PATTERNS.md sections "src/team/components/TeamTable.tsx" and "src/routes/app/team/TeamPage.tsx" (verbatim code shape, including CSS module + Mantine Table.Thead/Tbody/Tr/Th/Td idiom)
    - .planning/phases/05-team-help-polish/05-CONTEXT.md D-02 (Mechanism A: disabled-option dropdown for Owner row — preferred for guide-targeting symmetry), D-05 (inline role change behavior + toast + no confirmation modal)
  </read_first>
  <action>
    Create src/team/components/TeamTable.module.css with a single rule:
    `.cell { padding: var(--mantine-spacing-sm) var(--mantine-spacing-md); }` — vertical sm (12px), horizontal md (16px); identical to Phase 4 TaskTable.module.css. No raw px values anywhere (UI-SPEC Spacing rule).

    Create src/team/components/TeamTable.tsx using Mantine native `<Table>` (UI-SPEC §"Table library" — TanStack NOT used for this 8–12 row static-shape table):
    - Imports: `Paper, Table, Avatar, Badge, Text, Group` from '@mantine/core'; `Select` from '../../ui/primitives' (the wrapped primitive); `PENDO_IDS` from '../../pendo/PENDO_IDS'; `formatRelative` from '../../dashboard/relative-time'; `computeNowRef` from '../../tasks/now-ref'; `Teammate, WorkspaceRole` (type-only) from '../types'; CSS module via `import classes from './TeamTable.module.css'`.
    - Props: `{ teammates: Teammate[]; onRoleChange: (teammateId: string, nextRole: WorkspaceRole) => void }`.
    - Inside component: derive `nowRefIso = computeNowRef()` once via `useMemo(() => computeNowRef(), [])`.
    - Derive `orderedTeammates` via `useMemo` (UI-SPEC §"Team table" render order): Owner row first (filter `workspaceRole==='Owner'`), then `status==='active'` non-Owner sorted by firstName asc, then `status==='invited'` sorted by invitedAt desc (newest first).
    - Render outer `<Paper withBorder p={0} radius="md" style={{ overflow: 'hidden' }} data-pendo-id={PENDO_IDS.team.table.container}>`.
    - Inside Paper: `<Table className={classes.teamTable ?? undefined} verticalSpacing={0} horizontalSpacing={0}>` with `<Table.Thead>` containing 4 `<Table.Th className={classes.cell}>` columns: Name, Email, Role, Last active — each header is `<Text size="sm" fw={500} c="dimmed">{label}</Text>` per UI-SPEC Typography note 1 (fw={500} semi-emphasis exception).
    - `<Table.Tbody>` maps `orderedTeammates` to `<Table.Tr key={t.id}>` rows. Each row renders 4 `<Table.Td className={classes.cell}>` cells:
      - Name cell: `<Group gap={8}>` with `<Avatar size="sm" color="indigo" radius="xl" src={t.avatar ?? null}>{firstInitial}{lastInitial}</Avatar>`, `<Text size="sm">{t.firstName} {t.lastName}</Text>`, and if `t.status === 'invited'` a `<Badge color="yellow" variant="light" size="sm">Invited</Badge>`.
      - Email cell: `<Text size="sm">{t.email}</Text>`.
      - Role cell — Mechanism A from UI-SPEC §"Role cell" (preferred for guide-targeting symmetry per D-02):
        `<Select value={t.workspaceRole} data={t.workspaceRole === 'Owner' ? [{ value: 'Owner', label: 'Owner', disabled: true }] : [{ value: 'Admin', label: 'Admin' }, { value: 'Member', label: 'Member' }, { value: 'Viewer', label: 'Viewer' }]} disabled={t.workspaceRole === 'Owner'} onChange={(value) => { if (value && value !== t.workspaceRole) onRoleChange(t.id, value as WorkspaceRole) }} pendoId={PENDO_IDS.team.row.roleSelect} data-pendo-teammate-id={t.id} size="sm" w={140} allowDeselect={false} />`. The `data-pendo-teammate-id={t.id}` attribute MUST be present (CLAUDE.md dynamic-list parameterization rule + D-14).
      - Last active cell: `<Text size="sm" c={t.lastActiveAt ? undefined : 'dimmed'}>{t.lastActiveAt ? formatRelative(t.lastActiveAt, nowRefIso) : '—'}</Text>`.
    - Initials helper: `const firstInitial = t.firstName[0] ?? ''; const lastInitial = t.lastName[0] ?? ''` then uppercased; if both empty render the email's first character as a fallback.

    Modify src/routes/app/team/TeamPage.tsx — replace the entire body (currently a ComingSoonCard placeholder) with the composer per PATTERNS.md "src/routes/app/team/TeamPage.tsx":
    - Imports: `useState, useMemo` from 'react'; `Stack, Group, Title` from '@mantine/core'; `notifications` from '@mantine/notifications'; `IconUserPlus, IconCheck` from '@tabler/icons-react'; `Button` from '../../../ui/primitives'; `PENDO_IDS` from '../../../pendo/PENDO_IDS'; `useAuthStore` from '../../../auth/authStore'; `listTeammates, updateTeammate` from '../../../team/teamsRepo'; `TeamTable, TeamEmptyState, InviteTeammateModal` from '../../../team/components/{...}'; `WorkspaceRole` (type-only) from '../../../team/types'.
    - Inside the component:
      - `const visitor = useAuthStore((s) => s.currentVisitor); const workspace = useAuthStore((s) => s.currentWorkspace); const workspaceId = workspace?.id`.
      - `const [refreshKey, setRefreshKey] = useState(0); const refresh = () => setRefreshKey(k => k + 1)`.
      - `const [inviteOpen, setInviteOpen] = useState(false)`.
      - `const teammates = useMemo(() => workspaceId ? listTeammates(workspaceId) : [], [workspaceId, refreshKey])` — refreshKey in deps array forces re-read after mutations.
      - Defensive narrowing: `if (!workspaceId || !visitor || !workspace) return <></>` (mirrors ListsPage.tsx line 100 belt-and-suspenders).
      - `const handleRoleChange = (teammateId, nextRole) => { updateTeammate(workspaceId, teammateId, { workspaceRole: nextRole }); notifications.show({ title: 'Role updated', message: '', color: 'green', icon: <IconCheck size={18} />, autoClose: 3000 }); refresh() }` — exact toast copy per D-05 + UI-SPEC §"Toast notifications" (title 'Role updated', empty message).
    - JSX (UI-SPEC §"Team page (/app/team)"):
      - Outer `<Stack gap="lg">`.
      - Header `<Group justify="space-between" align="center">`: `<Title order={3}>Team</Title>` and a conditional Invite teammate `<Button variant="filled" leftSection={<IconUserPlus size={16} />} pendoId={PENDO_IDS.team.header.inviteButton} onClick={() => setInviteOpen(true)}>Invite teammate</Button>` — rendered only when `teammates.length > 0` (when zero, the empty-state CTA is the entry point).
      - Conditional body: if `teammates.length === 0` render `<TeamEmptyState workspaceName={workspace.companyName} onInviteClick={() => setInviteOpen(true)} />`; otherwise render `<TeamTable teammates={teammates} onRoleChange={handleRoleChange} />`.
      - Always-mounted `<InviteTeammateModal opened={inviteOpen} onClose={() => setInviteOpen(false)} onSuccess={refresh} workspaceId={workspaceId} />` (refresh ticker after successful invite triggers table re-read showing the new row).
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && npm run typecheck 2>&1 | tail -20 && npm run lint 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/team/components/TeamTable.module.css` exists with a `.cell` rule using Mantine spacing CSS vars (grep: `grep -E "padding:\\s*var\\(--mantine-spacing-sm\\)\\s*var\\(--mantine-spacing-md\\)" src/team/components/TeamTable.module.css` returns 1 match)
    - No raw px values in TeamTable.module.css (grep: `grep -E "[0-9]+px" src/team/components/TeamTable.module.css | wc -l` is 0)
    - File `src/team/components/TeamTable.tsx` uses Mantine native Table — `Table.Thead`, `Table.Tbody`, `Table.Tr`, `Table.Th`, `Table.Td` (grep: `grep -cE "Table\\.(Thead|Tbody|Tr|Th|Td)" src/team/components/TeamTable.tsx` is >= 5)
    - No TanStack imports in TeamTable.tsx (grep: `grep -E "@tanstack/react-table" src/team/components/TeamTable.tsx | wc -l` is 0 — D-of-UI-SPEC choice)
    - TeamTable renders exactly 4 column headers: Name, Email, Role, Last active (grep: `grep -cE ">(Name|Email|Role|Last active)<" src/team/components/TeamTable.tsx` is >= 4)
    - Owner-row Select uses disabled-option Mechanism A — `disabled: true` and `disabled={t.workspaceRole === 'Owner'}` both present (grep: `grep -cE "(disabled: true|disabled=\\{[^}]*'Owner')" src/team/components/TeamTable.tsx` is >= 2)
    - Non-Owner Select options are exactly Admin/Member/Viewer (grep: `grep -cE "value: '(Admin|Member|Viewer)'" src/team/components/TeamTable.tsx` is 3; `grep -cE "value: 'Owner'" src/team/components/TeamTable.tsx` is 1 — the single Owner literal appears only inside the disabled-option branch)
    - Dynamic-list rule: row Select includes `data-pendo-teammate-id={t.id}` (grep: `grep -E "data-pendo-teammate-id=\\{t\\.id\\}" src/team/components/TeamTable.tsx` returns at least 1 match)
    - PENDO_IDS references via the registry (grep: `grep -cE "PENDO_IDS\\.team\\.(row\\.roleSelect|table\\.container)" src/team/components/TeamTable.tsx` is >= 2)
    - 'Invited' badge yellow + variant light (grep: `grep -E "color=\"yellow\".*variant=\"light\"" src/team/components/TeamTable.tsx` returns at least 1 match)
    - formatRelative used in Last active cell (grep: `grep -E "formatRelative\\(" src/team/components/TeamTable.tsx` returns at least 1 match)
    - File `src/routes/app/team/TeamPage.tsx` no longer renders ComingSoonCard (grep: `grep -E "ComingSoonCard" src/routes/app/team/TeamPage.tsx | wc -l` is 0)
    - TeamPage imports the three Team components (grep: `grep -cE "from '\\.\\./\\.\\./\\.\\./team/components/(TeamTable|TeamEmptyState|InviteTeammateModal)'" src/routes/app/team/TeamPage.tsx` is >= 3)
    - TeamPage has refresh ticker (grep: `grep -cE "(refreshKey|setRefreshKey)" src/routes/app/team/TeamPage.tsx` is >= 2)
    - Role-update toast copy 'Role updated' present (grep: `grep -E "'Role updated'" src/routes/app/team/TeamPage.tsx` returns 1 match)
    - Defensive narrowing line present (grep: `grep -E "if \\(!workspaceId" src/routes/app/team/TeamPage.tsx` returns at least 1 match)
    - `Title order={3}>Team` page title (grep: `grep -E "Title order=\\{3\\}>Team" src/routes/app/team/TeamPage.tsx` returns 1 match)
    - No hand-typed Pendo IDs across the team module (grep: `grep -rE "data-pendo-id=\"team\\." src/team/ src/routes/app/team/ | wc -l` is 0 — every team-namespace string flows from PENDO_IDS.ts)
    - `npm run typecheck`, `npm run lint`, `npm run build` all exit 0
  </acceptance_criteria>
  <done>Visiting /app/team in the running dev server shows the Visitor-as-Owner row plus seeded teammates; Invite teammate opens the modal; submitting creates an invited row + 'Invite sent' toast; changing a non-Owner role shows 'Role updated' toast and persists across refresh; the Owner row's role Select is disabled.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run build` all pass
- `grep -rE "data-pendo-id=\"" src/team/ src/routes/app/team/` returns ZERO matches outside PENDO_IDS-driven values (no hand-typed strings)
- `grep -rE "@tanstack/react-table" src/team/` returns ZERO matches (Mantine native Table per UI-SPEC)
- `grep -rE "[0-9]+px\b" src/team/components/*.css` returns ZERO matches (no raw px)
- Manual smoke (developer can verify after execution): sign in, navigate /app/team → see Owner row at top + invited badge for invited rows; click Invite teammate → modal opens; submit with a duplicate email → see '{email} is already a teammate.' error; submit with a valid new email → row appears with 'Invited' badge + green toast; change a non-Owner role → green 'Role updated' toast; reload page → mutations persist.
</verification>

<success_criteria>
- TEAM-01: /app/team lists teammates with name, email, role, last-active columns and Visitor-as-Owner first
- TEAM-02: Invite modal creates 'invited' rows; 'Invite sent' toast confirms
- TEAM-03: Inline role-Select persists via teamsRepo.updateTeammate
- UI-01 (Team partial): Empty state with primary CTA established
- UI-02 (Team partial): Invite + role-change toasts shipped
- PEN-07: Every interactive element reads from PENDO_IDS.team.*; no hand-typed strings
- D-02: Owner row's role Select is disabled; Owner not in non-Owner options or Invite modal options
- D-14: data-pendo-teammate-id={t.id} on row Select (dynamic-list parameterization)
</success_criteria>

<output>
After completion, create `.planning/phases/05-team-help-polish/05-04-SUMMARY.md` documenting:
- Mechanism A chosen for D-02 (disabled-option dropdown) and rationale
- firstName derivation rule chosen (split-on-`.`/`_` Title-Case)
- Avatar render behavior (faker URL with initials fallback via Mantine Avatar children)
- Any deviations from PATTERNS.md
</output>
