---
phase: 05-team-help-polish
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-04"]
files_modified:
  - src/help/components/HelpSearchInput.tsx
  - src/help/components/HelpList.tsx
  - src/help/components/HelpNoResultsState.tsx
  - src/routes/app/help/HelpPage.tsx
  - src/routes/app/help/HelpArticlePage.tsx
  - src/router.tsx
  - src/pendo/PENDO_IDS.ts
autonomous: true
requirements: [HELP-01, HELP-02, HELP-03, HELP-04, UI-01]

must_haves:
  truths:
    - "/app/help lists ≥8 articles grouped by topic with a visible 'TOPIC' uppercase dimmed label above each group (HELP-01)"
    - "Search input is a TextInput with IconSearch leftSection; query is debounced 150ms via @mantine/hooks useDebouncedValue (D-08)"
    - "Search match is case-insensitive substring over title + keywords[].join(' ') + summary — body is NOT searched (D-08)"
    - "Empty query renders all articles grouped by topic; non-empty query with zero matches renders HelpNoResultsState with copy 'No articles match \"{query}\". Try a different keyword.' (D-08)"
    - "Clicking an article row navigates to /app/help/{slug} via React Router Link (HELP-03)"
    - "Each article row carries data-pendo-id={PENDO_IDS.help.article.row} AND a dynamic data-pendo-article-slug={article.slug} (D-14 + CLAUDE.md dynamic-list rule)"
    - "/app/help/{slug} renders article topic + title + body paragraphs (split on '\\n\\n') + 'Back to Help' anchor (HELP-03)"
    - "When the slug does not match any article, /app/help/{slug} renders an 'Article not found' state with a Back to Help anchor (NOT a router-level 404)"
    - "src/router.tsx adds a nested child route :slug under the existing /app/help route — first router.tsx edit since Phase 3 D-01 lock (D-07)"
    - "No new help-anchor markup added (no IconHelp top-bar button, no floating '?' FAB) — HELP-04 is satisfied by the existing PENDO_IDS.nav.help side-nav link (D-10)"
    - "PENDO_IDS.help namespace appended with leaves: search, topic.container, article.{row,detailBackLink}, emptyState.container, noResults.{container,clearLink} (D-14); leaves follow the dotted kebab-case leaf-string convention (e.g., help.article.detail-back-link) and Leaves<typeof PENDO_IDS> picks up the new namespace automatically — no PendoId type-definition change (D-15)"
  artifacts:
    - path: "src/pendo/PENDO_IDS.ts"
      provides: "help namespace appended"
      contains: "help:"
    - path: "src/help/components/HelpSearchInput.tsx"
      provides: "Controlled TextInput with IconSearch + pendoId"
    - path: "src/help/components/HelpList.tsx"
      provides: "Topic-grouped article list with anchor rows"
    - path: "src/help/components/HelpNoResultsState.tsx"
      provides: "Compact no-search-results state with Clear search anchor"
    - path: "src/routes/app/help/HelpPage.tsx"
      provides: "Composer with debounced search + topic grouping + no-results branch"
    - path: "src/routes/app/help/HelpArticlePage.tsx"
      provides: "Detail view + article-not-found state"
    - path: "src/router.tsx"
      provides: "Nested :slug child route under /app/help (D-07)"
      contains: "HelpArticlePage"
  key_links:
    - from: "src/routes/app/help/HelpPage.tsx"
      to: "@mantine/hooks useDebouncedValue"
      via: "150ms debounce per D-08"
      pattern: "useDebouncedValue\\(query, 150\\)"
    - from: "src/routes/app/help/HelpPage.tsx"
      to: "src/help/helpArticles.ts"
      via: "listHelpArticles() filtered against debouncedQuery"
      pattern: "listHelpArticles"
    - from: "src/routes/app/help/HelpArticlePage.tsx"
      to: "src/help/helpArticles.ts"
      via: "getHelpArticleBySlug(useParams.slug)"
      pattern: "getHelpArticleBySlug"
    - from: "src/router.tsx"
      to: "src/routes/app/help/HelpArticlePage.tsx"
      via: "Nested child route { path: ':slug', Component: HelpArticlePage } under /app/help"
      pattern: "HelpArticlePage"
---

<objective>
Build the /app/help list and /app/help/{slug} detail surfaces: PENDO_IDS help namespace, three components (HelpSearchInput, HelpList, HelpNoResultsState), the composer page, the new article detail page, and the additive router nested-child edit.

Purpose: D-07 (nested :slug child — first router edit since Phase 3 D-01), D-08 (debounced search over title/keywords/summary), D-09 already shipped via Plan 02. D-10 NO new anchor markup — HELP-04 satisfied by existing nav.help link.

Output: 4 new files (3 components + 1 page), 3 modifications (HelpPage body replace, router.tsx nested-child add, PENDO_IDS.ts help namespace append). No new runtime deps (@mantine/hooks already installed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-team-help-polish/05-CONTEXT.md
@.planning/phases/05-team-help-polish/05-UI-SPEC.md
@.planning/phases/05-team-help-polish/05-PATTERNS.md
@.planning/phases/05-team-help-polish/05-02-SUMMARY.md

<interfaces>
<!-- Plan 02 data layer outputs -->
From src/help/index.ts:
```typescript
export const HELP_ARTICLES: readonly HelpArticle[]
export type HelpArticle = { id, slug, title, topic, summary, body, keywords: string[], updatedAt }
export function listHelpArticles(): readonly HelpArticle[]
export function getHelpArticleBySlug(slug: string): HelpArticle | undefined
```

<!-- Debounce hook (already installed) -->
From @mantine/hooks:
```typescript
export function useDebouncedValue<T>(value: T, wait: number): readonly [T, () => void]
```

<!-- React Router 7 nested children pattern -->
From src/router.tsx (existing structure):
```typescript
// Inside the /app children array, currently a flat entry:
{ path: 'help', Component: HelpPage },
// D-07 transforms to nested-child form (parent still renders HelpPage list):
{
  path: 'help',
  Component: HelpPage,
  children: [
    { path: ':slug', Component: HelpArticlePage },
  ],
}
```

<!-- Existing PENDO_IDS append site -->
From src/pendo/PENDO_IDS.ts:
```typescript
// Append `help:` namespace after the new `team:` namespace from Plan 04 (Wave 3 runs after Wave 2's PENDO_IDS edit landed)
// Coordination note: Plan 04 also edits PENDO_IDS.ts. If both plans run in the same wave (Wave 3), the executor MUST serialize: read current file, append team if absent, append help if absent. The frontmatter files_modified collision is explicit — these two Wave 3 plans serialize on PENDO_IDS.ts.
```

<!-- React Router Link import location -->
```typescript
import { Link, useParams } from 'react-router'   // react-router 7+, NOT 'react-router-dom'
```
</interfaces>

<coordination_note>
Plan 04 and Plan 05 both edit `src/pendo/PENDO_IDS.ts` (adding the `team` namespace and the `help` namespace respectively). Both are Wave 3 plans. The execute-phase orchestrator must serialize these two writes — they CANNOT run in true parallel because they touch the same file. The executor for whichever plan runs second MUST:
1. Re-read PENDO_IDS.ts after the other plan's edit lands.
2. Detect the already-present sibling namespace.
3. Append only its own namespace, leaving the sibling untouched.
Both append-orders (team-then-help OR help-then-team) produce identical final output: both namespaces present.
</coordination_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PENDO_IDS help namespace + HelpSearchInput + HelpNoResultsState + HelpList</name>
  <files>src/pendo/PENDO_IDS.ts, src/help/components/HelpSearchInput.tsx, src/help/components/HelpNoResultsState.tsx, src/help/components/HelpList.tsx</files>
  <read_first>
    - src/pendo/PENDO_IDS.ts (confirm append site; if Plan 04 has already added the `team` namespace, append `help` immediately after it; if not yet present, append `help` before the closing `} as const` regardless — both orderings produce the same final output)
    - src/tasks/components/FilteredEmptyState.tsx (verbatim analog for HelpNoResultsState — same Center py="xl" + Stack + Text + Anchor component="button" idiom; copy the polymorphic Anchor S3-exception comment block)
    - src/tasks/components/TaskFiltersBar.tsx (analog for HelpSearchInput — controlled TextInput wrapped in primitive)
    - src/ui/primitives/TextInput.tsx (confirm pendoId prop contract + leftSection passthrough — Mantine TextInput accepts a `leftSection` prop natively, and the Halo wrapper forwards it via rest props)
    - src/dashboard/Dashboard.tsx (analog for activity-feed grouping shape — useful for HelpList's topic-grouped rendering pattern: Map<topic, articles[]>)
    - .planning/phases/05-team-help-polish/05-UI-SPEC.md sections "Help page (/app/help)", "Topic-grouped list layout", "Help no-results state (compact)", "Help article row", "Pendo ID Registry — Phase 5 Extensions" (verbatim shapes + locked copy)
    - .planning/phases/05-team-help-polish/05-PATTERNS.md sections "src/help/components/HelpSearchInput.tsx", "src/help/components/HelpList.tsx", "src/help/components/HelpNoResultsState.tsx" (verbatim code shape)
    - .planning/phases/05-team-help-polish/05-CONTEXT.md D-08 (debounce timing + match logic + no-results copy + Clear search anchor), D-14 (PENDO_IDS leaves), D-15 (kebab-case leaf convention)
  </read_first>
  <action>
    Modify src/pendo/PENDO_IDS.ts — append the `help` namespace. Coordinate with Plan 04's append:
    - If Plan 04's `team` namespace is already present, append `help` immediately AFTER it (before the closing `} as const`).
    - If Plan 04's `team` namespace is NOT yet present at execution time, append `help` before the closing `} as const`; Plan 04 will append `team` next to it. Both orders converge.
    - Leaves (per D-14 + UI-SPEC §"Pendo ID Registry"):
      - search → 'help.search'
      - topic.container → 'help.topic.container'
      - article.row → 'help.article.row' (consumers add data-pendo-article-slug={article.slug})
      - article.detailBackLink → 'help.article.detail-back-link'
      - emptyState.container → 'help.empty-state.container'
      - noResults.container → 'help.no-results.container'
      - noResults.clearLink → 'help.no-results.clear-link'
    - Add a comment above `article:` noting "consumers add data-pendo-article-slug={article.slug}" (dynamic-list rule).
    - Do NOT touch any existing namespace or the team namespace from Plan 04.
    - Do NOT add an `rc` namespace (D-10 no-op).

    Create src/help/components/HelpSearchInput.tsx (verbatim per PATTERNS.md §"src/help/components/HelpSearchInput.tsx"):
    - Imports: `IconSearch` from '@tabler/icons-react'; `TextInput` from '../../ui/primitives'; `PENDO_IDS` from '../../pendo/PENDO_IDS'.
    - Props: `{ value: string; onChange: (next: string) => void }`.
    - Render `<TextInput placeholder="Search articles" leftSection={<IconSearch size={16} />} value={value} onChange={(e) => onChange(e.currentTarget.value)} pendoId={PENDO_IDS.help.search} />` — copy + icon size verbatim per UI-SPEC §"Copywriting Contract" (placeholder "Search articles") and §"Design System" (icon size 16). Debounce is NOT applied here — it's a page-level concern via `useDebouncedValue` in HelpPage (D-08).

    Create src/help/components/HelpNoResultsState.tsx (mirrors src/tasks/components/FilteredEmptyState.tsx verbatim shape):
    - Imports: `Center, Stack, Text, Anchor as MantineAnchor` from '@mantine/core'; `PENDO_IDS` from '../../pendo/PENDO_IDS'.
    - Props: `{ query: string; onClear: () => void }`.
    - Render `<Center py="xl" data-pendo-id={PENDO_IDS.help.noResults.container}>` (compact py="xl" idiom — NOT mih={400}; this is the in-flow filtered-empty state, not the hero).
    - Inside: `<Stack align="center" gap="sm">` containing:
      - `<Text c="dimmed" ta="center">No articles match &quot;{query}&quot;. Try a different keyword.</Text>` (copy verbatim per UI-SPEC §"Empty-state copy"; use the HTML entity for the double quotes around `{query}` to avoid JSX-string escape confusion).
      - `<MantineAnchor component="button" type="button" size="sm" data-pendo-id={PENDO_IDS.help.noResults.clearLink} onClick={onClear}>Clear search</MantineAnchor>` — raw Mantine Anchor with explicit `data-pendo-id` because the Halo wrapper does not expose polymorphic `component` typing (S3 exception inherited from FilteredEmptyState.tsx; value still from PENDO_IDS.noResults.clearLink).
    - Include a brief comment above the Anchor explaining the polymorphic-Anchor S3 exception (mirrors FilteredEmptyState).

    Create src/help/components/HelpList.tsx (topic-grouped article list per UI-SPEC §"Topic-grouped list layout"):
    - Imports: `Stack, Text, Anchor as MantineAnchor` from '@mantine/core'; `Link` from 'react-router'; `PENDO_IDS` from '../../pendo/PENDO_IDS'; `HelpArticle` (type-only) from '../types'.
    - Props: `{ articles: readonly HelpArticle[] }`.
    - Private `groupByTopic(articles)` helper: build a Map<string, HelpArticle[]> preserving first-seen topic order (Map iteration order matches insertion order in JS); return `[...map.entries()].map(([topic, articles]) => ({ topic, articles }))`. Topic order is stable across reloads because HELP_ARTICLES is faker.seed(N)-pinned (D-09).
    - Render outer `<Stack gap="xl">` (between-group spacing).
    - Map groups to inner `<Stack key={group.topic} gap="md">` (topic-label to row-stack gap) containing:
      - `<Text size="sm" tt="uppercase" c="dimmed" fw={500}>{group.topic}</Text>` (topic-group label idiom mirroring Phase 3 KPI label idiom)
      - `<Stack gap="sm">` containing the article rows.
    - Each article row: `<MantineAnchor key={article.id} component={Link} to={\`/app/help/${article.slug}\`} underline="hover" c="inherit" data-pendo-id={PENDO_IDS.help.article.row} data-pendo-article-slug={article.slug}>` — Treatment A (flat anchor list) chosen for the polished-list aesthetic per UI-SPEC. Inside the anchor: `<Stack gap={4}>` containing `<Text size="sm" fw={500}>{article.title}</Text>` and `<Text size="sm" c="dimmed">{article.summary}</Text>`. The `data-pendo-article-slug={article.slug}` attribute is MANDATORY (CLAUDE.md dynamic-list parameterization + D-14).
    - Include a brief comment near the Anchor noting the S3-polymorphic-Anchor exception (value flows from PENDO_IDS.article.row + dynamic per-row attr).
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && npm run typecheck 2>&1 | tail -20 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/pendo/PENDO_IDS.ts` contains a `help:` object with all 7 leaves (grep: `grep -E "(help\\.search|help\\.topic\\.container|help\\.article\\.row|help\\.article\\.detail-back-link|help\\.empty-state\\.container|help\\.no-results\\.container|help\\.no-results\\.clear-link)" src/pendo/PENDO_IDS.ts | wc -l` is >= 7)
    - No `rc:` namespace exists (grep: `grep -E "^\\s*rc:\\s*\\{" src/pendo/PENDO_IDS.ts | wc -l` is 0 — D-10 no-op)
    - If Plan 04 ran first, the `team:` namespace is intact and unedited (grep: `grep -cE "^\\s*team:\\s*\\{" src/pendo/PENDO_IDS.ts` is 1 when Plan 04 already shipped — both namespaces coexist)
    - File `src/help/components/HelpSearchInput.tsx` exists; references PENDO_IDS.help.search via registry (grep: `grep -E "PENDO_IDS\\.help\\.search" src/help/components/HelpSearchInput.tsx` returns 1 match; `grep -E "data-pendo-id=\"help\\." src/help/components/HelpSearchInput.tsx | wc -l` is 0)
    - HelpSearchInput placeholder copy 'Search articles' (grep: `grep -E "placeholder=\"Search articles\"" src/help/components/HelpSearchInput.tsx` returns 1 match)
    - HelpSearchInput uses IconSearch with size 16 (grep: `grep -E "IconSearch size=\\{16\\}" src/help/components/HelpSearchInput.tsx` returns 1 match)
    - HelpSearchInput does NOT call useDebouncedValue (debounce is page-level per D-08; grep: `grep -E "useDebouncedValue" src/help/components/HelpSearchInput.tsx | wc -l` is 0)
    - File `src/help/components/HelpNoResultsState.tsx` renders compact py="xl" empty state with copy 'No articles match' (grep: `grep -E "No articles match" src/help/components/HelpNoResultsState.tsx` returns 1 match; `grep -E "py=\"xl\"" src/help/components/HelpNoResultsState.tsx` returns 1 match)
    - HelpNoResultsState uses polymorphic Anchor for the Clear search link (grep: `grep -E "component=\"button\"" src/help/components/HelpNoResultsState.tsx` returns at least 1 match) and references PENDO_IDS.help.noResults.clearLink (grep: `grep -E "PENDO_IDS\\.help\\.noResults\\.clearLink" src/help/components/HelpNoResultsState.tsx` returns 1 match)
    - File `src/help/components/HelpList.tsx` groups by topic and renders one anchor per article (grep: `grep -E "groupByTopic|Map<string, HelpArticle\\[\\]>" src/help/components/HelpList.tsx | wc -l` is >= 1)
    - HelpList article row carries BOTH PENDO_IDS.help.article.row AND data-pendo-article-slug={article.slug} (grep: `grep -E "PENDO_IDS\\.help\\.article\\.row" src/help/components/HelpList.tsx` returns at least 1 match; `grep -E "data-pendo-article-slug=\\{article\\.slug\\}" src/help/components/HelpList.tsx` returns at least 1 match)
    - HelpList topic label uses uppercase dimmed size sm (grep: `grep -cE "(tt=\"uppercase\"|c=\"dimmed\")" src/help/components/HelpList.tsx` is >= 2)
    - HelpList article-row anchor renders title (size sm, fw 500) + summary (size sm, dimmed) (grep: `grep -cE "(fw=\\{500\\}|c=\"dimmed\")" src/help/components/HelpList.tsx` is >= 2)
    - Article rows use React Router Link via Mantine Anchor component prop (grep: `grep -E "component=\\{Link\\}" src/help/components/HelpList.tsx` returns at least 1 match; `grep -E "from 'react-router'" src/help/components/HelpList.tsx` returns 1 match)
    - `npm run typecheck` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>PENDO_IDS help namespace appended; three Help UI components are independently consumable. The composer (Task 2) wires them together.</done>
</task>

<task type="auto">
  <name>Task 2: HelpPage composer + HelpArticlePage + router nested-child edit</name>
  <files>src/routes/app/help/HelpPage.tsx, src/routes/app/help/HelpArticlePage.tsx, src/router.tsx</files>
  <read_first>
    - src/router.tsx (entire file — confirm current /app children array shape around line 120; the existing `{ path: 'help', Component: HelpPage }` entry is what gets transformed into a nested-children parent per D-07)
    - src/routes/app/help/HelpPage.tsx (current — body is the ComingSoonCard placeholder; entire body gets replaced)
    - src/routes/app/lists/ListsPage.tsx (analog composer shape — useMemo for filtered data, conditional empty/content branches)
    - src/help/helpArticles.ts (Plan 02 output — listHelpArticles + getHelpArticleBySlug signatures)
    - .planning/phases/05-team-help-polish/05-UI-SPEC.md sections "Help page (/app/help)", "Help detail page (/app/help/:slug)", "Router Edit (D-07)" (verbatim composer shapes + nested vs flat router note)
    - .planning/phases/05-team-help-polish/05-PATTERNS.md sections "src/routes/app/help/HelpPage.tsx", "src/routes/app/help/HelpArticlePage.tsx", "src/router.tsx" (verbatim code shapes)
    - .planning/phases/05-team-help-polish/05-CONTEXT.md D-07 (nested child route shape; planner picks nested OR flat — UI-SPEC line 840 says either valid), D-08 (debounce + match logic), D-10 (NO new help-anchor markup anywhere)
  </read_first>
  <action>
    Modify src/routes/app/help/HelpPage.tsx — replace entire body with the composer per PATTERNS.md:
    - Imports: `useState, useMemo` from 'react'; `Stack, Title` from '@mantine/core'; `useDebouncedValue` from '@mantine/hooks'; `listHelpArticles` from '../../../help/helpArticles' (or from '../../../help' barrel); `HelpSearchInput, HelpList, HelpNoResultsState` from '../../../help/components/{...}'.
    - Inside component:
      - `const [query, setQuery] = useState('')`.
      - `const [debouncedQuery] = useDebouncedValue(query, 150)` — 150ms debounce per D-08; the second element (cancel function) is intentionally unused.
      - `const allArticles = useMemo(() => listHelpArticles(), [])` — read once at mount (the static module is reload-stable).
      - `const filtered = useMemo(() => { if (debouncedQuery.trim() === '') return allArticles; const needle = debouncedQuery.toLowerCase().trim(); return allArticles.filter(a => { const haystack = \`${a.title} ${a.keywords.join(' ')} ${a.summary}\`.toLowerCase(); return haystack.includes(needle) }) }, [allArticles, debouncedQuery])` — substring match over title + keywords joined + summary; body is NOT in the haystack per D-08.
    - JSX (UI-SPEC §"Help page (/app/help)"):
      - Outer `<Stack gap="lg">`.
      - `<Title order={3}>Help</Title>`.
      - `<HelpSearchInput value={query} onChange={setQuery} />`.
      - Conditional body: if `filtered.length === 0 && debouncedQuery.trim() !== ''` (i.e., there is a query but no matches) render `<HelpNoResultsState query={debouncedQuery} onClear={() => setQuery('')} />`. Otherwise render `<HelpList articles={filtered} />`. Note: the spec phrases this as `filtered.length === 0` triggering no-results — but the all-empty-query case returns allArticles (which has ≥8 per Plan 02), so the simpler check `filtered.length === 0` works only when there's a query. Use the explicit `&& debouncedQuery.trim() !== ''` guard for clarity. (When allArticles is empty — impossible per D-09 static module guarantee but worth defending — render HelpList with the empty array; the no-data fallback is the "rendered HelpList with no rows" case, which UI-SPEC line 555 marks as "no empty state required".)

    Create src/routes/app/help/HelpArticlePage.tsx (NEW file — per PATTERNS.md §"src/routes/app/help/HelpArticlePage.tsx"):
    - Imports: `Stack, Title, Text, Center, Anchor as MantineAnchor` from '@mantine/core'; `Link, useParams` from 'react-router'; `PENDO_IDS` from '../../../pendo/PENDO_IDS'; `getHelpArticleBySlug` from '../../../help/helpArticles' (or '../../../help').
    - Inside component:
      - `const { slug } = useParams<{ slug: string }>()`.
      - `const article = slug ? getHelpArticleBySlug(slug) : undefined`.
    - Article-not-found branch (when `article` is undefined): render `<Center mih={400}>` (hero idiom) with `<Stack align="center" gap="md">` containing `<Title order={3}>Article not found</Title>`, `<Text c="dimmed" ta="center" maw={420}>We couldn't find a help article at this URL. It may have been moved or removed.</Text>`, and `<MantineAnchor component={Link} to="/app/help" data-pendo-id={PENDO_IDS.help.article.detailBackLink}>← Back to Help</MantineAnchor>` — copy verbatim per UI-SPEC §"Empty-state copy" + §"CTA labels"; back-link uses the same PENDO_IDS leaf so Phase 6 guides can target one ID for "return to help list" intent regardless of slug-found/not-found state.
    - Article-found branch: render `<Stack gap="xl" maw={680}>` (maw={680} keeps body line length readable per UI-SPEC line 501) containing:
      - `<Stack gap={4}>` with `<Text size="sm" c="dimmed">{article.topic}</Text>` (topic line) and `<Title order={3}>{article.title}</Title>` (Sub-heading).
      - `<Stack gap="md">` containing `article.body.split('\n\n').map((paragraph, i) => <Text key={i} size="md">{paragraph}</Text>)` — one paragraph per Text, plain-text rendering (D-09 + UI-SPEC §"Help detail page").
      - `<MantineAnchor component={Link} to="/app/help" data-pendo-id={PENDO_IDS.help.article.detailBackLink}>← Back to Help</MantineAnchor>` — Unicode left-arrow U+2190 + space + label per UI-SPEC line 503; same PENDO_IDS leaf as not-found branch.
    - Include a brief comment above the MantineAnchor in each branch noting the polymorphic-Anchor S3 exception.

    Modify src/router.tsx — minimal additive edit per D-07:
    - Add an import near the top alongside the existing HelpPage import: `import { HelpArticlePage } from './routes/app/help/HelpArticlePage'`.
    - Locate the existing flat entry `{ path: 'help', Component: HelpPage }` (around line 120 inside the /app children array). Replace it with the nested-children form:
      ```
      {
        path: 'help',
        Component: HelpPage,
        children: [
          { path: ':slug', Component: HelpArticlePage },
        ],
      },
      ```
    - Per UI-SPEC line 840, a flat (sibling) shape is also valid: `{ path: 'help', Component: HelpPage }, { path: 'help/:slug', Component: HelpArticlePage }`. Planner picks the nested form (preferred per UI-SPEC for semantic guide-targeting consistency). IMPORTANT EXECUTOR DECISION: React Router v7 nested children render the child INSIDE the parent via `<Outlet />`. HelpPage does NOT include an `<Outlet />` — when /app/help/{slug} is matched, the routing should NOT render the list above the detail. If react-router v7 behavior under nested children with no Outlet causes the list to render with the detail invisible, FLATTEN the routes to the sibling shape (UI-SPEC §"Router Edit (D-07)" line 840 authorizes either). Validate at execution time: navigate /app/help/{slug} and confirm only the detail renders. If both render, switch to the flat shape (one-line change).
    - Do NOT touch any other route entry. RequireAuth is inherited from the `/app` parent — the child `:slug` route is automatically guarded.
    - Do NOT add help-anchor markup anywhere (D-10).
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && npm run typecheck 2>&1 | tail -20 && npm run lint 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/app/help/HelpPage.tsx` no longer renders ComingSoonCard (grep: `grep -E "ComingSoonCard" src/routes/app/help/HelpPage.tsx | wc -l` is 0)
    - HelpPage uses useDebouncedValue with 150ms wait (grep: `grep -E "useDebouncedValue\\(query, 150\\)" src/routes/app/help/HelpPage.tsx` returns 1 match)
    - HelpPage match logic operates on title + keywords + summary lower-cased — body is NOT in the haystack (grep: `grep -E "a\\.title.*a\\.keywords.*a\\.summary" src/routes/app/help/HelpPage.tsx` returns at least 1 match; `grep -E "a\\.body\\." src/routes/app/help/HelpPage.tsx | grep -v 'split' | wc -l` is 0 — body is not used in search; only the detail page splits body for rendering)
    - HelpPage renders HelpNoResultsState only when a query is present (grep: `grep -E "debouncedQuery.*!== ''" src/routes/app/help/HelpPage.tsx` returns at least 1 match)
    - HelpPage title is `<Title order={3}>Help</Title>` (grep: `grep -E "Title order=\\{3\\}>Help" src/routes/app/help/HelpPage.tsx` returns 1 match)
    - File `src/routes/app/help/HelpArticlePage.tsx` exists; reads slug from useParams (grep: `grep -E "useParams<\\{ slug: string \\}>" src/routes/app/help/HelpArticlePage.tsx` returns at least 1 match)
    - HelpArticlePage looks up via getHelpArticleBySlug (grep: `grep -E "getHelpArticleBySlug" src/routes/app/help/HelpArticlePage.tsx` returns at least 1 match)
    - HelpArticlePage has both branches — article-found and article-not-found — with 'Article not found' Title literal (grep: `grep -E "Article not found" src/routes/app/help/HelpArticlePage.tsx` returns 1 match)
    - HelpArticlePage renders body paragraphs via `body.split('\\n\\n')` (grep: `grep -E "article\\.body\\.split\\('\\\\n\\\\n'\\)" src/routes/app/help/HelpArticlePage.tsx` returns 1 match)
    - Both branches use the same PENDO_IDS.help.article.detailBackLink for the back link (grep: `grep -cE "PENDO_IDS\\.help\\.article\\.detailBackLink" src/routes/app/help/HelpArticlePage.tsx` is >= 2 — both branches reference it)
    - 'Back to Help' anchor uses the Unicode left-arrow character (grep: `grep -E "← Back to Help" src/routes/app/help/HelpArticlePage.tsx` returns at least 2 matches — one per branch)
    - Body container has maw={680} for readable line length (grep: `grep -E "maw=\\{680\\}" src/routes/app/help/HelpArticlePage.tsx` returns at least 1 match)
    - `src/router.tsx` imports HelpArticlePage (grep: `grep -E "import \\{ HelpArticlePage \\} from './routes/app/help/HelpArticlePage'" src/router.tsx` returns 1 match)
    - `src/router.tsx` adds a route entry for HelpArticlePage — either nested under help OR flat sibling 'help/:slug' (grep: `grep -E "Component: HelpArticlePage" src/router.tsx` returns at least 1 match)
    - If nested form chosen: `src/router.tsx` contains a `children:` entry with `path: ':slug'` (grep: `grep -E "path: ':slug'" src/router.tsx` returns at least 1 match) — alternative flat-form acceptance: `grep -E "path: 'help/:slug'" src/router.tsx` returns 1 match
    - No new help-anchor markup anywhere — grep across src/ for top-bar-help / floating-fab patterns: `grep -rE "(IconHelp\\b|Resource Center|floatingFab|helpFab)" src/ | grep -v IconHelpCircle | wc -l` is 0 (IconHelpCircle is the existing side-nav icon and is allowed; no other help-anchor symbols introduced)
    - PENDO_IDS.nav.help still references /app/help in AppLayout (grep: `grep -E "PENDO_IDS\\.nav\\.help" src/routes/app/AppLayout.tsx` returns at least 1 match — D-10 anchor stays intact)
    - `npm run typecheck`, `npm run lint`, `npm run build` all exit 0
  </acceptance_criteria>
  <done>Visiting /app/help shows the seeded ≥8 articles grouped by topic; typing in the search input filters live (150ms debounce); navigating to /app/help/{valid-slug} shows the article detail; navigating to /app/help/garbage shows 'Article not found' with a working back link; refreshing on any /app/help/* URL deep-links correctly (SHELL-04 inherited).</done>
</task>

</tasks>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run build` all pass
- `grep -rE "data-pendo-id=\"" src/help/ src/routes/app/help/` returns ZERO matches outside PENDO_IDS-driven values (no hand-typed strings; the dynamic data-pendo-article-slug attribute reads from article.slug, not a hand-typed string)
- `grep -rE "(IconHelp\\b|floatingFab|helpFab|Resource Center)" src/ | grep -v IconHelpCircle | wc -l` is 0 (D-10 no-op — no new help-anchor markup)
- `grep -E "PENDO_IDS\\.nav\\.help" src/routes/app/AppLayout.tsx | wc -l` is at least 1 (existing nav anchor satisfies HELP-04 per D-10)
- Manual smoke (developer can verify after execution): /app/help loads ≥8 articles grouped by topic; type to filter; navigate to a slug detail; navigate to a garbage slug → not-found state; back link returns to list.
</verification>

<success_criteria>
- HELP-01: ≥8 seeded articles grouped by topic on /app/help
- HELP-02: live debounced search filters title + keywords + summary
- HELP-03: clicking an article navigates to /app/help/{slug}; detail view renders topic + title + paragraph body
- HELP-04: NO new anchor markup added; existing PENDO_IDS.nav.help is the stable anchor (D-10)
- UI-01: Help no-search-results compact empty state shipped
- PEN-07: every interactive element reads from PENDO_IDS.help.*; no hand-typed strings
- D-07: First router.tsx edit since Phase 3 — nested :slug child under /app/help (or flat sibling shape; either acceptable per UI-SPEC line 840)
</success_criteria>

<output>
After completion, create `.planning/phases/05-team-help-polish/05-05-SUMMARY.md` documenting:
- Final router shape chosen (nested vs flat) and rationale
- Article-row treatment chosen (A flat anchor list, per UI-SPEC default)
- Whether HelpPage's no-results condition uses the explicit `debouncedQuery !== ''` guard or relies on the all-articles-empty case
- Any deviations from PATTERNS.md
</output>
