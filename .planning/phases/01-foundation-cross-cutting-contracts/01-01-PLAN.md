---
phase: 01-foundation-cross-cutting-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.app.json
  - .gitignore
  - README.md
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
autonomous: false
requirements_addressed:
  - FND-01
must_haves:
  truths:
    - "`npm install` completes with zero errors and zero peer-dep warnings"
    - "`npm run dev` starts a Vite dev server on http://localhost:5173 with exit code 0"
    - "Opening http://localhost:5173 in a browser shows a minimal Halo page with no console errors"
    - "`npm run build` produces a production bundle in `dist/` with exit code 0"
    - "`npm run typecheck` (or `tsc --noEmit`) passes with zero errors"
    - "`.env` is gitignored; `.env.example` does NOT exist (Phase 6 will create it)"
  artifacts:
    - path: package.json
      provides: "Vite + React 19 + TypeScript scaffold metadata"
      contains: '"name": "halo-app"'
    - path: vite.config.ts
      provides: "Vite config with @vitejs/plugin-react"
      contains: "@vitejs/plugin-react"
    - path: tsconfig.json
      provides: "TypeScript strict-mode root config"
      contains: '"strict": true'
    - path: index.html
      provides: "HTML entry — title 'Halo', root div, module script src=/src/main.tsx"
      contains: "<title>Halo</title>"
    - path: src/main.tsx
      provides: "React entry — createRoot + StrictMode + <App />"
      contains: "createRoot"
    - path: src/App.tsx
      provides: "Minimal app component (placeholder for Wave 2+)"
    - path: src/vite-env.d.ts
      provides: "Vite client types reference (no ImportMetaEnv augmentation yet; Phase 6 owns env vars)"
      contains: "vite/client"
    - path: .gitignore
      provides: "node_modules, dist, .env, .env.*, *.local; with !.env.example exception"
      contains: ".env"
  key_links:
    - from: index.html
      to: src/main.tsx
      via: '<script type="module" src="/src/main.tsx">'
    - from: src/main.tsx
      to: src/App.tsx
      via: "import App from './App'"
---

<objective>
Scaffold a fresh Vite + React 19 + TypeScript project at the repo root. This is the foundation every other plan builds on. The scaffold must boot cleanly with `npm run dev`, typecheck cleanly with `tsc --noEmit`, and build cleanly with `npm run build` — all before any other framework code is added.

Purpose: Phase 1's first deliverable is a runnable repo. Subsequent plans (Mantine, storage, router, providers, primitives) all depend on the scaffold existing first. This plan does NOT install Mantine, the router, Zod, Zustand, or nanoid — those come in their respective plans. This plan also does NOT install Pendo runtime, env files for `VITE_PENDO_API_KEY`, or `ImportMetaEnv` augmentation for `VITE_PENDO_API_KEY` — those are Phase 6.

Output:
- Vite-scaffolded React 19 + TypeScript project at the working directory
- `package.json` with verified-current versions (React 19.2.x, Vite 8.x, TypeScript 5.x or 6.x, @vitejs/plugin-react 6.x)
- Minimal `src/main.tsx` + `src/App.tsx` (placeholder content, no Mantine yet)
- `.gitignore` covering node_modules, dist, .env, .env.* (with `!.env.example` exception) and *.local
- Clean `npm run dev` boot at http://localhost:5173 with no console errors
- Clean `tsc --noEmit` and `npm run build`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/colin.maxfield/testspace/test-sites/halo-app/CLAUDE.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/PROJECT.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/ROADMAP.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/STATE.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-RESEARCH.md

<interfaces>
<!-- This is a greenfield bootstrap plan. No prior interfaces exist. -->
<!-- After this plan completes, the following modules/exports exist for downstream plans: -->

From src/main.tsx:
- Default export: none (entry file; calls createRoot(...).render(<App />))
- Imports: `import { StrictMode } from 'react'`, `import { createRoot } from 'react-dom/client'`, `import App from './App'`

From src/App.tsx:
- `export default function App(): JSX.Element` — placeholder returning a minimal landing element (will be replaced by Plan 02's MantineProvider wrapper, Plan 04's provider stack, and Plan 05's RouterProvider)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 19 + TypeScript at the repo root</name>
  <files>
    package.json,
    package-lock.json,
    index.html,
    vite.config.ts,
    tsconfig.json,
    tsconfig.node.json,
    tsconfig.app.json,
    .gitignore,
    src/main.tsx,
    src/App.tsx,
    src/vite-env.d.ts,
    src/assets/react.svg,
    public/vite.svg
  </files>
  <read_first>
    /Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-RESEARCH.md (sections "Standard Stack > Core", "Standard Stack > Phase 1 install set", "Pattern 9: index.html shape", "Pitfall 6: Mantine 9 + React 19 peer-dep mismatch", "Pitfall 9: .env accidentally committed", "Environment Availability"),
    /Users/colin.maxfield/testspace/test-sites/halo-app/CLAUDE.md (sections "Recommended Stack > Core Technologies", "Installation", "What NOT to Use" — note: do not use CRA, do not use Next.js)
  </read_first>
  <action>
    Working directory is the project root `/Users/colin.maxfield/testspace/test-sites/halo-app`. The directory already contains `.planning/`, `.git/` (implied), and `CLAUDE.md`. Scaffold a Vite React-TS project IN PLACE (do not create a sub-directory).

    Approach: run `npm create vite@latest . -- --template react-ts` with the `--overwrite` semantics handled appropriately (the CLI will complain about non-empty directory — use the prompt's "Ignore files and continue" option, or pass `--force` if supported by the installed Vite create version, or manually create the template files). The resulting scaffold MUST NOT delete `.planning/`, `.git/`, or `CLAUDE.md` — verify they remain after scaffold.

    Then run `npm install`. Confirm the installed versions in `package.json` are:
    - `react`: ^19.2.0 or newer (resolved version per `npm view react version` at install time)
    - `react-dom`: matching React major
    - `typescript`: ^5.4.0 minimum (Vite-template default is fine; do not pin down)
    - `vite`: ^7.0.0 or ^8.0.0 (whichever the current Vite template ships)
    - `@vitejs/plugin-react`: version matching the Vite major

    Do NOT install Mantine, react-router, zod, zustand, nanoid, or any other production runtime dependency in this plan — those are owned by Plans 02, 03, 05, 06.

    Edit `index.html` to set `<title>Halo</title>` (Vite-template default is "Vite + React + TS"). Do NOT add the Pendo snippet — that is Phase 6 work. Leave the body content as the default (root div + module script) without any Pendo references.

    Replace the Vite-template default content in `src/App.tsx` with a minimal placeholder body: a single `<main>` containing an `<h1>Halo</h1>` and a `<p>Phase 1 scaffold — Mantine, providers, router, and primitives arrive in later plans.</p>`. Strip the Vite-template demo state (counter, Vite/React logos) — they exist solely to demo HMR and add noise. Keep the import surface minimal: `import './App.css'` is allowed but the file should be empty or just contain a single reset rule.

    `src/main.tsx` MUST wrap `<App />` in `<StrictMode>` from React. Per research Pitfall 2 (React 19 StrictMode double-mount), the StrictMode wrapper is intentional — downstream plans (Plan 04's PendoBridge stub) will need to be StrictMode-safe.

    `src/vite-env.d.ts` MUST contain ONLY `/// <reference types="vite/client" />`. Do NOT add `ImportMetaEnv` augmentation for `VITE_PENDO_API_KEY` or any other env var — Phase 6 owns env var typing for Pendo. Per research Pattern 10, this file must remain non-module (no `import` statements) so future global augmentation works.

    Edit `.gitignore` to match the Vite-template default PLUS the following explicit additions, in this order:
    - Vite-template defaults (node_modules, dist, dist-ssr, *.local, .vscode/*, .idea, .DS_Store, *.log, etc.) — keep what the template provides
    - An explicit `.env` line (bare, no wildcard) — per research Pitfall 9, the template may only cover `*.local`, NOT `.env` itself
    - An explicit `.env.*` line
    - An explicit `!.env.example` exception line (so when Phase 6 creates `.env.example`, it can be committed)

    Create a minimal `README.md` at the repo root with three short sections: a one-sentence "Halo — fake multi-tenant SaaS for Pendo experimentation", a "Run" section with `npm install` + `npm run dev` commands, and a "Phase" section noting this is the Phase 1 scaffold and pointing at `.planning/ROADMAP.md`. Keep total length under 30 lines.

    Verify `package.json` has these npm scripts (the Vite-template provides all of them; confirm and do not modify): `dev`, `build`, `lint` (if template adds it; not required to add manually), and `preview`. Add a `typecheck` script with value `tsc --noEmit -p tsconfig.app.json` (or `tsc --noEmit` if the template uses a single tsconfig.json with includes) so future plans have a stable typecheck command.

    Confirm `tsconfig.app.json` (Vite-template default) has `"strict": true` in compilerOptions. If the template's strict flag is in `tsconfig.json` instead, that's fine. Do NOT loosen strict mode.
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && test -f package.json &amp;&amp; test -f vite.config.ts &amp;&amp; test -f src/main.tsx &amp;&amp; test -f src/App.tsx &amp;&amp; test -f .gitignore &amp;&amp; grep -q '^\.env$' .gitignore &amp;&amp; grep -q '^!\.env\.example$' .gitignore &amp;&amp; grep -q '"name": "halo' package.json &amp;&amp; grep -q '"react":' package.json &amp;&amp; grep -q '"vite":' package.json &amp;&amp; ! grep -q 'VITE_PENDO_API_KEY' index.html &amp;&amp; ! grep -q 'cdn\.pendo\.io' index.html &amp;&amp; ! test -f .env.example &amp;&amp; npm install --no-audit --no-fund &amp;&amp; npx tsc --noEmit -p tsconfig.app.json 2>/dev/null || npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root with `"name"` field starting with `"halo"` and the React major version in `dependencies.react` matches `19.*` (verify: `grep '"react":' package.json` shows `"^19` prefix)
    - `package.json` has `"type": "module"` (Vite-template default) — verify with `grep '"type": "module"' package.json`
    - `vite.config.ts` exists and imports `@vitejs/plugin-react` (verify: `grep "@vitejs/plugin-react" vite.config.ts`)
    - `tsconfig.app.json` (or `tsconfig.json` if single-file) contains `"strict": true` (verify: `grep '"strict": true' tsconfig.app.json tsconfig.json 2>/dev/null`)
    - `index.html` contains `<title>Halo</title>` exactly (verify: `grep '<title>Halo</title>' index.html`)
    - `index.html` does NOT contain any of: `cdn.pendo.io`, `VITE_PENDO_API_KEY`, `pendo.initialize`, `pendo` substring in any `<script>` tag (verify: `grep -i 'pendo' index.html | grep -v '^#'` returns no matches)
    - `src/vite-env.d.ts` contains the line `/// <reference types="vite/client" />` and does NOT contain any `import` statement and does NOT contain `VITE_PENDO_API_KEY` (verify: `grep 'vite/client' src/vite-env.d.ts && ! grep -E '^import' src/vite-env.d.ts && ! grep VITE_PENDO src/vite-env.d.ts`)
    - `src/main.tsx` imports `StrictMode` and `createRoot` and renders `<StrictMode><App /></StrictMode>` (verify: `grep -E 'StrictMode|createRoot' src/main.tsx` shows both)
    - `src/App.tsx` exports a default function component containing the literal text `Halo` (verify: `grep -E 'export default' src/App.tsx && grep -E 'Halo' src/App.tsx`)
    - `.gitignore` contains literal lines `.env`, `.env.*`, and `!.env.example` (verify: `grep -E '^\.env$' .gitignore && grep -E '^\.env\.\*$' .gitignore && grep -E '^!\.env\.example$' .gitignore`)
    - `.env.example` does NOT exist (Phase 6 owns it) — verify: `test ! -f .env.example`
    - `node_modules/` exists after `npm install` and the install exits with code 0 with no peer-dep warning lines (verify: `npm install --no-audit --no-fund 2>&1 | grep -i 'peer dep' | grep -i 'warn' | wc -l` returns 0)
    - `npx tsc --noEmit` (or `npx tsc --noEmit -p tsconfig.app.json`) exits with code 0
    - `npm run build` exits with code 0 and produces `dist/` (verify: `test -d dist`)
    - `.planning/` directory is still present and intact at repo root (verify: `test -d .planning && test -f .planning/ROADMAP.md`)
    - `CLAUDE.md` is still present at repo root (verify: `test -f CLAUDE.md`)
  </acceptance_criteria>
  <done>
    Scaffold is in place. `npm install`, `npm run build`, and `tsc --noEmit` all exit 0. The repo can be opened, edited, and run by downstream plans without further scaffold work.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — dev server boots cleanly</name>
  <files>(human verification — no files modified by this task)</files>
  <what-built>
    A fresh Vite + React 19 + TypeScript scaffold at the repo root. `npm install` completed, `npm run build` produced `dist/`, and `tsc --noEmit` passed. The dev server has not yet been started against a live browser.
  </what-built>
  <how-to-verify>
    1. From `/Users/colin.maxfield/testspace/test-sites/halo-app`, run `npm run dev`.
    2. Observe terminal output: it MUST show a `Local:` line with a URL (typically `http://localhost:5173/`) and MUST NOT show any error or warning lines (a Vite "ready in Nms" line is expected).
    3. Open the printed URL in a browser.
    4. Confirm the page shows a heading reading `Halo` and the placeholder paragraph mentioning later plans.
    5. Open browser DevTools → Console tab. Confirm there are zero errors (red entries). One or two Vite HMR info lines (gray, like `[vite] connected.`) are acceptable.
    6. Stop the dev server with Ctrl+C.

    If any of the above fail, describe the failure (terminal output, console errors, screenshot if visual) and resume-signal "issues: ...".
  </how-to-verify>
  <action>
    PAUSE for the developer to run the verification steps listed in &lt;how-to-verify&gt; above. The executor MUST NOT proceed to subsequent tasks (or subsequent plans) until the developer types the resume signal described in &lt;resume-signal&gt;. This task modifies no files — it is a manual verification gate.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual verification gate; resume signal: see &lt;resume-signal&gt; in plan source"</automated>
  </verify>
  <done>
    Developer typed the resume signal (or an equivalent approval) after completing the verification steps in &lt;how-to-verify&gt;. If issues were reported, they have been resolved and re-verified before resuming.
  </done>
  <resume-signal>Type "approved" if dev server boots cleanly with no console errors, or describe the failure (e.g., "issues: console shows 'X is not defined'").</resume-signal>
</task>

</tasks>

<verification>
- `npm install` exits with code 0 and produces zero peer-dep warnings
- `npm run dev` boots a Vite dev server on http://localhost:5173 with no terminal errors or warnings
- Browser at http://localhost:5173 shows a minimal Halo placeholder page with no console errors
- `npm run build` exits with code 0 and produces a `dist/` folder
- `npx tsc --noEmit` exits with code 0
- `.gitignore` covers `.env` and `.env.*` while preserving `!.env.example`
- `.planning/` and `CLAUDE.md` are still present at repo root after scaffolding
- No Pendo runtime artifacts exist (`.env.example` absent, no `cdn.pendo.io` in `index.html`, no `VITE_PENDO_API_KEY` typing in `vite-env.d.ts`)
</verification>

<success_criteria>
- FND-01 is satisfied: `npm run dev` boots a Halo-titled (placeholder) app locally with no console errors
- Repo is ready for Plan 02 (Mantine theme + ColorSchemeScript) and Plan 03 (storage envelope) to run in parallel in Wave 2
- No Phase 6 (Pendo runtime) artifacts have leaked into Phase 1's scaffold
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cross-cutting-contracts/01-01-SUMMARY.md` capturing the exact versions installed (from `package.json`), the scaffold options chosen, and any deviations from the Vite-template defaults so Plan 02 / Plan 03 executors know what they're starting from.
</output>
