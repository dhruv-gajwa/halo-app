---
phase: 01-foundation-cross-cutting-contracts
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-04
files_modified:
  - package.json
  - package-lock.json
  - src/App.tsx
  - src/router.tsx
  - src/routes/public/PublicLayout.tsx
  - src/routes/public/Landing.tsx
  - src/routes/app/AppLayout.tsx
  - src/routes/app/AppPlaceholder.tsx
  - src/ui/DemoBanner.tsx
autonomous: false
requirements_addressed:
  - FND-03
  - FND-06
must_haves:
  truths:
    - "React Router 7 is installed at the current major (`react-router@^7.x`)"
    - "`createBrowserRouter` is used (History API routing); the URL bar shows real paths, never `#/...`"
    - "Routing is split into two top-level branches: `/` (public, rooted at `PublicLayout`) and `/app` (authenticated shell, rooted at `AppLayout`); neither layout imports from the other"
    - "`PublicLayout` renders a persistent `<DemoBanner>` above the `<Outlet />` showing the literal text 'Demo data only — never enter real credentials'"
    - "Deep-linking works: navigating to http://localhost:5173/app directly (and refreshing while on /app) lands on the AppLayout placeholder, NOT a 404 or a redirect to /"
    - "`src/App.tsx`'s child of `<PendoBridge>` is now `<RouterProvider router={router} />` — the placeholder body from Plan 04 is gone"
    - "`PendoRouteBridge` does NOT exist in this phase — Phase 6 owns route-change reporting to Pendo"
  artifacts:
    - path: src/router.tsx
      provides: "`router` — `createBrowserRouter` config with `/` and `/app` branches"
      exports: ["router"]
    - path: src/routes/public/PublicLayout.tsx
      provides: "Public chrome with `<DemoBanner />` + `<Outlet />`"
      exports: ["PublicLayout"]
    - path: src/routes/public/Landing.tsx
      provides: "Phase 1 landing placeholder — `<Title>Halo</Title>` + a sign-in/sign-up CTA stub"
      exports: ["Landing"]
    - path: src/routes/app/AppLayout.tsx
      provides: "Authenticated shell placeholder for Phase 1 (Phase 3 expands to AppShell + side nav)"
      exports: ["AppLayout"]
    - path: src/routes/app/AppPlaceholder.tsx
      provides: "Inert placeholder content for /app in Phase 1"
      exports: ["AppPlaceholder"]
    - path: src/ui/DemoBanner.tsx
      provides: "Persistent demo banner — 'Demo data only — never enter real credentials'"
      exports: ["DemoBanner"]
    - path: src/App.tsx
      provides: "Final provider stack with <RouterProvider> as the child of <PendoBridge>"
  key_links:
    - from: src/App.tsx
      to: src/router.tsx
      via: "import { router } from './router'; <RouterProvider router={router} />"
    - from: src/router.tsx
      to: src/routes/public/PublicLayout.tsx
      via: "createBrowserRouter([{ path: '/', Component: PublicLayout, children: [...] }])"
    - from: src/router.tsx
      to: src/routes/app/AppLayout.tsx
      via: "createBrowserRouter([..., { path: '/app', Component: AppLayout, children: [...] }])"
    - from: src/routes/public/PublicLayout.tsx
      to: src/ui/DemoBanner.tsx
      via: "import { DemoBanner } from '../../ui/DemoBanner'; renders <DemoBanner /> above <Outlet />"
---

<objective>
Install React Router 7, build the public/`/app` route split per FND-03, mount the demo-data banner on the public layout per FND-06, and replace the Plan 04 placeholder body in `src/App.tsx` with `<RouterProvider router={router} />`.

The router config has exactly two top-level routes in Phase 1:
- `/` -> `PublicLayout` (DemoBanner + Outlet) with child `index` -> `Landing` (Phase 1 placeholder; Phase 2 adds `/signup`, `/signin`)
- `/app` -> `AppLayout` (Phase 1 placeholder shell — NOT the full AppShell; Phase 3 expands this) with child `index` -> `AppPlaceholder`

In Phase 1, there is no `RequireAuth` guard yet — Phase 2 adds it. Visiting `/app` directly in Phase 1 lands on the placeholder without redirect (because there is no auth state to check). This is intentional: Phase 1 verifies the route plumbing, Phase 2 adds the gate.

Purpose: FND-03 (router + public/`/app` split) and FND-06 (demo banner). Plan 06 will register a sandbox route (as a child of the public layout) to smoke-render the UI primitives — this plan exposes the router so Plan 06 can extend it.

Phase 6 explicitly does NOT belong here:
- No `PendoRouteBridge` component is created (Phase 6 owns it)
- No `pendo.location.setUrl(...)` call (Phase 6 owns it)
- No `useLocation`-driven Pendo effect (Phase 6 owns it)

Output:
- `react-router@^7.x` installed
- `src/router.tsx` with `createBrowserRouter` config
- `src/routes/public/{PublicLayout, Landing}.tsx`
- `src/routes/app/{AppLayout, AppPlaceholder}.tsx`
- `src/ui/DemoBanner.tsx` showing the literal demo-banner text
- `src/App.tsx` final form: `<MantineProvider>` -> providers stack -> `<PendoBridge>` -> `<RouterProvider router={router} />`
- Working deep-link: refresh on `/app` lands on `/app`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-RESEARCH.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-02-SUMMARY.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-04-SUMMARY.md
@/Users/colin.maxfield/testspace/test-sites/halo-app/src/App.tsx
@/Users/colin.maxfield/testspace/test-sites/halo-app/src/theme.ts

<interfaces>
<!-- After this plan completes, downstream plans can import: -->

From src/router.tsx:
- `export const router: ReturnType<typeof createBrowserRouter>` — the application's router instance
- Plan 06 will extend the route config to add a sandbox route. Whether by mutating the array literal in `src/router.tsx` directly (preferred) or by re-creating the router after Plan 05 ships, Plan 06's executor decides.

From src/routes/public/PublicLayout.tsx:
- `export function PublicLayout(): JSX.Element` — renders `<DemoBanner />` + `<Outlet />`. Phase 2 will wrap the Outlet in a Mantine `<Container>` for the signup card; Phase 1 just emits the Outlet directly.

From src/routes/public/Landing.tsx:
- `export function Landing(): JSX.Element` — Phase 1 landing content. Phase 2 will add real "Sign In" / "Sign Up" routes to /signin and /signup.

From src/routes/app/AppLayout.tsx:
- `export function AppLayout(): JSX.Element` — Phase 1 placeholder shell (NO Mantine AppShell yet — Phase 3 owns the real side-nav+top-bar shell). Just renders `<Outlet />` with a brief heading so the route is visibly distinct from `/`.

From src/ui/DemoBanner.tsx:
- `export function DemoBanner(): JSX.Element` — Mantine `<Alert>`-based styled banner; renders the literal text "Demo data only — never enter real credentials"

Consumed by Plan 06 (UI primitives): Plan 06 builds primitives that wrap Mantine components and adds a `/sandbox` child route under PublicLayout to smoke-render them. The route config in `src/router.tsx` is the extension point.

Consumed by Phase 2 (Auth):
- Phase 2 adds child routes `/signup`, `/signup/details`, `/signup/company`, `/signup/preferences`, `/signin` under `PublicLayout`'s `children`
- Phase 2 wraps `/app/*` in a `<RequireAuth>` route element (NOT in Phase 1)
- Phase 2 wraps `/signup*` and `/signin` in `<RequireAnon>` (NOT in Phase 1)

Consumed by Phase 6 (Pendo Wiring):
- Phase 6 creates `src/pendo/PendoRouteBridge.tsx` and mounts it inside BOTH `PublicLayout` and `AppLayout` (per RESEARCH.md Pattern 5 + Open Question 1's recommendation). Phase 6 — NOT Phase 1 — owns this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router 7 and build the route components + layouts + DemoBanner</name>
  <files>
    package.json,
    package-lock.json,
    src/router.tsx,
    src/routes/public/PublicLayout.tsx,
    src/routes/public/Landing.tsx,
    src/routes/app/AppLayout.tsx,
    src/routes/app/AppPlaceholder.tsx,
    src/ui/DemoBanner.tsx
  </files>
  <read_first>
    /Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-RESEARCH.md (sections "Standard Stack > Core" — react-router@7.x, "Pattern 5: Two-bridge split" — Phase 1 implements ONLY the public/app split and creates the slot for Phase 6's PendoRouteBridge but does NOT create PendoRouteBridge itself, "Recommended file layout"),
    /Users/colin.maxfield/testspace/test-sites/halo-app/.planning/research/PITFALLS.md (Pitfall 3 "Pendo not seeing SPA route changes" — relevant for Phase 6, not Phase 1; ensure routes use real paths not hash routes),
    /Users/colin.maxfield/testspace/test-sites/halo-app/src/App.tsx (current state — has provider stack with placeholder body)
  </read_first>
  <action>
    Install React Router: `npm install react-router`. Per research, current major is `^7.x` and the unified `react-router` package (NOT `react-router-dom`) is the recommended v7 import target. Confirm the installed version is `^7.x` via `npm view react-router version` or by inspecting `package.json`. If npm resolves to anything below v7, force `react-router@latest`.

    `src/router.tsx`:
    - Import `createBrowserRouter` from `'react-router'`
    - Import `PublicLayout`, `Landing` from `'./routes/public/PublicLayout'` and `'./routes/public/Landing'`
    - Import `AppLayout`, `AppPlaceholder` from `'./routes/app/AppLayout'` and `'./routes/app/AppPlaceholder'`
    - Export `export const router = createBrowserRouter([...])` with the two-route config:
      - `{ path: '/', Component: PublicLayout, children: [{ index: true, Component: Landing }] }`
      - `{ path: '/app', Component: AppLayout, children: [{ index: true, Component: AppPlaceholder }] }`
    - Add a top-of-file JSDoc comment noting: "Phase 1 route map. Phase 2 adds /signup, /signup/details, /signup/company, /signup/preferences, /signin under PublicLayout. Phase 2 also wraps /app/* with RequireAuth. Phase 6 — NOT Phase 1 — adds PendoRouteBridge mounts inside both PublicLayout and AppLayout."

    `src/ui/DemoBanner.tsx`:
    - Use Mantine's `<Alert>` component with `color="yellow"` (or `color="orange"`) and an icon from `@tabler/icons-react` (recommend `IconAlertTriangle`) — distinct from page content so it reads as "system notice", not "marketing"
    - The Alert's content MUST contain the literal string `Demo data only — never enter real credentials` exactly (em-dash character `—`, U+2014)
    - The component MUST be a function component named `DemoBanner` exporting as `export function DemoBanner(): JSX.Element`
    - Style notes: place the banner at the top of the page; non-sticky banner above the Outlet is acceptable for Phase 1
    - The component MUST NOT carry a `data-pendo-id` attribute YET — Plan 06's primitives don't wrap layout components like banners. If Phase 6 wants to instrument it, that's a Phase 6 task.

    `src/routes/public/PublicLayout.tsx`:
    - Import `Outlet` from `'react-router'`, `DemoBanner` from `'../../ui/DemoBanner'`, and Mantine `<Container>` (the layout renders `<DemoBanner /><Container size="lg" py="md"><Outlet /></Container>` or similar)
    - Body: render `<><DemoBanner /><Container size="lg" py="md"><Outlet /></Container></>`
    - Add a comment noting: "Phase 6 will mount <PendoRouteBridge /> here (and in AppLayout). Phase 1 deliberately does not."

    `src/routes/public/Landing.tsx`:
    - A simple page: Mantine `<Title order={1}>Halo</Title>`, a `<Text>` describing the demo, and TWO Mantine `<Anchor>` placeholder links — `<Anchor href="/signin">Sign in (Phase 2)</Anchor>` and `<Anchor href="/signup">Sign up (Phase 2)</Anchor>`. The links are intentionally non-functional in Phase 1 (those routes don't exist yet) — they exist as visible placeholders to confirm the Landing is the index of `/`.
    - Phase 1 explicitly does NOT use Plan 06's `<Anchor>` primitive — Plan 06 has not run yet at this point in the wave order. Use Mantine's `<Anchor>` directly.

    `src/routes/app/AppLayout.tsx`:
    - Imports `Outlet` from `'react-router'`
    - Body: a Mantine `<Container size="lg" py="md">` wrapping `<Title order={2}>App (Phase 1 placeholder)</Title>` + `<Outlet />`
    - Add a comment noting: "Phase 3 replaces this with the full Mantine AppShell (side nav + top bar + user menu). Phase 2 wraps the route in RequireAuth so unauthenticated visitors redirect to /signin. Phase 6 mounts <PendoRouteBridge /> here."

    `src/routes/app/AppPlaceholder.tsx`:
    - A simple page: `<Title order={2}>Authenticated area (Phase 3 placeholder)</Title>` + a `<Text>` noting Phase 3 will build out the Dashboard.

    Verify directories are created (`src/routes/public/`, `src/routes/app/`, `src/ui/`). Run `npx tsc --noEmit` to confirm types resolve.
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app &amp;&amp; node -e "const p=require('./package.json'); const v=p.dependencies['react-router']||''; if(!/^\^?7\./.test(v)){console.error('react-router not at v7, got:',v);process.exit(1)}; console.log('OK react-router',v)" &amp;&amp; test -f src/router.tsx &amp;&amp; test -f src/routes/public/PublicLayout.tsx &amp;&amp; test -f src/routes/public/Landing.tsx &amp;&amp; test -f src/routes/app/AppLayout.tsx &amp;&amp; test -f src/routes/app/AppPlaceholder.tsx &amp;&amp; test -f src/ui/DemoBanner.tsx &amp;&amp; grep -q "createBrowserRouter" src/router.tsx &amp;&amp; grep -q "path: '/'" src/router.tsx &amp;&amp; grep -q "path: '/app'" src/router.tsx &amp;&amp; grep -q "Demo data only — never enter real credentials" src/ui/DemoBanner.tsx &amp;&amp; grep -q "DemoBanner" src/routes/public/PublicLayout.tsx &amp;&amp; grep -q "Outlet" src/routes/public/PublicLayout.tsx &amp;&amp; grep -q "Outlet" src/routes/app/AppLayout.tsx &amp;&amp; test ! -f src/pendo/PendoRouteBridge.tsx &amp;&amp; PHASE6_LEAK=$(grep -r "pendo\.location\.setUrl\|window\.pendo" src/ --include='*.tsx' --include='*.ts' | grep -v '^[[:space:]]*//' | grep -v '^[[:space:]]*\*' | wc -l) &amp;&amp; if [ "$PHASE6_LEAK" -gt 0 ]; then echo "FAIL: Phase 6 Pendo runtime references found in Phase 1 code"; exit 1; fi &amp;&amp; (npx tsc --noEmit -p tsconfig.app.json 2>/dev/null || npx tsc --noEmit)</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.react-router` is at `^7.x` (verify: the node one-liner returns OK)
    - All seven files exist (verify: `test -f` on each)
    - `src/router.tsx` calls `createBrowserRouter` (NOT `createHashRouter` — FND-03 mandates History API routing) (verify: `grep "createBrowserRouter" src/router.tsx && ! grep "createHashRouter" src/router.tsx`)
    - `src/router.tsx` exports a `router` constant (verify: `grep -E "^export const router" src/router.tsx`)
    - `src/router.tsx` defines exactly two top-level routes with paths `/` and `/app` (verify: `grep -cE "path: '/'" src/router.tsx` returns 1 AND `grep -cE "path: '/app'" src/router.tsx` returns 1)
    - `src/ui/DemoBanner.tsx` contains the exact string `Demo data only — never enter real credentials` (verify: `grep "Demo data only — never enter real credentials" src/ui/DemoBanner.tsx`)
    - `src/routes/public/PublicLayout.tsx` imports `DemoBanner` and renders both `<DemoBanner />` and `<Outlet />` (verify: `grep "DemoBanner" src/routes/public/PublicLayout.tsx && grep "<DemoBanner" src/routes/public/PublicLayout.tsx && grep "<Outlet" src/routes/public/PublicLayout.tsx`)
    - `src/routes/app/AppLayout.tsx` renders an `<Outlet />` (verify: `grep "<Outlet" src/routes/app/AppLayout.tsx`)
    - Phase 6 Pendo runtime artifacts do NOT exist in Phase 1 code outside comments (verify: the PHASE6_LEAK grep in `<automated>` returns 0)
    - `src/pendo/PendoRouteBridge.tsx` does NOT exist (verify: `test ! -f src/pendo/PendoRouteBridge.tsx`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    All route components, layouts, and the demo banner exist. `src/router.tsx` is wired to the layouts. The Phase 6 PendoRouteBridge is referenced only in TODO comments — no Phase 6 runtime file exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire RouterProvider into src/App.tsx as the child of PendoBridge; verify deep-linking</name>
  <files>
    src/App.tsx
  </files>
  <read_first>
    /Users/colin.maxfield/testspace/test-sites/halo-app/.planning/phases/01-foundation-cross-cutting-contracts/01-RESEARCH.md (section "Pattern 1: Strict Provider Stack" — RouterProvider is the innermost slot inside PendoBridge),
    /Users/colin.maxfield/testspace/test-sites/halo-app/src/App.tsx (current state from Plan 04 — has the provider stack with placeholder JSX child)
  </read_first>
  <action>
    Edit `src/App.tsx`:
    1. Add `import { RouterProvider } from 'react-router'`
    2. Add `import { router } from './router'`
    3. Locate the JSX comment `{/* Plan 05 replaces this with <RouterProvider router={router} /> */}` left by Plan 04
    4. Replace that comment AND the placeholder `<Container><Stack>...</Stack></Container>` body with a single `<RouterProvider router={router} />`
    5. Remove the now-unused imports of `Container`, `Stack`, `Title`, `Text` from `'@mantine/core'` (the only remaining import from `@mantine/core` should be `MantineProvider`)
    6. KEEP the import of `haloTheme` — it is still used by `<MantineProvider theme={haloTheme}>`

    Final `src/App.tsx` body shape (high-level):
    - `<MantineProvider theme={haloTheme} defaultColorScheme="light">`
    -   `<StorageProvider>`
    -     `<AuthProvider>`
    -       `<WorkspaceProvider>`
    -         `<PendoBridge>`
    -           `<RouterProvider router={router} />`
    -         `</PendoBridge>`
    -       `</WorkspaceProvider>`
    -     `</AuthProvider>`
    -   `</StorageProvider>`
    - `</MantineProvider>`

    Run `npx tsc --noEmit` and `npm run build` to confirm the production build succeeds. The dev-server deep-link checks happen in the human verification task below.
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app &amp;&amp; grep -q "import { RouterProvider } from 'react-router'" src/App.tsx &amp;&amp; grep -q "import { router } from './router'" src/App.tsx &amp;&amp; grep -q "<RouterProvider router={router} />" src/App.tsx &amp;&amp; ! grep -q "Plan 05 replaces this" src/App.tsx &amp;&amp; (npx tsc --noEmit -p tsconfig.app.json 2>/dev/null || npx tsc --noEmit) &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` imports `RouterProvider` from `'react-router'` and `router` from `'./router'` (verify: `grep -E "from 'react-router'" src/App.tsx && grep -E "from './router'" src/App.tsx`)
    - `src/App.tsx` renders `<RouterProvider router={router} />` exactly once (verify: `grep -c "<RouterProvider router=\\{router\\} />" src/App.tsx` returns 1)
    - The Plan 04 placeholder comment `Plan 05 replaces this` has been removed (verify: `! grep -q "Plan 05 replaces this" src/App.tsx`)
    - The `<RouterProvider>` is the direct child of `<PendoBridge>` (verify by reading the file: the only thing between `<PendoBridge>` opening and `</PendoBridge>` closing is `<RouterProvider router={router} />`)
    - `src/App.tsx` does NOT import `Container`, `Stack`, `Title`, `Text` from `@mantine/core` (the only Mantine import should be `MantineProvider`) (verify: `grep -E "from '@mantine/core'" src/App.tsx` shows only the `MantineProvider` import)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    `src/App.tsx` is in its final Phase 1 form: MantineProvider wraps the provider stack, PendoBridge wraps RouterProvider. Plan 06 can now extend the route config without touching App.tsx. Phase 2 will add route children for /signup and /signin. Phase 6 will swap the PendoBridge body for the real implementation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — public/app routes work and deep-linking survives refresh</name>
  <files>(human verification — no files modified by this task)</files>
  <what-built>
    React Router 7 is installed and wired. `/` renders Landing inside PublicLayout (with DemoBanner). `/app` renders AppPlaceholder inside AppLayout (no DemoBanner). `src/App.tsx` mounts `<RouterProvider>` as the child of `<PendoBridge>`.
  </what-built>
  <how-to-verify>
    1. From `/Users/colin.maxfield/testspace/test-sites/halo-app`, run `npm run dev`.
    2. Open `http://localhost:5173/` in a browser. Confirm:
       - The DemoBanner is visible at the top with the EXACT text `Demo data only — never enter real credentials`
       - Below the banner, the Landing page shows the `<Title>Halo</Title>` heading and two link placeholders for sign-in and sign-up
       - The URL bar shows `http://localhost:5173/` (NOT `http://localhost:5173/#/`)
       - DevTools Console: zero errors
    3. In the URL bar, change the path to `http://localhost:5173/app` and press Enter (this is a fresh navigation, not an SPA link click — exercises the History API + dev-server fallback). Confirm:
       - The DemoBanner is NOT visible
       - The AppLayout placeholder heading shows ("App (Phase 1 placeholder)") and AppPlaceholder's heading shows ("Authenticated area (Phase 3 placeholder)")
       - DevTools Console: zero errors
    4. With the browser sitting on `/app`, press Refresh (Cmd-R / Ctrl-R). Confirm:
       - The page does NOT 404
       - The page does NOT redirect to `/`
       - The same `/app` content re-renders cleanly (this is the FND-03 "deep-linking works" requirement)
    5. Open DevTools -> Application -> Local Storage. Confirm `halo:v1:meta` still exists with the expected shape (Plan 03's migration runner still runs on every boot).
    6. Stop the dev server with Ctrl+C.

    If `/app` refresh produces a 404 from Vite's dev server, Vite's dev server is configured wrong (Vite-template default should already do SPA fallback — this should "just work"). If it fails, check `vite.config.ts` and/or the Vite version.
  </how-to-verify>
  <action>
    PAUSE for the developer to run the verification steps listed in &lt;how-to-verify&gt; above. The executor MUST NOT proceed to subsequent tasks (or subsequent plans) until the developer types the resume signal described in &lt;resume-signal&gt;. This task modifies no files — it is a manual verification gate.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual verification gate; resume signal: see &lt;resume-signal&gt; in plan source"</automated>
  </verify>
  <done>
    Developer typed the resume signal (or an equivalent approval) after completing the verification steps in &lt;how-to-verify&gt;. If issues were reported, they have been resolved and re-verified before resuming.
  </done>
  <resume-signal>Type "approved" if both `/` and `/app` render correctly, the DemoBanner shows on `/` only, and refreshing `/app` keeps the user on `/app`. Otherwise describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- `react-router@^7.x` in `package.json` dependencies (no `react-router-dom`)
- `src/router.tsx` uses `createBrowserRouter` with exactly two top-level routes (`/` and `/app`)
- `src/ui/DemoBanner.tsx` renders the exact text `Demo data only — never enter real credentials`
- `src/routes/public/PublicLayout.tsx` mounts DemoBanner + Outlet; `src/routes/app/AppLayout.tsx` mounts Outlet
- `src/App.tsx` final form has `<RouterProvider router={router} />` as the only child of `<PendoBridge>` and removes the Plan 04 placeholder body and its Mantine layout imports
- No `PendoRouteBridge.tsx` exists in Phase 1; no `pendo.location.setUrl` / `window.pendo` references in Phase 1 source code
- `npx tsc --noEmit` and `npm run build` exit 0
- Manual: `/` shows DemoBanner + Landing; `/app` shows AppPlaceholder; refreshing `/app` stays on `/app` (no 404)
</verification>

<success_criteria>
- FND-03 is satisfied: React Router 7 + History API routing + hard split between `/` (public, PublicLayout) and `/app/*` (AppLayout)
- FND-06 is satisfied: persistent demo banner visible on the public layout with the literal required text
- Plan 06 can extend the route config to register a sandbox route under PublicLayout
- Phase 2 can add `/signup*` and `/signin` as PublicLayout children and wrap `/app/*` in RequireAuth — no `src/App.tsx` edits needed
- Phase 6 can drop `<PendoRouteBridge />` into PublicLayout and AppLayout in a body-only change
- No Phase 6 Pendo runtime artifacts have leaked into Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cross-cutting-contracts/01-05-SUMMARY.md` capturing: react-router version installed, the route map shape (exactly two top-level routes — `/` and `/app` — with index children), confirmation that `src/App.tsx` is in its final Phase 1 form (RouterProvider as the only child of PendoBridge), and a reminder that Plan 06's sandbox route will register under `PublicLayout`'s children.
</output>
