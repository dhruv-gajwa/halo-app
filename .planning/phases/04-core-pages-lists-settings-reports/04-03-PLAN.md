---
phase: 04-core-pages-lists-settings-reports
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/routes/app/lists/ListsPage.tsx
  - src/tasks/components/TaskTable.tsx
  - src/tasks/components/TaskFiltersBar.tsx
  - src/tasks/components/TaskFormModal.tsx
  - src/tasks/components/DeleteConfirmModal.tsx
  - src/tasks/components/ListsEmptyState.tsx
  - src/tasks/components/FilteredEmptyState.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07
  - LIST-08
  - LIST-09
must_haves:
  truths:
    - "User can navigate to /app/lists (already routed by Phase 3) and see a Tasks page with the page header (Title 'Tasks' + 'New task' primary button), a three-Select filter bar, and either a TanStack Table over filtered tasks OR an empty state — covering LIST-01 + LIST-08."
    - "Clicking 'New task' opens a Mantine Modal in mode='create' with the six form fields from D-08 (title, description, status, priority, assignee, due date). RHF + Zod via zodResolver(TaskFormSchema) validates on submit. Submitting calls tasksRepo.createTask, closes the modal, fires 'Task created' toast, and the new row appears in the table without a page refresh (LIST-02)."
    - "Row kebab → Edit opens the SAME modal in mode='edit' with the task's current values prefilled; Save calls tasksRepo.updateTask and the row reflects new values without refresh (LIST-03)."
    - "Leading Checkbox column toggles status: checked iff status==='done'; onChange calls tasksRepo.updateTask({ status: checked ? 'todo' : 'done' }). The repo (per plan 04-02) stamps/clears completedAt; UI never thinks about it (LIST-04)."
    - "Row kebab → Delete AND edit-modal footer 'Delete task' BOTH open a separate Mantine size='sm' confirm modal whose 'Delete task' button (color='red') calls tasksRepo.deleteTask, fires 'Task deleted' toast, and removes the row. Modal-on-modal stacking is avoided — edit modal closes before delete confirm opens (LIST-05 + D-11)."
    - "Column headers click-to-sort with single-arrow affordance: ascending → descending → clear (back to createdAt desc default). Sortable columns: Title, Status, Priority, Assignee, Due date. Leading Checkbox + trailing Actions are not sortable (LIST-06)."
    - "Filter bar has three <Select> controls (Status, Priority, Assignee). 'All' is the default + cleared state. Changing any Select narrows the visible table rows. Filters AND together (LIST-07)."
    - "When listTasks(workspaceId).length === 0: hero ListsEmptyState renders with company-named copy, IconChecklist, and 'Create your first task' CTA opening the create modal. Same surface carries data-pendo-id={PENDO_IDS.lists.emptyState.container} + .cta (LIST-08 guide-anchor surface)."
    - "When tasks exist but filters yield zero rows: compact FilteredEmptyState renders inside the table body area with 'No tasks match these filters.' + 'Clear filters' Anchor that resets all three Selects to 'All' (D-06 distinct-from-hero rule)."
    - "All CRUD persists across page refresh: after create/edit/delete + refresh, tasks reflect the persisted state (LIST-09 — verified via writeJSON path locked by plan 04-02)."
    - "Every interactive control carries a data-pendo-id from PENDO_IDS.lists.* — no hand-typed string literals. Per-row Checkbox AND kebab + kebab items also carry data-pendo-task-id={task.id} per CLAUDE.md dynamic-list rule (PEN-07)."
    - "D-01: The Lists table is built on TanStack Table v8 as the single table primitive (shared with Reports per 04-05)."
    - "D-03: Lists filter bar renders exactly three Select controls (Status / Priority / Assignee); 'All' is the default and the cleared state; clearable={false} on every Select."
    - "D-04: Single-click status toggle is implemented as a leading Checkbox column on each row carrying data-pendo-task-id={task.id}; no Status Select inside row cells."
    - "D-05: Filter and sort state are component-level useState only — no URL persistence (no ?status=, ?priority=, ?assignee=, ?sort=) in Phase 4."
    - "D-07: One shared <TaskFormModal> component handles both create and edit via a mode='create'|'edit' prop; both paths use RHF + zodResolver(TaskFormSchema) and fire 'Task created' / 'Changes saved' toasts on save."
  artifacts:
    - path: "src/routes/app/lists/ListsPage.tsx"
      provides: "Page composer: page header + filter bar + table or empty states + modals"
      contains: "PENDO_IDS.lists"
    - path: "src/tasks/components/TaskTable.tsx"
      provides: "TanStack Table v8 with 7 columns + sort"
      contains: "@tanstack/react-table"
    - path: "src/tasks/components/TaskFiltersBar.tsx"
      provides: "Three-Select filter bar"
      contains: "TASK_STATUS_LABELS"
    - path: "src/tasks/components/TaskFormModal.tsx"
      provides: "Shared create/edit modal + RHF + Zod + repo write + toast"
      contains: "TaskFormSchema"
    - path: "src/tasks/components/DeleteConfirmModal.tsx"
      provides: "Destructive confirmation dialog"
      contains: "Delete this task"
    - path: "src/tasks/components/ListsEmptyState.tsx"
      provides: "Hero empty state for no-tasks-ever"
      contains: "Create your first task"
    - path: "src/tasks/components/FilteredEmptyState.tsx"
      provides: "Compact empty state for filter-yields-zero"
      contains: "Clear filters"
  key_links:
    - from: "src/tasks/components/TaskFormModal.tsx"
      to: "src/tasks/tasksRepo.ts createTask/updateTask"
      via: "RHF onSubmit handler"
      pattern: "tasksRepo\\.(createTask|updateTask)"
    - from: "src/tasks/components/TaskTable.tsx Checkbox column"
      to: "src/tasks/tasksRepo.ts updateTask"
      via: "Checkbox onChange handler"
      pattern: "updateTask.*status"
    - from: "src/tasks/components/TaskFormModal.tsx"
      to: "src/tasks/schemas.ts TaskFormSchema"
      via: "zodResolver(TaskFormSchema)"
      pattern: "zodResolver\\(TaskFormSchema\\)"
    - from: "ListsPage CRUD success"
      to: "@mantine/notifications notifications.show"
      via: "S8 toast pattern"
      pattern: "notifications\\.show"
    - from: "src/tasks/components/TaskFiltersBar.tsx Assignee select"
      to: "src/tasks/assigneeOptions.ts getAssigneeOptions"
      via: "function call with workspaceId + visitor"
      pattern: "getAssigneeOptions"
---

<objective>
Replace the Phase 3 placeholder body of `src/routes/app/lists/ListsPage.tsx` with the real Lists UI per the locked 04-UI-SPEC §"Lists page" contract: a Tasks page header with a primary "New task" button, a three-Select filter bar (Status / Priority / Assignee), a TanStack Table v8 with 7 columns + single-arrow sort + leading-checkbox status toggle + trailing-kebab Edit/Delete, two distinct empty states (no-tasks-ever vs filters-yield-zero), a shared create/edit modal with RHF + Zod, and a separate destructive delete-confirm modal. Closes LIST-01..09.

Purpose: Lists is the most-interactive Halo page and the primary Pendo-demo surface for tracked events (task created/saved/deleted), guides (empty states + filter bar), and Session Replay (per-row data-pendo-task-id for attribution). Every interactive control sources its `data-pendo-id` from `PENDO_IDS.lists.*` (extended in plan 04-01). All CRUD persists via the tasksRepo functions (extended in plan 04-02 with the completedAt invariant).

Output: Rewritten `src/routes/app/lists/ListsPage.tsx` (Phase 3 placeholder body replaced) + six new sub-component files in `src/tasks/components/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages-lists-settings-reports/04-CONTEXT.md
@.planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md
@.planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md
@CLAUDE.md
@src/routes/app/lists/ListsPage.tsx
@src/dashboard/Dashboard.tsx
@src/routes/public/signup/Step1AccountPage.tsx
@src/routes/public/signup/Step3CompanyPage.tsx
@src/tasks/tasksRepo.ts
@src/tasks/schemas.ts
@src/tasks/types.ts
@src/tasks/labels.ts
@src/auth/authStore.ts
@src/ui/primitives/index.ts
@src/pendo/PENDO_IDS.ts

<interfaces>
<!-- From src/tasks/types.ts (after plan 04-02) -->
`Task`, `TaskStatus`, `TaskPriority`, `Assignee`, `TaskFormValues` types.
- `Task = { id, title, description, status, priority, assignee, dueDate: string | null, completedAt: string | null, createdAt, updatedAt }`
- `TaskFormValues = z.infer<typeof TaskFormSchema>` — 6 user-editable fields.

<!-- From src/tasks/tasksRepo.ts (after plan 04-02) -->
`listTasks(workspaceId)`, `getTaskById(workspaceId, id)`, `createTask(workspaceId, input)`, `updateTask(workspaceId, id, patch)` — owns completedAt invariant, `deleteTask(workspaceId, id): boolean`. `CreateTaskInput` shape includes `completedAt: null` initially (or omitted; createTask stamps if `status==='done'`).

<!-- From src/tasks/schemas.ts (after plan 04-02) -->
`TaskFormSchema` z.object with title/description/status/priority/assignee/dueDate.

<!-- From src/tasks/labels.ts (after plan 04-02) -->
`TASK_STATUS_LABELS: Record<TaskStatus, string>`, `TASK_PRIORITY_LABELS: Record<TaskPriority, string>`, `TASK_STATUS_BADGE_COLOR: Record<TaskStatus, string>`, `TASK_PRIORITY_BADGE_COLOR: Record<TaskPriority, string>`.

<!-- From src/tasks/assigneeOptions.ts (created in plan 04-02) -->
`getAssigneeOptions(workspaceId: string, visitor: Visitor): Array<{ value: string; label: string }>` — pre-sorted, deduped, always includes current visitor.

<!-- From src/auth/authStore.ts -->
`useAuthStore` Zustand hook. Selectors used: `s.currentVisitor` (Visitor | null), `s.currentWorkspace?.id` (string | undefined).

<!-- From src/ui/primitives/index.ts (after plan 04-02) -->
Wrapped: `Button`, `TextInput`, `Textarea`, `Select`, `MultiSelect`, `PasswordInput`, `NumberInput`, `Checkbox` (with `taskId` prop), `DatePickerInput`, `Anchor`, `NavLink`. All accept `pendoId: PendoId` required.

<!-- From src/pendo/PENDO_IDS.ts lists namespace (after plan 04-01) -->
`PENDO_IDS.lists.newTaskButton`
`PENDO_IDS.lists.filter.{bar, status, priority, assignee}`
`PENDO_IDS.lists.row.{completeToggle, kebab, kebabEdit, kebabDelete}`
`PENDO_IDS.lists.modal.{container, title, description, status, priority, assignee, dueDate, cancel, save, delete}`
`PENDO_IDS.lists.deleteConfirm.{cancel, confirm}`
`PENDO_IDS.lists.emptyState.{container, cta}`
`PENDO_IDS.lists.filteredEmpty.{container, clearLink}`

<!-- Mantine 9 -->
`<Modal opened onClose title size centered>`, `<Tabs>`, `<Menu>` with `<Menu.Target>` + `<Menu.Dropdown>` + `<Menu.Item>`, `<ActionIcon>`, `<Badge>`, `<Center>`, `<Group>`, `<Stack>`, `<Paper>`, `<SegmentedControl>`, `<Title>`, `<Text>`.

<!-- @tanstack/react-table v8 -->
- `useReactTable({ data, columns, state, onSortingChange, getCoreRowModel, getSortedRowModel })`
- `getCoreRowModel()`, `getSortedRowModel()` — imported functions, not called from instance.
- `flexRender(column.columnDef.header, header.getContext())` for header cells; `flexRender(cell.column.columnDef.cell, cell.getContext())` for body cells.
- `createColumnHelper<Task>()` then `helper.accessor('title', { header: 'Title', cell: ... })`.
- `SortingState` type for the sorting state.
- `header.column.getCanSort()`, `header.column.getIsSorted()`, `header.column.toggleSorting()` for the sort affordance.

<!-- @mantine/notifications -->
`import { notifications } from '@mantine/notifications'` → `notifications.show({ title, message, color, icon, autoClose })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sub-components (table, filters, modals, empty states)</name>
  <files>src/tasks/components/TaskTable.tsx, src/tasks/components/TaskFiltersBar.tsx, src/tasks/components/TaskFormModal.tsx, src/tasks/components/DeleteConfirmModal.tsx, src/tasks/components/ListsEmptyState.tsx, src/tasks/components/FilteredEmptyState.tsx</files>
  <read_first>
    - src/dashboard/Dashboard.tsx (full file — analog for page-level useState, useMemo, conditional rendering, EmptyState function lines 264-280, SegmentedControl lines 316-328, Recharts Paper wrapper, parameterized data-pendo-task-id pattern at lines 437-448)
    - src/routes/public/signup/Step1AccountPage.tsx (analog for RHF + Zod + zodResolver + onSubmit + TextInput wiring + error= prop — lines 49-167)
    - src/routes/public/signup/Step3CompanyPage.tsx (analog for Select onChange enum-membership narrowing — lines 119-201; this is the canonical pattern for setValue with Mantine Select string | null result)
    - src/tasks/tasksRepo.ts (after plan 04-02 — confirm completedAt invariant landed; understand CreateTaskInput shape)
    - src/tasks/schemas.ts (after plan 04-02 — TaskFormSchema field list)
    - src/tasks/labels.ts (after plan 04-02 — badge color maps)
    - src/tasks/assigneeOptions.ts (after plan 04-02 — getAssigneeOptions signature)
    - src/pendo/PENDO_IDS.ts (after plan 04-01 — lists namespace leaf paths)
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"TanStack Table (Lists)" lines 208-260, §"Lists empty states" lines 263-309, §"Task form modal" lines 311-393, §"Delete confirmation modal" lines 395-426, §"Toast Notifications" lines 815-848, §"Copywriting Contract" lines 1018-1115
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/tasks/components/TaskTable.tsx" lines 454-462 (no-analog notes), §"src/tasks/components/TaskFormModal.tsx" lines 380-450, §"src/tasks/components/DeleteConfirmModal.tsx" lines 544-553, §"src/tasks/components/ListsEmptyState.tsx" lines 495-519, §"src/tasks/components/FilteredEmptyState.tsx" lines 523-539, §"Shared Patterns S8 Toast notifications" lines 996-1015
    - https://tanstack.com/table/v8/docs/api/core/table (column helper, getCoreRowModel, getSortedRowModel, flexRender). Look up at execute time if patterns unclear.
  </read_first>
  <action>
    Create six new files in `src/tasks/components/` (mkdir the directory first). Each component is self-contained and consumes props from the parent ListsPage (no global state beyond useAuthStore selectors). Use the wrapped primitives from `@/ui/primitives` for every interactive control — no raw Mantine components for inputs/buttons except `<Modal>`, `<Menu>`, `<ActionIcon>`, `<Tabs>`, `<SegmentedControl>` (which forward `data-pendo-id` via their direct slot props per S3 exception).

    ---

    **A) `src/tasks/components/TaskTable.tsx`** (no analog — TanStack Table is new):

    Props interface:
    ```
    type TaskTableProps = {
      tasks: Task[]                                       // already filtered by parent
      workspaceId: string
      onEdit: (task: Task) => void                        // opens edit modal
      onDelete: (task: Task) => void                      // opens delete confirm
      onToggleComplete: (task: Task, nextDone: boolean) => void  // calls tasksRepo.updateTask then refresh
    }
    ```

    Build with `useReactTable` + `createColumnHelper<Task>()`. Columns left → right per UI-SPEC §"TanStack Table (Lists)" table (rows 212-220):
    1. `helper.display({ id: 'select', header: '', cell: ({ row }) => <Checkbox checked={row.original.status === 'done'} onChange={(e) => onToggleComplete(row.original, e.currentTarget.checked)} pendoId={PENDO_IDS.lists.row.completeToggle} taskId={row.original.id} />, enableSorting: false })`
    2. `helper.accessor('title', { header: 'Title', cell: (info) => <Text size="sm">{info.getValue()}</Text> })`
    3. `helper.accessor('status', { header: 'Status', cell: (info) => <Badge color={TASK_STATUS_BADGE_COLOR[info.getValue()]} variant="light" size="sm">{TASK_STATUS_LABELS[info.getValue()]}</Badge> })`
    4. `helper.accessor('priority', { header: 'Priority', cell: same idiom with priority maps })`
    5. `helper.accessor((row) => row.assignee?.name ?? '—', { id: 'assignee', header: 'Assignee', cell: (info) => <Text size="sm">{info.getValue()}</Text> })`
    6. `helper.accessor('dueDate', { header: 'Due date', cell: (info) => <Text size="sm">{info.getValue() ? dayjs(info.getValue() as string).format('MMM D, YYYY') : '—'}</Text> })`
    7. `helper.display({ id: 'actions', header: '', cell: ({ row }) => <KebabMenu task={row.original} onEdit={onEdit} onDelete={onDelete} />, enableSorting: false })`

    Sorting state: `const [sorting, setSorting] = useState<SortingState>([{ id: 'createdAt', desc: true }])` — but createdAt is NOT a visible column, so use TanStack's `initialState.sorting` AND a hidden accessor: add an 8th column `helper.accessor('createdAt', { id: 'createdAt', header: '', cell: () => null, enableHiding: true })` then call `table.getColumn('createdAt')?.toggleVisibility(false)` after init. Alternative simpler path: just sort the input `tasks` array by `createdAt` desc as the default order when the user has not clicked any sortable column header, and let TanStack take over once they do. The clean idiom is to track `sorting` state explicitly and provide a `getSortedRowModel`. Pick the simpler "pre-sort default + let TanStack override on header click" path: sort `tasks` by `createdAt desc` BEFORE passing to TanStack; pass empty initial `sorting` state; toggle three-state click cycle (`asc → desc → clear → default createdAt desc`).

    Header rendering: For sortable columns (`getCanSort()` true), wrap header text in a clickable `<UnstyledButton onClick={() => column.toggleSorting()}>` with `<Group gap={6}>` + label + sort icon (IconChevronUp / IconChevronDown size 14, c="indigo.6" when `column.getIsSorted()`). For non-sortable columns (select + actions), render header as plain `<Text size="sm" fw={500}>{header}</Text>` (or empty for the no-label slots).

    Kebab menu — extract to a local `function KebabMenu({ task, onEdit, onDelete })` inside the file:
    ```
    <Menu position="bottom-end" withinPortal>
      <Menu.Target>
        <ActionIcon variant="subtle" data-pendo-id={PENDO_IDS.lists.row.kebab} data-pendo-task-id={task.id}>
          <IconDotsVertical size={16} />
        </ActionIcon>
      </Menu.Target>
      <Menu.Dropdown>
        <Menu.Item leftSection={<IconPencil size={16} />} data-pendo-id={PENDO_IDS.lists.row.kebabEdit} data-pendo-task-id={task.id} onClick={() => onEdit(task)}>Edit</Menu.Item>
        <Menu.Item leftSection={<IconTrash size={16} />} color="red" data-pendo-id={PENDO_IDS.lists.row.kebabDelete} data-pendo-task-id={task.id} onClick={() => onDelete(task)}>Delete</Menu.Item>
      </Menu.Dropdown>
    </Menu>
    ```

    Table wrapper: `<Paper withBorder p={0} radius="md" style={{ overflow: 'hidden' }}>` with a Mantine `<Table>` inside. Cell padding: use Mantine `Table` defaults OR add a CSS module `TaskTable.module.css` with `padding: var(--mantine-spacing-sm) var(--mantine-spacing-md)` per UI-SPEC line 226. Pick CSS module since no raw px is allowed.

    ---

    **B) `src/tasks/components/TaskFiltersBar.tsx`**:

    Props:
    ```
    type TaskFiltersBarProps = {
      workspaceId: string
      visitor: Visitor
      statusValue: string                                  // 'all' or TaskStatus
      priorityValue: string                                // 'all' or TaskPriority
      assigneeValue: string                                // 'all' or Assignee.id
      onChange: (next: { status?: string; priority?: string; assignee?: string }) => void
    }
    ```

    Render `<Group gap="md" data-pendo-id={PENDO_IDS.lists.filter.bar}>` with three `<Select>` (wrapped primitive) controls:
    - Status: data = `[{ value: 'all', label: 'All' }, ...TaskStatusEnum.options.map(s => ({ value: s, label: TASK_STATUS_LABELS[s] }))]`. `clearable={false}`. `w={160}`. `pendoId={PENDO_IDS.lists.filter.status}`. `value={statusValue}` + `onChange={(v) => onChange({ status: v ?? 'all' })}`.
    - Priority: same shape against `TaskPriorityEnum` + `TASK_PRIORITY_LABELS`.
    - Assignee: data = `[{ value: 'all', label: 'All' }, ...getAssigneeOptions(workspaceId, visitor)]`. `w={200}` (assignee names can be longer).

    No `<Paper>` wrapper around the filter bar per UI-SPEC line 200.

    ---

    **C) `src/tasks/components/TaskFormModal.tsx`**:

    Props:
    ```
    type TaskFormModalProps = {
      opened: boolean
      onClose: () => void
      onSuccess: () => void                                // signal parent to bump refreshKey
      onRequestDelete?: (task: Task) => void               // edit mode only: closes self, parent opens delete confirm
      workspaceId: string
      visitor: Visitor
      mode: 'create' | 'edit'
      initialTask?: Task                                   // required when mode='edit'
    }
    ```

    RHF setup (S2 pattern):
    ```
    const form = useForm<TaskFormValues>({
      resolver: zodResolver(TaskFormSchema),
      mode: 'onSubmit',
      defaultValues: mode === 'edit' && initialTask
        ? { title, description, status, priority, assignee, dueDate } from initialTask
        : { title: '', description: '', status: 'todo', priority: 'medium', assignee: { id: visitor.id, name: `${visitor.firstName} ${visitor.lastName}` }, dueDate: null },
    })
    ```

    Submit handler:
    ```
    const onSubmit = form.handleSubmit((values) => {
      if (mode === 'create') {
        tasksRepo.createTask(workspaceId, { ...values, completedAt: null })
        notifications.show({ title: 'Task created', message: '', color: 'green', icon: <IconCheck size={18} />, autoClose: 3000 })
      } else if (initialTask) {
        tasksRepo.updateTask(workspaceId, initialTask.id, values)
        notifications.show({ title: 'Changes saved', message: '', color: 'green', icon: <IconCheck size={18} />, autoClose: 3000 })
      }
      onSuccess()
      onClose()
    })
    ```

    Modal markup per UI-SPEC lines 315-323:
    ```
    <Modal opened={opened} onClose={onClose} title={<Title order={3}>{mode === 'create' ? 'New task' : 'Edit task'}</Title>} size="md" centered data-pendo-id={PENDO_IDS.lists.modal.container}>
      <form onSubmit={onSubmit} noValidate>
        <Stack gap="md">
          <TextInput {...form.register('title')} label="Title" required pendoId={PENDO_IDS.lists.modal.title} error={form.formState.errors.title?.message} />
          <Textarea {...form.register('description')} label="Description" rows={3} pendoId={PENDO_IDS.lists.modal.description} error={form.formState.errors.description?.message} />
          <Group gap="md" grow>
            <Select label="Status"   data={statusOptions}   value={form.watch('status') ?? null}   onChange={(v) => handleEnumChange('status', v, TaskStatusEnum)}     pendoId={PENDO_IDS.lists.modal.status}   error={form.formState.errors.status?.message} />
            <Select label="Priority" data={priorityOptions} value={form.watch('priority') ?? null} onChange={(v) => handleEnumChange('priority', v, TaskPriorityEnum)} pendoId={PENDO_IDS.lists.modal.priority} error={form.formState.errors.priority?.message} />
          </Group>
          <Group gap="md" grow>
            <Select label="Assignee" data={assigneeOptions} value={form.watch('assignee')?.id ?? null} onChange={(v) => handleAssigneeChange(v)} pendoId={PENDO_IDS.lists.modal.assignee} error={form.formState.errors.assignee?.message as string | undefined} />
            <DatePickerInput label="Due date" clearable value={form.watch('dueDate') ? new Date(form.watch('dueDate')!) : null} onChange={(d) => form.setValue('dueDate', d ? new Date(d).toISOString() : null)} pendoId={PENDO_IDS.lists.modal.dueDate} error={form.formState.errors.dueDate?.message as string | undefined} />
          </Group>
        </Stack>
        <Group justify="space-between" mt="lg">
          {mode === 'edit' && initialTask && (
            <Button color="red" variant="subtle" pendoId={PENDO_IDS.lists.modal.delete} onClick={() => { onClose(); onRequestDelete?.(initialTask) }}>Delete task</Button>
          )}
          <Group gap="md" ml="auto">
            <Button variant="default" pendoId={PENDO_IDS.lists.modal.cancel} onClick={onClose}>Discard</Button>
            <Button variant="filled" type="submit" disabled={mode === 'edit' && !form.formState.isDirty} pendoId={PENDO_IDS.lists.modal.save}>{mode === 'create' ? 'Create task' : 'Save changes'}</Button>
          </Group>
        </Group>
      </form>
    </Modal>
    ```

    `handleEnumChange` and `handleAssigneeChange` use the Step3CompanyPage Select narrowing idiom (lines 119-148 of Step3CompanyPage) — narrow against enum membership before `form.setValue`, never use `as` casts that hide type drift. For assignee: map the selected id back to a full Assignee object by scanning the assigneeOptions or using a Map<id, Assignee>.

    Delete-from-edit-modal flow: clicking the left "Delete task" calls `onClose()` first, then `onRequestDelete?.(initialTask)` — parent (ListsPage) opens the delete confirm modal in the next tick. Modal-on-modal stacking avoided per D-11.

    ---

    **D) `src/tasks/components/DeleteConfirmModal.tsx`**:

    Props:
    ```
    type DeleteConfirmModalProps = {
      opened: boolean
      onClose: () => void
      onConfirm: () => void                                // parent calls tasksRepo.deleteTask + fires toast + bumps refreshKey
      taskTitle: string
    }
    ```

    Markup verbatim from UI-SPEC lines 399-419:
    ```
    <Modal opened={opened} onClose={onClose} title={<Title order={3}>Delete this task?</Title>} size="sm" centered>
      <Text size="md">Delete "{taskTitle}"? This cannot be undone.</Text>
      <Group justify="flex-end" mt="lg" gap="md">
        <Button variant="default" pendoId={PENDO_IDS.lists.deleteConfirm.cancel} onClick={onClose}>Cancel</Button>
        <Button color="red" variant="filled" pendoId={PENDO_IDS.lists.deleteConfirm.confirm} onClick={() => { onConfirm(); onClose() }}>Delete task</Button>
      </Group>
    </Modal>
    ```

    No `data-pendo-id` on the Modal container itself for the delete confirm (the wrapped Button pendoIds are sufficient for guide targeting).

    ---

    **E) `src/tasks/components/ListsEmptyState.tsx`**:

    Per UI-SPEC lines 269-287 + analog Dashboard.tsx lines 264-280:
    ```
    type ListsEmptyStateProps = { workspaceName: string; onCreateClick: () => void }
    <Center mih={400} data-pendo-id={PENDO_IDS.lists.emptyState.container}>
      <Stack align="center" gap="md">
        <IconChecklist size={64} stroke={1.2} color="var(--mantine-color-gray-4)" />
        <Title order={3}>No tasks yet</Title>
        <Text c="dimmed" ta="center" maw={420}>{workspaceName} doesn't have any tasks yet. Create your first task to get started.</Text>
        <Button variant="filled" pendoId={PENDO_IDS.lists.emptyState.cta} onClick={onCreateClick}>Create your first task</Button>
      </Stack>
    </Center>
    ```

    ---

    **F) `src/tasks/components/FilteredEmptyState.tsx`**:

    Per UI-SPEC lines 293-307:
    ```
    type FilteredEmptyStateProps = { onClearFilters: () => void }
    <Center py="xl" data-pendo-id={PENDO_IDS.lists.filteredEmpty.container}>
      <Stack align="center" gap="sm">
        <Text c="dimmed">No tasks match these filters.</Text>
        <Anchor component="button" size="sm" pendoId={PENDO_IDS.lists.filteredEmpty.clearLink} onClick={onClearFilters}>Clear filters</Anchor>
      </Stack>
    </Center>
    ```

    Use the wrapped `Anchor` primitive (`Anchor.tsx` exists from Phase 1).

    ---

    After all six files exist, run `npm run typecheck` and `npm run build` — both must exit 0.
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      test -f src/tasks/components/TaskTable.tsx && \
      test -f src/tasks/components/TaskFiltersBar.tsx && \
      test -f src/tasks/components/TaskFormModal.tsx && \
      test -f src/tasks/components/DeleteConfirmModal.tsx && \
      test -f src/tasks/components/ListsEmptyState.tsx && \
      test -f src/tasks/components/FilteredEmptyState.tsx && \
      grep -c "from '@tanstack/react-table'" src/tasks/components/TaskTable.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "PENDO_IDS.lists.row.completeToggle" src/tasks/components/TaskTable.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "data-pendo-task-id" src/tasks/components/TaskTable.tsx | awk '$1 >= 3' | grep -q '.' && \
      grep -c "zodResolver(TaskFormSchema)" src/tasks/components/TaskFormModal.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "tasksRepo.createTask\\|createTask(workspaceId" src/tasks/components/TaskFormModal.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "tasksRepo.updateTask\\|updateTask(workspaceId" src/tasks/components/TaskFormModal.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "notifications.show" src/tasks/components/TaskFormModal.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "Create your first task" src/tasks/components/ListsEmptyState.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "No tasks match these filters" src/tasks/components/FilteredEmptyState.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "PENDO_IDS.lists.filter" src/tasks/components/TaskFiltersBar.tsx | awk '$1 >= 3' | grep -q '.' && \
      grep -c "Delete this task" src/tasks/components/DeleteConfirmModal.tsx | awk '$1 == 1' | grep -q '.' && \
      ! grep -rE "data-pendo-id=\"[^\"]+\"" src/tasks/components/ && \
      npm run typecheck && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - All six files exist under `src/tasks/components/`.
    - **TaskTable.tsx:** imports from `@tanstack/react-table` (`useReactTable`, `createColumnHelper`, `flexRender`, `getCoreRowModel`, `getSortedRowModel`, `SortingState`). Defines 7 columns. Leading column renders `<Checkbox>` from wrapped primitives with `pendoId={PENDO_IDS.lists.row.completeToggle}` and `taskId={row.original.id}`. Trailing column renders kebab `<Menu>` with Edit + Delete items, each carrying `data-pendo-id` from `PENDO_IDS.lists.row.kebab*` + `data-pendo-task-id={task.id}`. Wrapped in `<Paper withBorder p={0} radius="md">` with `overflow: 'hidden'`. Default sort `createdAt` desc applied to input tasks; header click toggles per-column sort.
    - **TaskFiltersBar.tsx:** Three `<Select>` controls inside `<Group gap="md" data-pendo-id={PENDO_IDS.lists.filter.bar}>`. Each Select has `clearable={false}`. Status options derive from `TaskStatusEnum.options` / `TASK_STATUS_LABELS`. Assignee options derive from `getAssigneeOptions(workspaceId, visitor)`. All three pendoIds from `PENDO_IDS.lists.filter.*`.
    - **TaskFormModal.tsx:** Uses `useForm<TaskFormValues>({ resolver: zodResolver(TaskFormSchema), mode: 'onSubmit', defaultValues: ... })`. Two distinct render paths for create vs edit (title text + button labels + Delete button visibility). Submit calls either `tasksRepo.createTask` or `tasksRepo.updateTask` based on mode, then fires `notifications.show` with the verbatim toast copy from UI-SPEC §"Copywriting Contract > Toast copy (verbatim)", then `onSuccess()` + `onClose()`. Edit mode footer has "Delete task" left-aligned (color="red", variant="subtle") that calls `onClose()` THEN `onRequestDelete?.(initialTask)`. Save disabled in edit mode iff form is not dirty. All six pendoIds from `PENDO_IDS.lists.modal.*` placed on the corresponding inputs/buttons.
    - **DeleteConfirmModal.tsx:** Size `sm`, centered, title "Delete this task?", body "Delete \"{taskTitle}\"? This cannot be undone.", footer with Cancel (variant="default") + Delete task (color="red" variant="filled"). pendoIds `PENDO_IDS.lists.deleteConfirm.cancel` + `.confirm`.
    - **ListsEmptyState.tsx:** Center mih=400, IconChecklist size=64 stroke=1.2, Title order=3 "No tasks yet", Text c="dimmed" with company-name-interpolated body, Button "Create your first task". pendoIds `PENDO_IDS.lists.emptyState.container` + `.cta`.
    - **FilteredEmptyState.tsx:** Center py="xl", Text c="dimmed" "No tasks match these filters.", Anchor `component="button"` "Clear filters". pendoIds `PENDO_IDS.lists.filteredEmpty.container` + `.clearLink`.
    - **Hand-typed string ban:** `grep -rE "data-pendo-id=\"[a-z][^\"]*\"" src/tasks/components/` returns NO matches (every `data-pendo-id=` references `PENDO_IDS.*` from the registry, not a literal string).
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Six self-contained sub-components ship with all PENDO_IDS plumbing, RHF + Zod + repo + toast wiring, exact-copy from UI-SPEC, and pass typecheck + build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose ListsPage and verify end-to-end CRUD + persistence</name>
  <files>src/routes/app/lists/ListsPage.tsx</files>
  <read_first>
    - src/routes/app/lists/ListsPage.tsx (current Phase 3 placeholder — its body is replaced)
    - src/dashboard/Dashboard.tsx (analog for page composer — workspaceId narrowing, useState/useMemo orchestration, conditional branch rendering)
    - All six sub-component files from Task 1 (read public Props types)
    - src/auth/authStore.ts (selectors used: currentVisitor, currentWorkspace)
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"Lists page" lines 164-195 (page structure verbatim), §"Copywriting Contract > CTA labels" lines 1036-1056
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/routes/app/lists/ListsPage.tsx" lines 747-776
  </read_first>
  <action>
    Replace the existing Phase 3 placeholder body of `src/routes/app/lists/ListsPage.tsx` with the real composer. Preserve the exported function name and signature (it's referenced by `src/router.tsx` — DO NOT edit the router).

    State surface (component-state only per D-05):
    ```
    const visitor = useAuthStore((s) => s.currentVisitor)
    const workspace = useAuthStore((s) => s.currentWorkspace)
    const workspaceId = workspace?.id

    const [statusFilter, setStatusFilter] = useState<string>('all')
    const [priorityFilter, setPriorityFilter] = useState<string>('all')
    const [assigneeFilter, setAssigneeFilter] = useState<string>('all')

    const [refreshKey, setRefreshKey] = useState(0)
    const refresh = () => setRefreshKey((k) => k + 1)

    const [createOpen, setCreateOpen] = useState(false)
    const [editTask, setEditTask] = useState<Task | null>(null)
    const [deleteTarget, setDeleteTarget] = useState<Task | null>(null)

    const allTasks = useMemo(
      () => (workspaceId ? listTasks(workspaceId) : []),
      [workspaceId, refreshKey],
    )

    const filteredTasks = useMemo(() => {
      return allTasks.filter((t) =>
        (statusFilter === 'all' || t.status === statusFilter) &&
        (priorityFilter === 'all' || t.priority === priorityFilter) &&
        (assigneeFilter === 'all' || t.assignee?.id === assigneeFilter),
      )
    }, [allTasks, statusFilter, priorityFilter, assigneeFilter])

    const filtersActive = statusFilter !== 'all' || priorityFilter !== 'all' || assigneeFilter !== 'all'
    const resetFilters = () => { setStatusFilter('all'); setPriorityFilter('all'); setAssigneeFilter('all') }
    ```

    Defensive narrowing: `if (!workspaceId || !visitor || !workspace) return <></>` — mirrors Dashboard.tsx line 307 belt-and-suspenders even though RequireAuth gates the route.

    Page body structure per UI-SPEC lines 166-196:
    ```
    return (
      <Stack gap="lg">
        <Group justify="space-between" align="center">
          <Title order={3}>Tasks</Title>
          {allTasks.length > 0 && (
            <Button variant="filled" leftSection={<IconPlus size={16} />} pendoId={PENDO_IDS.lists.newTaskButton} onClick={() => setCreateOpen(true)}>New task</Button>
          )}
        </Group>

        {allTasks.length === 0 ? (
          <ListsEmptyState workspaceName={workspace.companyName} onCreateClick={() => setCreateOpen(true)} />
        ) : (
          <>
            <TaskFiltersBar
              workspaceId={workspaceId}
              visitor={visitor}
              statusValue={statusFilter}
              priorityValue={priorityFilter}
              assigneeValue={assigneeFilter}
              onChange={(next) => {
                if (next.status !== undefined) setStatusFilter(next.status)
                if (next.priority !== undefined) setPriorityFilter(next.priority)
                if (next.assignee !== undefined) setAssigneeFilter(next.assignee)
              }}
            />
            {filteredTasks.length === 0 && filtersActive ? (
              <FilteredEmptyState onClearFilters={resetFilters} />
            ) : (
              <TaskTable
                tasks={filteredTasks}
                workspaceId={workspaceId}
                onEdit={(task) => setEditTask(task)}
                onDelete={(task) => setDeleteTarget(task)}
                onToggleComplete={(task, nextDone) => {
                  tasksRepo.updateTask(workspaceId, task.id, { status: nextDone ? 'done' : 'todo' })
                  refresh()
                }}
              />
            )}
          </>
        )}

        <TaskFormModal
          opened={createOpen}
          onClose={() => setCreateOpen(false)}
          onSuccess={refresh}
          workspaceId={workspaceId}
          visitor={visitor}
          mode="create"
        />

        <TaskFormModal
          opened={editTask !== null}
          onClose={() => setEditTask(null)}
          onSuccess={refresh}
          onRequestDelete={(task) => setDeleteTarget(task)}
          workspaceId={workspaceId}
          visitor={visitor}
          mode="edit"
          initialTask={editTask ?? undefined}
        />

        <DeleteConfirmModal
          opened={deleteTarget !== null}
          onClose={() => setDeleteTarget(null)}
          onConfirm={() => {
            if (deleteTarget) {
              tasksRepo.deleteTask(workspaceId, deleteTarget.id)
              notifications.show({ title: 'Task deleted', message: '', color: 'red', icon: <IconTrash size={18} />, autoClose: 3000 })
              refresh()
            }
          }}
          taskTitle={deleteTarget?.title ?? ''}
        />
      </Stack>
    )
    ```

    Imports needed: `useState`, `useMemo` from React; `useAuthStore` from `@/auth/authStore`; `listTasks`, all of `tasksRepo` as `* as tasksRepo` (or named imports); `PENDO_IDS` from `@/pendo/PENDO_IDS`; `Stack`, `Group`, `Title` from `@mantine/core`; `Button` from `@/ui/primitives`; `IconPlus`, `IconTrash` from `@tabler/icons-react`; `notifications` from `@mantine/notifications`; six sub-components from `@/tasks/components/...`; `Task` type from `@/tasks/types`.

    After writing the page, run:
    1. `npm run typecheck` → exit 0.
    2. `npm run build` → exit 0.
    3. `npm run dev`, sign in (or sign up), navigate to `/app/lists`. Manual smoke (record in SUMMARY):
       a. With seeded tasks present: filter bar + table render; click a row checkbox → row badge toggles to/from "Done"; refresh page → toggle persists.
       b. Click "New task" → modal opens with status='To do', priority='Medium' defaults; submit empty Title → "Enter a task title." error renders inline; fill required + submit → toast "Task created" + new row appears.
       c. Click row kebab → Edit → modal opens with task values; change title + Save → toast "Changes saved" + row updates.
       d. Click row kebab → Delete → confirm modal opens with task title quoted; click Delete task → toast "Task deleted" + row removed.
       e. From edit modal, click left "Delete task" button → edit modal closes, delete confirm opens → confirm → row removed.
       f. Set filters such that no rows match → FilteredEmptyState renders inside table area with "Clear filters" Anchor; click it → all filters reset to All + table shows tasks.
       g. Manually wipe tasks via `localStorage.removeItem('halo:v1:tasks:<wsId>')` in DevTools + reload → hero ListsEmptyState renders with company name interpolated + Create your first task CTA.
       h. Toggle dark mode via Settings (if plan 04-04 has shipped — else manually via DevTools matchMedia override): table, filter bar, modals, badges, empty states all recolor cleanly with no hardcoded hex bleed-through.
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      grep -c "from '../../../tasks/components" src/routes/app/lists/ListsPage.tsx | awk '$1 >= 5' | grep -q '.' || \
      grep -c "from '@/tasks/components" src/routes/app/lists/ListsPage.tsx | awk '$1 >= 5' | grep -q '.' && \
      grep -c "TaskFormModal" src/routes/app/lists/ListsPage.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "DeleteConfirmModal" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "ListsEmptyState" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "FilteredEmptyState" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "TaskTable" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "TaskFiltersBar" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "PENDO_IDS.lists.newTaskButton" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "Task deleted" src/routes/app/lists/ListsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "useAuthStore" src/routes/app/lists/ListsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "listTasks" src/routes/app/lists/ListsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "tasksRepo.deleteTask\\|deleteTask(workspaceId" src/routes/app/lists/ListsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      npm run typecheck && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/app/lists/ListsPage.tsx` exports a default function (same export name as before — verify with the router. `grep -E "export (default )?function ListsPage|export default" src/routes/app/lists/ListsPage.tsx | wc -l ≥ 1`).
    - Imports all six sub-components from `src/tasks/components/`.
    - Defensive narrowing: `if (!workspaceId || !visitor || !workspace) return <></>` present.
    - State surface: useState calls for statusFilter, priorityFilter, assigneeFilter, refreshKey, createOpen, editTask, deleteTarget.
    - `listTasks(workspaceId)` is called inside `useMemo` keyed on `[workspaceId, refreshKey]`.
    - Filter logic ANDs the three predicates (status / priority / assignee).
    - "New task" button renders ONLY when `allTasks.length > 0` (when 0, the empty state CTA replaces it).
    - Page header pendoId: `PENDO_IDS.lists.newTaskButton`.
    - DeleteConfirmModal onConfirm fires `notifications.show({ title: 'Task deleted', ... })` and calls `tasksRepo.deleteTask`.
    - Edit modal's `onRequestDelete` callback sets `deleteTarget` — the edit modal's own onClose has already fired by then (Sub-component handles this internally per Task 1 D step).
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
    - **End-to-end CRUD persistence smoke (manual, record in SUMMARY):** Each of the seven scenarios (a-g) in the Task 2 action block succeeds. Hard refresh after each mutation confirms persistence (LIST-09).
    - **Dark mode regression check (manual, record in SUMMARY):** Page renders cleanly in both schemes (verify after plan 04-04 ships, or use DevTools "Emulate prefers-color-scheme: dark" temporarily).
    - **Pendo target audit:** Open DevTools, query `document.querySelectorAll('[data-pendo-id^="lists."]').length` ≥ 12 on a non-empty Lists page (covers: newTaskButton + 3 filter selects + N row checkboxes + N row kebabs + filter bar container + modal pendoIds when open). Record exact count in SUMMARY.
  </acceptance_criteria>
  <done>
    `/app/lists` renders the real Lists UI: page header, filter bar, TanStack Table, both empty states, and the three modals. All nine LIST requirements satisfied: LIST-01 (page exists at /app/lists), LIST-02 (create modal), LIST-03 (edit modal), LIST-04 (checkbox toggle), LIST-05 (delete confirm), LIST-06 (column sort), LIST-07 (three filters), LIST-08 (empty state), LIST-09 (persistence via repo writeJSON). Typecheck + build pass. Manual CRUD smoke recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TaskFormModal RHF onSubmit → tasksRepo | User input crosses into the persistence layer; Zod validates first |
| Leading checkbox onChange → tasksRepo.updateTask | Toggle crosses into repo; completedAt invariant owned by repo |
| Kebab Delete + edit-modal Delete → DeleteConfirmModal → tasksRepo.deleteTask | Destructive action gated by explicit confirmation modal (UI-03 lock) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | TaskFormModal submit | mitigate | `zodResolver(TaskFormSchema)` rejects invalid types at the form boundary; only validated values reach `tasksRepo.createTask`/`updateTask`. |
| T-04-03-02 | Tampering | Direct-DevTools mutation of `halo:v1:tasks:<wsId>` | accept | Single-user demo, browser-local storage. FND-04 `readWithSchema` falls back to `[]` on parse failure, so corrupt local data does not crash — accepted risk per Phase 1 lock. |
| T-04-03-03 | Repudiation | Destructive delete with no undo | mitigate | DeleteConfirmModal explicit confirmation gate (per UI-03). Body copy "This cannot be undone." sets user expectation. |
| T-04-03-04 | Information Disclosure | data-pendo-task-id={task.id} | accept | Task IDs are nanoid-generated, not PII; the per-row attribution is the explicit Pendo-readiness contract (PEN-08 + CLAUDE.md dynamic-list rule). |
| T-04-03-05 | Tampering | Checkbox onChange race vs concurrent edit-modal | mitigate | Checkbox writes ONLY the `status` patch; the edit modal writes a full patch on Save. The tasksRepo's read-modify-write is non-atomic per Phase 2 WR-04 note, but in a single-user demo there is no concurrent writer to race with. |
| T-04-03-06 | Spoofing | Hand-typed `data-pendo-id` strings | mitigate | TypeScript `pendoId: PendoId` requires the value to be a Leaves-derived literal; hand-typed strings fail at compile time (CONVENTIONS.md §1 enforcement). Acceptance criterion `grep -rE "data-pendo-id=\"[a-z]" src/tasks/components/` = 0. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after both tasks.
- `npm run build` exits 0.
- All 9 LIST-* requirements have a corresponding code path:
  - LIST-01: `src/routes/app/lists/ListsPage.tsx` body replaced; route already registered by Phase 3.
  - LIST-02: TaskFormModal mode='create' + `tasksRepo.createTask` invocation.
  - LIST-03: TaskFormModal mode='edit' + `tasksRepo.updateTask` invocation.
  - LIST-04: Checkbox column onChange → `tasksRepo.updateTask({ status })`.
  - LIST-05: DeleteConfirmModal + `tasksRepo.deleteTask` invocation, dual-trigger from kebab + edit-modal delete button.
  - LIST-06: TanStack getSortedRowModel + column header click + single-arrow affordance.
  - LIST-07: Three Select controls + AND-filter logic in ListsPage useMemo.
  - LIST-08: ListsEmptyState renders when `allTasks.length === 0`.
  - LIST-09: All mutations route through tasksRepo, which routes through `writeJSON` (FND-04 lock — persistence is structural).
- `grep -rE "data-pendo-id=\"[a-z]" src/tasks/components/ src/routes/app/lists/ListsPage.tsx` returns 0 matches (no hand-typed strings).
- TanStack Table renders without console errors at the dev URL.
</verification>

<success_criteria>
- A user can complete the full Lists CRUD loop end-to-end: see tasks → create one → edit it → toggle complete via checkbox → delete it → all changes persist across refresh.
- The page satisfies the locked UI design contract in 04-UI-SPEC.md verbatim (page header, filter bar, table columns, empty states, modal field order, button labels).
- Pendo guides can target every interactive control via stable `data-pendo-id` from the registry.
- Lists page renders cleanly in both light and dark schemes (relies on plan 04-01 dark-mode wiring + 04-02 badge color tokens).
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages-lists-settings-reports/04-03-SUMMARY.md` capturing:
- The seven-scenario manual CRUD smoke test results.
- Exact pendoId selector count on a non-empty Lists page (DevTools `querySelectorAll('[data-pendo-id^="lists."]').length`).
- Any deviations from UI-SPEC (with rationale, if any).
- Any Mantine `<Modal>`-specific behavior worth noting for downstream plans (esp. modal-on-modal stacking caveats encountered).
</output>
