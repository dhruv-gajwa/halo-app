---
phase: 04-core-pages-lists-settings-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main.tsx
  - src/App.tsx
  - index.html
  - src/pendo/PENDO_IDS.ts
autonomous: true
requirements:
  - SET-04
must_haves:
  truths:
    - "TanStack Table v8, @mantine/dates, dayjs, and @mantine/notifications are installed at pinned versions and resolve at build time without peer-dep errors."
    - "The Mantine ColorSchemeScript in index.html performs real auto-detection (reads localStorage[mantine-color-scheme-value] and falls back to prefers-color-scheme), instead of hardcoded 'light' (D-18)."
    - "MantineProvider in App.tsx uses defaultColorScheme='auto' (not 'light') so the theme toggle in Phase 4's Preferences tab can actually take effect (D-18)."
    - "<Notifications /> is mounted inside MantineProvider in App.tsx so any toast fired from a Phase 4 page renders correctly (S8 prerequisite for plans 04-03 and 04-04)."
    - "PENDO_IDS exports three new top-level namespaces — lists, settings, reports — with every leaf string from 04-UI-SPEC §'Pendo ID Registry — Phase 4 Extensions' verbatim (D-24, D-25, PEN-07)."
    - "The PendoId type derivation automatically picks up the new namespaces (Leaves<typeof PENDO_IDS> includes 'lists.new-task-button' etc.)."
    - "D-01: TanStack Table v8 is installed as the single table primitive for Phase 4 (used by Lists in 04-03 and Reports in 04-05)."
    - "D-26: Phase 4 does NOT bump SCHEMA_VERSION on these dependency/wiring extensions; the Mantine theme localStorage key (mantine-color-scheme-value) is preserved across the Settings reset flow."
  artifacts:
    - path: "package.json"
      provides: "Runtime deps: @tanstack/react-table ^8, @mantine/dates ^9, dayjs ^1, @mantine/notifications ^9"
      contains: "@tanstack/react-table"
    - path: "src/App.tsx"
      provides: "MantineProvider with defaultColorScheme='auto' and <Notifications /> mount inside it"
      contains: "defaultColorScheme=\"auto\""
    - path: "index.html"
      provides: "Auto-detecting ColorSchemeScript that reads localStorage and prefers-color-scheme"
      contains: "mantine-color-scheme-value"
    - path: "src/pendo/PENDO_IDS.ts"
      provides: "lists, settings, reports namespaces appended to PENDO_IDS const"
      contains: "lists.new-task-button"
  key_links:
    - from: "src/App.tsx"
      to: "@mantine/notifications"
      via: "import { Notifications } from '@mantine/notifications' + <Notifications /> child of MantineProvider"
      pattern: "<Notifications"
    - from: "src/main.tsx"
      to: "@mantine/notifications/styles.css"
      via: "import '@mantine/notifications/styles.css'"
      pattern: "@mantine/notifications/styles.css"
    - from: "src/main.tsx"
      to: "@mantine/dates/styles.css"
      via: "import '@mantine/dates/styles.css'"
      pattern: "@mantine/dates/styles.css"
    - from: "src/pendo/PENDO_IDS.ts"
      to: "PendoId type"
      via: "Leaves<typeof PENDO_IDS> includes lists/settings/reports leaves"
      pattern: "lists:|settings:|reports:"
---

<objective>
Wave-1 foundation plan that installs the four new runtime deps Phase 4 needs, fixes the dark-mode wiring (App.tsx defaultColorScheme + index.html ColorSchemeScript) so the Settings Preferences theme toggle in plan 04-04 actually applies, mounts `<Notifications />` inside `MantineProvider` so the toast pattern in S8 works for plans 04-03 and 04-04, and extends `src/pendo/PENDO_IDS.ts` with the three new top-level namespaces (`lists`, `settings`, `reports`) that every Phase 4 page needs to import IDs from.

Purpose: All three page plans (04-03 Lists, 04-04 Settings, 04-05 Reports) depend on this plan's outputs. Without it: hand-typed pendo strings would be needed (CONVENTIONS.md §1 ban), toasts would silently no-op, the theme toggle would render but not apply, and `@tanstack/react-table` / `dayjs` imports would fail at build.

Output: Updated `package.json` + lockfile with four new pinned deps. Updated `src/App.tsx`, `index.html`, `src/main.tsx`. Extended `src/pendo/PENDO_IDS.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages-lists-settings-reports/04-CONTEXT.md
@.planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md
@.planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md
@CLAUDE.md
@src/App.tsx
@src/main.tsx
@index.html
@src/pendo/PENDO_IDS.ts

<interfaces>
<!-- From src/pendo/PENDO_IDS.ts (existing) -->
Existing `PENDO_IDS` is an `as const` object with namespaces: layout, sandbox, signup (with step1..step4 + stepper), signin, nav, topbar, dashboard, comingSoon. `Leaves<typeof PENDO_IDS>` derives `PendoId` as the union of every string leaf. Phase 4 APPENDS three new top-level keys (lists, settings, reports) — does NOT touch existing namespaces.

<!-- From src/App.tsx (current) -->
```typescript
export default function App(): React.JSX.Element {
  return (
    <MantineProvider theme={haloTheme} defaultColorScheme="light">
      <StorageProvider>
        <AuthProvider>
          <WorkspaceProvider>
            <PendoBridge>
              <RouterProvider router={router} />
            </PendoBridge>
          </WorkspaceProvider>
        </AuthProvider>
      </StorageProvider>
    </MantineProvider>
  )
}
```
The FND-07 provider order (MantineProvider → StorageProvider → AuthProvider → WorkspaceProvider → PendoBridge → Router) is locked. `<Notifications />` is a Mantine sibling that mounts INSIDE `MantineProvider` and OUTSIDE the rest of the provider tree (per Mantine docs convention).

<!-- From index.html (current) -->
The existing inline script hardcodes `'light'`. Phase 4's first plan replaces it with the real Mantine v9 inline auto-detection snippet.

<!-- @mantine/notifications module shape -->
Exports: `Notifications` (React component to mount once), `notifications` (object with `.show()`, `.update()`, `.hide()`, `.clean()` methods). Side-effect CSS: `@mantine/notifications/styles.css`.

<!-- @mantine/dates module shape -->
Exports: `DatePickerInput`, `DateInput`, `DatePicker`, `MonthPickerInput`, etc. Peer dep: `dayjs`. Side-effect CSS: `@mantine/dates/styles.css`.

<!-- @tanstack/react-table v8 module shape -->
Exports: `useReactTable`, `getCoreRowModel`, `getSortedRowModel`, `flexRender`, `createColumnHelper`, `ColumnDef` (type), `SortingState` (type). No styles import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install runtime deps + wire dark-mode + mount Notifications</name>
  <files>package.json, package-lock.json, src/main.tsx, src/App.tsx, index.html</files>
  <read_first>
    - package.json (verify @mantine/core 9.2.0, react 19.2.0, recharts 3.8.1 versions for peer-dep alignment)
    - src/App.tsx (existing MantineProvider stack — must preserve FND-07 ordering)
    - src/main.tsx (existing Mantine core styles import — adding two more side-effect CSS imports)
    - index.html (existing hardcoded ColorSchemeScript line 11 — replacing with real auto-detection)
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"Design System", §"Dark mode color contract" lines 131-156
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/App.tsx" lines 803-833, §"index.html" lines 836-848
  </read_first>
  <action>
    1) Install four new runtime deps via `npm install --save @tanstack/react-table @mantine/dates @mantine/notifications dayjs`. Pin to the version `npm view <pkg> version` reports at install time (Phase 1 blocker compliance — no version drift). `@mantine/dates` and `@mantine/notifications` MUST be the same major as `@mantine/core` 9.2.0 (peer-dep alignment); if `npm view` reports a 9.x that satisfies, use it. `@tanstack/react-table` ^8.x latest. `dayjs` ^1.x latest. After install, run `npm ls @tanstack/react-table @mantine/dates @mantine/notifications dayjs` to confirm no UNMET PEER DEPENDENCY warnings on react@19; document any non-fatal peer warnings in the plan SUMMARY.

    2) Edit `src/main.tsx`: keep the existing `import '@mantine/core/styles.css'` line, and append (in this order) `import '@mantine/dates/styles.css'` and `import '@mantine/notifications/styles.css'`. Order matters — Mantine docs say core CSS first, then per-package CSS. Do NOT reorder `runMigrations()` — it stays before `createRoot`.

    3) Edit `src/App.tsx`:
       - Add `import { Notifications } from '@mantine/notifications'` near the other Mantine imports (after `import { MantineProvider } from '@mantine/core'`).
       - Change `defaultColorScheme="light"` to `defaultColorScheme="auto"` (D-18 lock — first phase to wire real dark mode).
       - Mount `<Notifications />` as the FIRST child of `<MantineProvider>` (immediately before `<StorageProvider>`). Notifications is a Mantine sibling renderer, not a Halo provider — it does NOT slot into the FND-07 Storage→Auth→Workspace→PendoBridge chain. Preserve the FND-07 ordering JSDoc comment at lines 11-24 verbatim.

    4) Edit `index.html` line 11: replace the hardcoded `setAttribute('data-mantine-color-scheme', 'light')` script with Mantine v9's real auto-detection snippet that reads `localStorage.getItem('mantine-color-scheme-value')` first, then falls back to `window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'`. The script MUST handle the `'auto'` stored value by resolving via matchMedia. Inline script (no external src so it runs before paint with zero network cost). Mantine 9 docs provide the canonical snippet; replicate the resolution logic functionally — wrap in IIFE, try/catch around localStorage access (private-browsing defense). The snippet should match Mantine v9's `<ColorSchemeScript defaultColorScheme="auto" />` server-rendered output (look up the official Mantine v9 snippet at install time per CLAUDE.md "verify install patterns" note).

    5) After all edits, run `npm run typecheck` then `npm run build`. Both MUST exit 0. Run `npm run dev` and confirm the app boots without console errors (manual check; record in SUMMARY).
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      grep -q '"@tanstack/react-table"' package.json && \
      grep -q '"@mantine/dates"' package.json && \
      grep -q '"@mantine/notifications"' package.json && \
      grep -q '"dayjs"' package.json && \
      grep -q "import '@mantine/notifications/styles.css'" src/main.tsx && \
      grep -q "import '@mantine/dates/styles.css'" src/main.tsx && \
      grep -q 'defaultColorScheme="auto"' src/App.tsx && \
      grep -q "from '@mantine/notifications'" src/App.tsx && \
      grep -q '<Notifications' src/App.tsx && \
      grep -q 'mantine-color-scheme-value' index.html && \
      grep -q 'prefers-color-scheme' index.html && \
      npm run typecheck && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import '@mantine/notifications/styles.css'" src/main.tsx` returns 1.
    - `grep -c 'defaultColorScheme="auto"' src/App.tsx` returns 1; `grep -c 'defaultColorScheme="light"' src/App.tsx` returns 0.
    - `grep -c '<Notifications' src/App.tsx` returns 1.
    - `grep -E "from '@mantine/notifications'" src/App.tsx` matches (Notifications import line).
    - `grep -c 'data-mantine-color-scheme.*light.*</script>' index.html` returns 0 (the hardcoded line is gone).
    - `grep -c 'mantine-color-scheme-value' index.html` returns 1 (real ColorSchemeScript reads the key).
    - `grep -c 'prefers-color-scheme' index.html` returns 1 (fallback to OS scheme).
    - `node -e "const p = require('./package.json'); ['@tanstack/react-table','@mantine/dates','@mantine/notifications','dayjs'].forEach(k => { if (!p.dependencies[k]) { console.error('missing', k); process.exit(1) } })"` exits 0.
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
    - FND-07 provider order intact: `grep -A 10 'MantineProvider theme={haloTheme}' src/App.tsx | grep -v '^#'` shows StorageProvider before AuthProvider before WorkspaceProvider before PendoBridge before RouterProvider.
  </acceptance_criteria>
  <done>
    Four deps installed at pinned versions, App.tsx + index.html dark-mode wired (auto detection live), Notifications mounted, typecheck + build pass, dev server boots without console errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend PENDO_IDS registry with lists / settings / reports namespaces</name>
  <files>src/pendo/PENDO_IDS.ts</files>
  <read_first>
    - src/pendo/PENDO_IDS.ts (full file — appending three top-level keys between `comingSoon` and the closing `} as const` brace)
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"Pendo ID Registry — Phase 4 Extensions" lines 881-974 (the LITERAL leaf strings to ship — verbatim)
    - .planning/phases/04-core-pages-lists-settings-reports/04-CONTEXT.md D-24, D-25
    - docs/CONVENTIONS.md §1 (hand-typed string ban) — every leaf is kebab-case dotted path
  </read_first>
  <action>
    Append three new top-level keys to the `PENDO_IDS` const object literal in `src/pendo/PENDO_IDS.ts`, between the existing `comingSoon: { card: 'coming-soon.card' }` entry and the closing `} as const` brace.

    The new namespaces are `lists`, `settings`, and `reports`. Use the EXACT structure and leaf strings from 04-UI-SPEC.md §"Pendo ID Registry — Phase 4 Extensions" lines 885-973 — verbatim, no edits. Key keys to ship (non-exhaustive sanity reference; UI-SPEC is source of truth):

    `lists`: top-level leaves and groups —
      - `newTaskButton: 'lists.new-task-button'`
      - `filter: { bar, status, priority, assignee }` → 'lists.filter.bar', 'lists.filter.status', 'lists.filter.priority', 'lists.filter.assignee'
      - `row: { completeToggle, kebab, kebabEdit, kebabDelete }` → 'lists.row.complete-toggle', 'lists.row.kebab', 'lists.row.kebab-edit', 'lists.row.kebab-delete'
      - `modal: { container, title, description, status, priority, assignee, dueDate, cancel, save, delete }` → 'lists.modal.container' through 'lists.modal.delete' (note `dueDate` leaf is 'lists.modal.due-date')
      - `deleteConfirm: { cancel, confirm }` → 'lists.delete-confirm.cancel', 'lists.delete-confirm.confirm'
      - `emptyState: { container, cta }` → 'lists.empty-state.container', 'lists.empty-state.cta'
      - `filteredEmpty: { container, clearLink }` → 'lists.filtered-empty.container', 'lists.filtered-empty.clear-link'

    `settings`: —
      - `tabs: { profile, workspace, preferences }` → 'settings.tabs.profile' etc.
      - `profile: { firstName, lastName, username, jobTitle, role, location, save, cancel }` → 'settings.profile.first-name' etc. (note kebab-case: `firstName` key → 'first-name' leaf)
      - `workspace: { companyName, companySize, industry, planTier, save, cancel }` → 'settings.workspace.company-name' etc.
      - `preferences: { themeToggle }` → 'settings.preferences.theme-toggle'
      - `dangerZone: { button, confirmCancel, confirmButton }` → 'settings.danger-zone.button', 'settings.danger-zone.confirm-cancel', 'settings.danger-zone.confirm-button'

    `reports`: —
      - `filter: { dateRange, assignee, status }` → 'reports.filter.date-range', 'reports.filter.assignee', 'reports.filter.status'
      - `chart: { statusByDay }` → 'reports.chart.status-by-day'
      - `table: { container }` → 'reports.table.container'
      - `csvExport: 'reports.csv-export'`

    Add a brief leading JSDoc comment block above each new top-level key matching the precedent at lines 110-119 (the `nav:` comment is the shortest existing example): one-line description of the namespace's scope. For example:
      - `/** Phase 4 Lists page targets (task table + filters + modals + empty states + row-level dynamic IDs). */`
      - `/** Phase 4 Settings page targets (three tabs + profile/workspace forms + theme toggle + danger zone). */`
      - `/** Phase 4 Reports page targets (filter bar + stacked-bar chart + read-only table + CSV export). */`

    Do NOT touch the existing namespaces (layout, sandbox, signup, signin, nav, topbar, dashboard, comingSoon). Do NOT modify the `Leaves<T>` type or the `export type PendoId` derivation — both pick up the new leaves automatically.

    Do NOT edit the header comment "Namespace growth plan" block at lines 13-19 — it already lists `Phase 4: lists, settings, reports` as planned, so no edit needed.
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      grep -c "lists:" src/pendo/PENDO_IDS.ts | awk '$1 >= 1' | grep -q '.' && \
      grep -c "settings:" src/pendo/PENDO_IDS.ts | awk '$1 >= 1' | grep -q '.' && \
      grep -c "reports:" src/pendo/PENDO_IDS.ts | awk '$1 >= 1' | grep -q '.' && \
      grep -c "'lists.new-task-button'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'lists.row.complete-toggle'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'lists.modal.due-date'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'settings.tabs.profile'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'settings.profile.first-name'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'settings.danger-zone.confirm-button'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'reports.filter.date-range'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'reports.csv-export'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "'reports.chart.status-by-day'" src/pendo/PENDO_IDS.ts | awk '$1 == 1' | grep -q '.' && \
      npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  lists:" src/pendo/PENDO_IDS.ts` returns 1 (new top-level key).
    - `grep -c "^  settings:" src/pendo/PENDO_IDS.ts` returns 1.
    - `grep -c "^  reports:" src/pendo/PENDO_IDS.ts` returns 1.
    - Every leaf string from 04-UI-SPEC §"Pendo ID Registry — Phase 4 Extensions" lines 885-973 appears verbatim. Spot-check: `grep -c "'lists.modal.due-date'" src/pendo/PENDO_IDS.ts` = 1; `grep -c "'settings.profile.job-title'" src/pendo/PENDO_IDS.ts` = 1; `grep -c "'reports.chart.status-by-day'" src/pendo/PENDO_IDS.ts` = 1.
    - Existing namespaces unchanged: `grep -c "'signup.step1.email'" src/pendo/PENDO_IDS.ts` returns 1, `grep -c "'dashboard.kpi.active'" src/pendo/PENDO_IDS.ts` returns 1, `grep -c "'coming-soon.card'" src/pendo/PENDO_IDS.ts` returns 1.
    - `npm run typecheck` exits 0 (no TS errors from `as const` literal narrowing or Leaves derivation).
    - `node -e "const m = require('./src/pendo/PENDO_IDS.ts'.replace('.ts','')); /* runtime cannot import .ts directly */"` — skip; rely on typecheck.
    - Leaf-string convention enforced: every new leaf is a dotted, kebab-case string (no camelCase in the VALUES). Audit: `grep -E "'(lists|settings|reports)\." src/pendo/PENDO_IDS.ts | grep -E '[A-Z]'` returns no matches (no uppercase in any new leaf string).
  </acceptance_criteria>
  <done>
    PENDO_IDS exports three new namespaces with all leaves from 04-UI-SPEC §"Pendo ID Registry — Phase 4 Extensions" verbatim. PendoId type derivation picks them up. Existing namespaces untouched. Typecheck passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localStorage | Mantine reads `mantine-color-scheme-value` from localStorage in the inline script; private-browsing throws when localStorage is disabled |
| dependency supply chain → app bundle | Four new npm deps enter the runtime bundle; integrity comes from `package-lock.json` |
| inline script → DOM | `index.html` inline script runs before React mounts, mutates `<html>` attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `package-lock.json` | mitigate | Lockfile committed; deps pinned via `npm view <pkg> version` at install (Phase 1 blocker compliance). Reviewers verify resolved versions match. |
| T-04-01-02 | Denial of Service | `index.html` ColorSchemeScript | mitigate | Wrap localStorage read in try/catch — private-browsing environments throw on `localStorage.getItem` access. Fallback to matchMedia (defense-in-depth). |
| T-04-01-03 | Information Disclosure | PENDO_IDS leaves | accept | Leaves are kebab-case selectors (e.g., `'lists.new-task-button'`) — semantically neutral; reveal no PII or implementation secrets. Same risk profile as Phase 3 dashboard.* leaves. |
| T-04-01-04 | Spoofing | `<Notifications />` mount | accept | Notifications are application-internal; no untrusted input crosses this boundary in Phase 4. Future user-content notifications (Phase 5+) will need escaping. |
| T-04-01-05 | Elevation of Privilege | new deps' transitive tree | mitigate | `npm audit` reviewed in plan SUMMARY; any high-severity finding triggers version bump or work pause. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after all tasks.
- `npm run build` exits 0.
- `npm run dev` starts the server with no console errors.
- `grep -r "import.*from '@tanstack/react-table'" src/` returns 0 matches (no consumer yet — that's plan 04-02/03/05's job, not this plan's).
- `grep -r "import.*from '@mantine/notifications'" src/App.tsx` returns 1 match.
- `grep -c '_pendo' src/pendo/PENDO_IDS.ts` unchanged from before (no accidental rename).
</verification>

<success_criteria>
- Phase 4's three dependent plans (04-03, 04-04, 04-05) can begin without import errors or missing pendo IDs.
- The Settings Preferences theme toggle in plan 04-04 will actually apply when SegmentedControl onChange fires (App.tsx + index.html provide the auto/light/dark plumbing).
- All toast calls in plans 04-03 and 04-04 render visibly (Notifications mounted).
- TypeScript autocompletes new pendo leaves in any consumer (Leaves derivation picks them up).
- FND-07 provider stack ordering is preserved verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages-lists-settings-reports/04-01-SUMMARY.md` capturing:
- Pinned versions installed (`npm view` outputs).
- Any peer-dep warnings (especially against react@19).
- The exact ColorSchemeScript snippet shipped in index.html.
- `npm audit` summary for the four new deps.
</output>
