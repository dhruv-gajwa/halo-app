---
phase: 04-core-pages-lists-settings-reports
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/routes/app/reports/ReportsPage.tsx
  - src/reports/ReportsFiltersBar.tsx
  - src/reports/ReportsChart.tsx
  - src/reports/ReportsTable.tsx
  - src/reports/csvExport.ts
autonomous: true
requirements:
  - REP-01
  - REP-02
  - REP-03
  - REP-04
must_haves:
  truths:
    - "At /app/reports the user sees three filter controls — Date range (DatePickerInput type='range' from @mantine/dates) + Assignee Select + Status MultiSelect — above a Recharts stacked-bar chart and a read-only TanStack Table. Closes REP-01."
    - "The TanStack Table has 6 columns: Title / Status / Priority / Assignee / Due date / Completed at. No actions column (read-only). Default sort createdAt desc. Header click toggles per-column sort. Wrapped in <Paper withBorder p={0} radius='md'> + overflow hidden. Closes REP-02."
    - "The Recharts stacked-bar chart (BarChart + Bar stackId='status') renders 3 stacked bars per day across the selected date range with tick density per UI-SPEC (≤14d=daily, 15-31d=every 3d, 32-90d=every 10d). Theme-resolved colors via useMantineTheme() + useComputedColorScheme() — no hardcoded hex (D-18). Wrapped in <Paper withBorder p='md' radius='md'> with data-pendo-id on inner <div> (PEN-08 outer-wrapper rule). Closes REP-03."
    - "An Export CSV button (top-right of page header, Button variant='outline' + IconDownload, disabled when filteredTasks.length === 0) downloads halo-tasks-YYYY-MM-DD.csv with the 6 visible columns honoring active filters. RFC 4180 quoting (hand-rolled, no library). Closes REP-04."
    - "Filter defaults: Date range = [nowRef - 30d, nowRef] using computeNowRef from src/tasks/now-ref.ts (shared with Dashboard per D-22). Assignee = 'all'. Status = ['todo', 'in_progress', 'done'] (all three selected — equivalent to no filter)."
    - "Every interactive control carries a data-pendo-id from PENDO_IDS.reports.* — no hand-typed string literals (PEN-07)."
    - "D-01: The Reports table is built on TanStack Table v8 as the single table primitive (shared with Lists per 04-03)."
    - "D-19: Reports filters are exactly DatePickerInput type='range' + Assignee Select + Status MultiSelect; default date range is [nowRef - 30 days, nowRef]."
    - "D-20: Reports chart is a Recharts stacked BarChart with three stacked Bars whose colors resolve via the Mantine theme — indigo.3 for To do, indigo.6 (indigo.4 in dark) for In progress, gray.5 for Done — and data-pendo-id sits on the inner chart-wrapper div, not on <Bar>/<rect>."
    - "D-21: Reports TanStack table renders exactly 6 read-only columns (Title / Status / Priority / Assignee / Due date / Completed at) with default sort createdAt desc and NO actions column."
    - "D-23: CSV export is hand-rolled (no library) — RFC 4180 quoting on Title/Status/Priority/Assignee, dates as YYYY-MM-DD, filename halo-tasks-YYYY-MM-DD.csv, delivered via Blob + URL.createObjectURL + ephemeral <a download> click + URL.revokeObjectURL."
  artifacts:
    - path: "src/routes/app/reports/ReportsPage.tsx"
      provides: "Page composer: filter state + filtered tasks memo + chart + table + CSV export trigger"
      contains: "computeNowRef"
    - path: "src/reports/ReportsFiltersBar.tsx"
      provides: "DatePickerInput range + Assignee Select + Status MultiSelect"
      contains: "DatePickerInput"
    - path: "src/reports/ReportsChart.tsx"
      provides: "Recharts BarChart with three stacked Bars + theme-resolved colors"
      contains: "BarChart"
    - path: "src/reports/ReportsTable.tsx"
      provides: "TanStack Table v8 with 6 read-only columns"
      contains: "@tanstack/react-table"
    - path: "src/reports/csvExport.ts"
      provides: "Hand-rolled RFC 4180 CSV serializer + Blob download"
      contains: "URL.createObjectURL"
  key_links:
    - from: "src/routes/app/reports/ReportsPage.tsx"
      to: "src/tasks/now-ref.ts"
      via: "computeNowRef import for default date range anchor"
      pattern: "from .*tasks/now-ref"
    - from: "src/reports/ReportsChart.tsx"
      to: "@mantine/core useMantineTheme + useComputedColorScheme"
      via: "theme-resolved chart colors per D-18"
      pattern: "useMantineTheme|useComputedColorScheme"
    - from: "src/reports/csvExport.ts"
      to: "Blob + URL.createObjectURL"
      via: "client-side download sequence"
      pattern: "URL\\.createObjectURL"
    - from: "src/reports/ReportsFiltersBar.tsx Assignee select"
      to: "src/tasks/assigneeOptions.ts getAssigneeOptions"
      via: "shared helper with Lists"
      pattern: "getAssigneeOptions"
    - from: "src/routes/app/reports/ReportsPage.tsx filtered memo"
      to: "src/tasks/tasksRepo.ts listTasks"
      via: "read filtered set from repo"
      pattern: "listTasks"
---

<objective>
Replace the Phase 3 placeholder body of `src/routes/app/reports/ReportsPage.tsx` with the real Reports UI per the locked 04-UI-SPEC §"Reports page" contract: three filter controls (date range / assignee / status multi-select), a Recharts stacked-bar chart with theme-resolved colors, a read-only TanStack Table with 6 columns, and an Export CSV button that downloads the visible filtered data as a client-side Blob. Closes REP-01..04.

Purpose: Reports is the canonical "filter + visualize + export" Pendo demo surface — track events on filter changes, guides on the chart, and CSV export as a measurable conversion. Reuses the TanStack Table infrastructure from plan 04-03's Lists page (D-01) and the shared `computeNowRef` from plan 04-02 (D-22) so the Reports default date range stays anchored to the same demo-non-staleness anchor as the Dashboard.

Output: Rewritten `src/routes/app/reports/ReportsPage.tsx` + four new files under `src/reports/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages-lists-settings-reports/04-CONTEXT.md
@.planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md
@.planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md
@CLAUDE.md
@src/routes/app/reports/ReportsPage.tsx
@src/dashboard/Dashboard.tsx
@src/tasks/tasksRepo.ts
@src/tasks/labels.ts
@src/tasks/now-ref.ts
@src/tasks/assigneeOptions.ts
@src/pendo/PENDO_IDS.ts
@src/ui/primitives/index.ts

<interfaces>
<!-- From src/tasks/tasksRepo.ts -->
`listTasks(workspaceId): Task[]` — Phase 4 reads the unfiltered list and filters in-memory per the three filter dimensions.

<!-- From src/tasks/labels.ts (after plan 04-02) -->
`TASK_STATUS_LABELS`, `TASK_PRIORITY_LABELS`, `TASK_STATUS_BADGE_COLOR`, `TASK_PRIORITY_BADGE_COLOR`.

<!-- From src/tasks/now-ref.ts (after plan 04-02) -->
`computeNowRef(tasks: Task[]): Date`.

<!-- From src/tasks/assigneeOptions.ts (after plan 04-02) -->
`getAssigneeOptions(workspaceId, visitor): Array<{ value: string; label: string }>`.

<!-- From src/pendo/PENDO_IDS.ts reports namespace (after plan 04-01) -->
`PENDO_IDS.reports.filter.{dateRange, assignee, status}`
`PENDO_IDS.reports.chart.statusByDay`
`PENDO_IDS.reports.table.container`
`PENDO_IDS.reports.csvExport`

<!-- @mantine/dates -->
`DatePickerInput` with `type='range'` returns `[Date | null, Date | null]`. Requires `dayjs` peer (installed in plan 04-01).

<!-- recharts 3.8.1 -->
`<BarChart data><CartesianGrid /><XAxis /><YAxis /><Tooltip /><Legend /><Bar dataKey stackId fill /></BarChart>`. ResponsiveContainer for width.

<!-- @tanstack/react-table v8 -->
Same API surface as plan 04-03's TaskTable.

<!-- @mantine/core -->
`useMantineTheme()` returns the theme object. `useComputedColorScheme('light')` returns the active scheme. Mantine v9 color access: `theme.colors.indigo[3]`, `theme.colors.gray[5]`, etc.

<!-- dayjs -->
`dayjs(date).format('MMM D, YYYY')` for display. `dayjs(date).format('YYYY-MM-DD')` for CSV / filename. `dayjs(date).subtract(30, 'day').toDate()` for default range start.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Reports sub-components (filters bar, chart, table, CSV export utility)</name>
  <files>src/reports/ReportsFiltersBar.tsx, src/reports/ReportsChart.tsx, src/reports/ReportsTable.tsx, src/reports/csvExport.ts</files>
  <read_first>
    - src/dashboard/Dashboard.tsx (analog for Recharts Paper wrapper pattern lines 360-422 + SegmentedControl filter idiom + EmptyState; the BarChart code in Phase 3 uses AreaChart — Reports swaps to BarChart but the wrapping pattern is identical)
    - src/tasks/components/TaskTable.tsx (from plan 04-03 — TanStack column setup; Reports' ReportsTable copies the same skeleton with 6 columns and no Checkbox/Kebab)
    - src/tasks/labels.ts (after plan 04-02 — badge color maps)
    - src/tasks/now-ref.ts (after plan 04-02 — computeNowRef)
    - src/tasks/assigneeOptions.ts (after plan 04-02 — getAssigneeOptions)
    - src/pendo/PENDO_IDS.ts (after plan 04-01 — reports namespace)
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"Reports page" lines 634-770, §"CSV Export Contract" lines 773-812, §"Color > Dark mode color contract" lines 131-156
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/reports/ReportsFiltersBar.tsx" lines 681-689, §"src/reports/ReportsChart.tsx" lines 693-722, §"src/reports/ReportsTable.tsx" lines 726-730, §"src/reports/csvExport.ts" lines 734-743
  </read_first>
  <action>
    Create the four files under `src/reports/` (mkdir first). Use wrapped primitives where available (`Select`, `MultiSelect`, `DatePickerInput`, `Button`).

    ---

    **A) `src/reports/ReportsFiltersBar.tsx`**:

    Props:
    ```typescript
    type ReportsFiltersBarProps = {
      workspaceId: string
      visitor: Visitor
      dateRange: [Date | null, Date | null]
      assignee: string                          // 'all' or Assignee.id
      statusFilter: TaskStatus[]                // multi-select: subset of TaskStatusEnum.options
      onDateRangeChange: (next: [Date | null, Date | null]) => void
      onAssigneeChange: (next: string) => void
      onStatusChange: (next: TaskStatus[]) => void
    }
    ```

    Render `<Group gap="md" align="flex-end">` per UI-SPEC lines 653-681:
    ```tsx
    <DatePickerInput
      type="range"
      label="Date range"
      value={dateRange}
      onChange={onDateRangeChange}
      valueFormat="MMM D, YYYY"
      clearable
      pendoId={PENDO_IDS.reports.filter.dateRange}
    />
    <Select
      label="Assignee"
      value={assignee}
      onChange={(v) => onAssigneeChange(v ?? 'all')}
      data={[{ value: 'all', label: 'All' }, ...getAssigneeOptions(workspaceId, visitor)]}
      clearable={false}
      pendoId={PENDO_IDS.reports.filter.assignee}
      w={200}
    />
    <MultiSelect
      label="Status"
      value={statusFilter}
      onChange={(v) => onStatusChange(v as TaskStatus[])}
      data={[
        { value: 'todo',        label: TASK_STATUS_LABELS.todo },
        { value: 'in_progress', label: TASK_STATUS_LABELS.in_progress },
        { value: 'done',        label: TASK_STATUS_LABELS.done },
      ]}
      pendoId={PENDO_IDS.reports.filter.status}
      w={240}
    />
    ```

    Default values set by the parent (ReportsPage). This component is controlled-only.

    ---

    **B) `src/reports/ReportsChart.tsx`**:

    Props:
    ```typescript
    type ReportsChartProps = {
      tasks: Task[]                              // already filtered
      dateRange: [Date | null, Date | null]
    }
    ```

    Internal logic — compute day buckets across the date range:
    ```typescript
    const start = dateRange[0] ? dayjs(dateRange[0]) : dayjs(nowRef).subtract(30, 'day')
    const end = dateRange[1] ? dayjs(dateRange[1]) : dayjs(nowRef)
    const dayCount = end.diff(start, 'day') + 1
    const dayBuckets = Array.from({ length: dayCount }, (_, i) => {
      const day = start.add(i, 'day').format('YYYY-MM-DD')
      const dayTasks = tasks.filter((t) => dayjs(t.createdAt).format('YYYY-MM-DD') === day)
      return {
        date: dayjs(day).format('MMM D'),
        todo:        dayTasks.filter((t) => t.status === 'todo').length,
        in_progress: dayTasks.filter((t) => t.status === 'in_progress').length,
        done:        dayTasks.filter((t) => t.status === 'done').length,
      }
    })
    ```

    Tick density (D-20):
    ```typescript
    let tickStep = 1
    if (dayCount > 14 && dayCount <= 31) tickStep = 3
    else if (dayCount > 31) tickStep = 10
    const ticks = dayBuckets.filter((_, i) => i % tickStep === 0).map((b) => b.date)
    ```

    Theme-resolved colors (D-18, UI-SPEC lines 138-152):
    ```typescript
    const theme = useMantineTheme()
    const computed = useComputedColorScheme('light')
    const colorTodo       = theme.colors.indigo[3]
    const colorInProgress = computed === 'dark' ? theme.colors.indigo[4] : theme.colors.indigo[6]
    const colorDone       = theme.colors.gray[5]
    const colorGrid       = theme.colors.gray[2]
    ```

    Markup:
    ```tsx
    <Paper withBorder p="md" radius="md">
      <div data-pendo-id={PENDO_IDS.reports.chart.statusByDay}>
        <Text fw={600} mb="md">Tasks by status per day</Text>
        <ResponsiveContainer width="100%" height={280}>
          <BarChart data={dayBuckets} barCategoryGap="20%">
            <CartesianGrid strokeDasharray="3 3" stroke={colorGrid} vertical={false} />
            <XAxis dataKey="date" ticks={ticks} tick={{ fontSize: 14 }} />
            <YAxis allowDecimals={false} tick={{ fontSize: 14 }} />
            <Tooltip />
            <Legend wrapperStyle={{ fontSize: 14 }} />
            <Bar dataKey="todo"        stackId="status" name="To do"       fill={colorTodo} />
            <Bar dataKey="in_progress" stackId="status" name="In progress" fill={colorInProgress} />
            <Bar dataKey="done"        stackId="status" name="Done"        fill={colorDone} radius={[4, 4, 0, 0]} />
          </BarChart>
        </ResponsiveContainer>
      </div>
    </Paper>
    ```

    Add a leading code comment: `// data-pendo-id on the outer Paper-wrapping div, NEVER on Recharts <Bar> / <rect> SVG children (PEN-08 — mirrors Dashboard.tsx line 361).`

    ---

    **C) `src/reports/ReportsTable.tsx`**:

    Props:
    ```typescript
    type ReportsTableProps = { tasks: Task[] }     // already filtered + pre-sorted by createdAt desc
    ```

    Build TanStack Table v8 with 6 columns per UI-SPEC §"Reports TanStack Table" lines 744-756. Use the same column-helper pattern as plan 04-03's TaskTable.tsx but WITHOUT the leading Checkbox or trailing Actions columns:
    1. `helper.accessor('title', { header: 'Title', cell: (info) => <Text size="sm">{info.getValue()}</Text> })`
    2. `helper.accessor('status', { header: 'Status', cell: (info) => <Badge color={TASK_STATUS_BADGE_COLOR[info.getValue()]} variant="light" size="sm">{TASK_STATUS_LABELS[info.getValue()]}</Badge> })`
    3. `helper.accessor('priority', { header: 'Priority', cell: similar via priority maps })`
    4. `helper.accessor((row) => row.assignee?.name ?? '—', { id: 'assignee', header: 'Assignee', cell: (info) => <Text size="sm">{info.getValue()}</Text> })`
    5. `helper.accessor('dueDate', { header: 'Due date', cell: (info) => <Text size="sm">{info.getValue() ? dayjs(info.getValue() as string).format('MMM D, YYYY') : '—'}</Text> })`
    6. `helper.accessor('completedAt', { header: 'Completed at', cell: (info) => <Text size="sm">{info.getValue() ? dayjs(info.getValue() as string).format('MMM D, YYYY') : '—'}</Text> })`

    Default sort: `createdAt` desc — pre-sort the input `tasks` array before passing to TanStack; let TanStack take over once the user clicks a header.

    Column header click affordance: same single-arrow IconChevronUp / IconChevronDown size 14 + indigo.6 when sorted, identical to TaskTable.

    Wrapper: `<Paper withBorder p={0} radius="md" data-pendo-id={PENDO_IDS.reports.table.container} style={{ overflow: 'hidden' }}>` with Mantine `<Table>` inside.

    **Empty state (filters yield zero):** per UI-SPEC lines 761-768 — render inside the table body area:
    ```tsx
    {tasks.length === 0 && (
      <Center py="xl">
        <Text c="dimmed">No tasks match these filters.</Text>
      </Center>
    )}
    ```
    No "Clear filters" anchor in Reports empty state (filters are individually clearable; per UI-SPEC line 769).

    Cell padding: same CSS module approach as TaskTable (sm vertical, md horizontal) — share `Table.module.css` between Lists and Reports if straightforward; otherwise duplicate the module per package and document. Pick whichever is simpler.

    ---

    **D) `src/reports/csvExport.ts`**:

    Pure utility module — no React imports. Exports:
    ```typescript
    export function exportTasksToCsv(tasks: Task[]): void
    ```

    Implementation per UI-SPEC §"CSV Export Contract" lines 776-811 verbatim:
    ```typescript
    import dayjs from 'dayjs'
    import type { Task } from '../tasks/types'
    import { TASK_STATUS_LABELS, TASK_PRIORITY_LABELS } from '../tasks/labels'

    // RFC 4180 quoter: wrap in "..." iff value contains a comma, newline, or double-quote.
    // Escape internal double-quotes by doubling.
    function quote(value: string): string {
      if (value === '' || /[,\n"]/.test(value)) {
        return `"${value.replace(/"/g, '""')}"`
      }
      return value
    }

    function toCsv(rows: Task[]): string {
      const headers = ['Title', 'Status', 'Priority', 'Assignee', 'Due date', 'Completed at']
      const lines = rows.map((t) => [
        quote(t.title),
        quote(TASK_STATUS_LABELS[t.status]),
        quote(TASK_PRIORITY_LABELS[t.priority]),
        quote(t.assignee?.name ?? ''),
        t.dueDate     ? dayjs(t.dueDate).format('YYYY-MM-DD')     : '',
        t.completedAt ? dayjs(t.completedAt).format('YYYY-MM-DD') : '',
      ].join(','))
      return [headers.join(','), ...lines].join('\r\n')
    }

    export function exportTasksToCsv(tasks: Task[]): void {
      const csv = toCsv(tasks)
      const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' })
      const url = URL.createObjectURL(blob)
      const filename = `halo-tasks-${dayjs().format('YYYY-MM-DD')}.csv`
      const a = document.createElement('a')
      a.href = url
      a.download = filename
      a.style.display = 'none'
      document.body.appendChild(a)
      a.click()
      document.body.removeChild(a)
      URL.revokeObjectURL(url)
    }
    ```

    Notes:
    - Header row uses the display labels per UI-SPEC line 780.
    - Dates use `YYYY-MM-DD` (ISO) in the CSV body (machine-readable), distinct from the table's display format `MMM D, YYYY`.
    - Empty assignee shows as empty string (not `—`) in CSV per UI-SPEC line 786.
    - No external CSV library — hand-rolled RFC 4180.

    ---

    After all four files exist, run `npm run typecheck` and `npm run build` — both must exit 0.
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      test -f src/reports/ReportsFiltersBar.tsx && \
      test -f src/reports/ReportsChart.tsx && \
      test -f src/reports/ReportsTable.tsx && \
      test -f src/reports/csvExport.ts && \
      grep -c "from '@mantine/dates'\\|DatePickerInput" src/reports/ReportsFiltersBar.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "MultiSelect" src/reports/ReportsFiltersBar.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "getAssigneeOptions" src/reports/ReportsFiltersBar.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "PENDO_IDS.reports.filter" src/reports/ReportsFiltersBar.tsx | awk '$1 >= 3' | grep -q '.' && \
      grep -c "BarChart" src/reports/ReportsChart.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "useMantineTheme\\|useComputedColorScheme" src/reports/ReportsChart.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "stackId" src/reports/ReportsChart.tsx | awk '$1 >= 3' | grep -q '.' && \
      grep -c "PENDO_IDS.reports.chart.statusByDay" src/reports/ReportsChart.tsx | awk '$1 == 1' | grep -q '.' && \
      ! grep -E "fill=\"#[0-9a-fA-F]{3,6}\"" src/reports/ReportsChart.tsx && \
      grep -c "from '@tanstack/react-table'" src/reports/ReportsTable.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "createColumnHelper\\|useReactTable" src/reports/ReportsTable.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "PENDO_IDS.reports.table.container" src/reports/ReportsTable.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "TASK_STATUS_BADGE_COLOR" src/reports/ReportsTable.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "URL.createObjectURL" src/reports/csvExport.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "URL.revokeObjectURL" src/reports/csvExport.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "halo-tasks-" src/reports/csvExport.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "Title,Status,Priority,Assignee,Due date,Completed at" src/reports/csvExport.ts | awk '$1 == 1' | grep -q '.' && \
      ! grep -rE "data-pendo-id=\"[a-z][^\"]*\"" src/reports/ && \
      npm run typecheck && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - **ReportsFiltersBar.tsx:** Renders `<Group gap="md" align="flex-end">` with three controls. DatePickerInput type='range' + valueFormat='MMM D, YYYY' + clearable. Assignee Select with `[All, ...getAssigneeOptions(...)]`, clearable={false}, w=200. MultiSelect Status with three options + w=240. All three pendoIds from `PENDO_IDS.reports.filter.*`.
    - **ReportsChart.tsx:** Imports `useMantineTheme`, `useComputedColorScheme` from `@mantine/core`. Renders `<BarChart>` with three `<Bar stackId="status">` (todo, in_progress, done). Day buckets computed from filtered tasks + dateRange. Tick density per UI-SPEC: ≤14 days → daily, 15-31 → every 3, >31 → every 10. NO hardcoded hex colors — every color comes from `theme.colors.<token>[<shade>]`. In dark scheme, in_progress uses `indigo[4]` (better contrast). Wrapped in `<Paper withBorder p="md" radius="md">` with `data-pendo-id={PENDO_IDS.reports.chart.statusByDay}` on the inner `<div>` (PEN-08 rule — never on `<Bar>` / `<rect>`). `grep -c 'fill="#' src/reports/ReportsChart.tsx` returns 0.
    - **ReportsTable.tsx:** TanStack Table v8 with 6 columns (Title / Status / Priority / Assignee / Due date / Completed at). NO actions column. NO leading checkbox. Default sort createdAt desc applied via pre-sort. Header click toggles per-column sort with single-arrow affordance. Wrapper `<Paper withBorder p={0} radius="md" data-pendo-id={PENDO_IDS.reports.table.container}>` with overflow hidden. Date cells format via `dayjs(...).format('MMM D, YYYY')` with `'—'` fallback. Renders inline empty state `<Center py="xl"><Text c="dimmed">No tasks match these filters.</Text></Center>` when tasks.length === 0 (no clear-filters anchor per UI-SPEC line 769).
    - **csvExport.ts:** Pure module exporting `exportTasksToCsv(tasks: Task[]): void`. Internal RFC 4180 `quote()` helper handles `,`, `\n`, `"` (replace `"` → `""` inside quoted fields). Header row exactly `Title,Status,Priority,Assignee,Due date,Completed at`. Body row formatting: title/status/priority/assignee.name via `quote()`, dueDate/completedAt as `YYYY-MM-DD` or empty string. Line separator `\r\n`. Blob with `type: 'text/csv;charset=utf-8'`. Filename `halo-tasks-YYYY-MM-DD.csv` where YYYY-MM-DD = `dayjs().format('YYYY-MM-DD')`. Sequence: createObjectURL → ephemeral `<a download>` → click → removeChild → revokeObjectURL.
    - **No hand-typed pendo strings:** `grep -rE "data-pendo-id=\"[a-z][^\"]*\"" src/reports/` returns 0 matches.
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Four self-contained sub-components ship. Filter bar wires three controls with shared assignee-options helper. Chart resolves colors via Mantine theme (dark-mode safe). Table runs TanStack v8 with 6 read-only columns + sort. CSV export hand-rolls RFC 4180 and triggers a Blob download. Typecheck + build pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose ReportsPage and verify end-to-end filter+chart+table+CSV flow</name>
  <files>src/routes/app/reports/ReportsPage.tsx</files>
  <read_first>
    - src/routes/app/reports/ReportsPage.tsx (current Phase 3 placeholder — body replaced)
    - All four sub-components from Task 1
    - src/dashboard/Dashboard.tsx (analog for page composer — workspaceId selector + useMemo over listTasks)
    - src/tasks/now-ref.ts (after plan 04-02 — computeNowRef for default date range)
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"Reports page" lines 634-697 (page structure verbatim + filter defaults)
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/routes/app/reports/ReportsPage.tsx" lines 798-800
  </read_first>
  <action>
    Replace the existing Phase 3 placeholder body of `src/routes/app/reports/ReportsPage.tsx` with the composer. Preserve the exported function name (referenced by the router — DO NOT edit the router).

    Imports: `useState`, `useMemo` from React; `useAuthStore` from `@/auth/authStore`; `listTasks` from `@/tasks/tasksRepo`; `computeNowRef` from `@/tasks/now-ref`; `PENDO_IDS` from `@/pendo/PENDO_IDS`; `Stack`, `Group`, `Title` from `@mantine/core`; `Button` from `@/ui/primitives`; `IconDownload` from `@tabler/icons-react`; `dayjs`; the four sub-components.

    State surface:
    ```typescript
    const visitor = useAuthStore((s) => s.currentVisitor)
    const workspaceId = useAuthStore((s) => s.currentWorkspace?.id)

    const allTasks = useMemo(() => (workspaceId ? listTasks(workspaceId) : []), [workspaceId])
    const nowRef = useMemo(() => computeNowRef(allTasks), [allTasks])

    const [dateRange, setDateRange] = useState<[Date | null, Date | null]>([
      dayjs(nowRef).subtract(30, 'day').toDate(),
      dayjs(nowRef).toDate(),
    ])
    const [assignee, setAssignee] = useState<string>('all')
    const [statusFilter, setStatusFilter] = useState<TaskStatus[]>(['todo', 'in_progress', 'done'])

    const filteredTasks = useMemo(() => {
      return allTasks.filter((t) => {
        // Date range — filter on createdAt (the chart and table both anchor to createdAt buckets)
        if (dateRange[0] && dayjs(t.createdAt).isBefore(dayjs(dateRange[0]).startOf('day'))) return false
        if (dateRange[1] && dayjs(t.createdAt).isAfter(dayjs(dateRange[1]).endOf('day'))) return false
        // Assignee
        if (assignee !== 'all' && t.assignee?.id !== assignee) return false
        // Status (multi-select)
        if (statusFilter.length === 0) return false // no statuses selected = no tasks
        if (!statusFilter.includes(t.status)) return false
        return true
      }).sort((a, b) => dayjs(b.createdAt).valueOf() - dayjs(a.createdAt).valueOf()) // default sort createdAt desc
    }, [allTasks, dateRange, assignee, statusFilter])
    ```

    Defensive narrowing: `if (!workspaceId || !visitor) return <></>` (Dashboard.tsx precedent).

    Page body per UI-SPEC lines 638-697:
    ```tsx
    return (
      <Stack gap="lg">
        <Group justify="space-between" align="center">
          <Title order={3}>Reports</Title>
          <Button
            variant="outline"
            leftSection={<IconDownload size={16} />}
            pendoId={PENDO_IDS.reports.csvExport}
            onClick={() => exportTasksToCsv(filteredTasks)}
            disabled={filteredTasks.length === 0}
          >
            Export CSV
          </Button>
        </Group>

        <ReportsFiltersBar
          workspaceId={workspaceId}
          visitor={visitor}
          dateRange={dateRange}
          assignee={assignee}
          statusFilter={statusFilter}
          onDateRangeChange={setDateRange}
          onAssigneeChange={setAssignee}
          onStatusChange={setStatusFilter}
        />

        <ReportsChart tasks={filteredTasks} dateRange={dateRange} />

        <ReportsTable tasks={filteredTasks} />
      </Stack>
    )
    ```

    After implementation, run typecheck + build + dev server. Manual smoke (record in SUMMARY):
    1. Sign in, navigate to `/app/reports` → page renders with default filters (Date range = nowRef-30d to nowRef, Assignee = All, Status = all three).
    2. Chart shows stacked bars across the 30-day window with reasonable tick density (daily ticks at this density).
    3. Table renders 6 columns with read-only rows; click "Title" header → ascending sort; click again → descending; click third time → back to createdAt desc default.
    4. Narrow Date range to a 7-day window → chart re-buckets to daily ticks, table rows narrow.
    5. Select Assignee = current visitor → table narrows to that assignee's tasks only.
    6. Deselect all three Status options → table empties + shows compact "No tasks match these filters" state; chart shows empty buckets (3 stacks at zero across all days). Export CSV button becomes disabled.
    7. Click Export CSV → file `halo-tasks-2026-05-15.csv` (or current date) downloads; open it → headers `Title,Status,Priority,Assignee,Due date,Completed at`; rows match the visible table; titles with commas or quotes are properly quoted; dates are `YYYY-MM-DD`.
    8. **Dark mode regression check (after plan 04-04 ships):** Toggle to Dark in Settings → Reports chart bars use `indigo[4]` for in-progress; table + filter bar recolor cleanly. No hardcoded hex bleed-through.
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      grep -c "from '../../../tasks/now-ref'\\|from '@/tasks/now-ref'" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "computeNowRef" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "ReportsFiltersBar" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "ReportsChart" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "ReportsTable" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 2' | grep -q '.' && \
      grep -c "exportTasksToCsv" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "PENDO_IDS.reports.csvExport" src/routes/app/reports/ReportsPage.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "filteredTasks.length === 0" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "dayjs" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      grep -c "listTasks" src/routes/app/reports/ReportsPage.tsx | awk '$1 >= 1' | grep -q '.' && \
      npm run typecheck && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/app/reports/ReportsPage.tsx` exports the same function name as before (no router break).
    - Imports `computeNowRef` from `src/tasks/now-ref.ts` (NOT a local re-implementation).
    - State surface: useState for `dateRange: [Date | null, Date | null]`, `assignee: string`, `statusFilter: TaskStatus[]`.
    - Default dateRange = `[dayjs(nowRef).subtract(30, 'day').toDate(), dayjs(nowRef).toDate()]`.
    - Default assignee = `'all'`.
    - Default statusFilter = `['todo', 'in_progress', 'done']`.
    - `filteredTasks` useMemo applies all three filter predicates AND pre-sorts by createdAt desc.
    - Page header `<Group justify="space-between">` with Title left + "Export CSV" Button right.
    - Export CSV button: variant="outline", leftSection IconDownload, `pendoId={PENDO_IDS.reports.csvExport}`, `disabled={filteredTasks.length === 0}`, onClick calls `exportTasksToCsv(filteredTasks)`.
    - Composes ReportsFiltersBar + ReportsChart + ReportsTable in a `<Stack gap="lg">`.
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
    - **All 8 manual smoke scenarios pass (record results in SUMMARY).** Specifically: REP-01 (three filter dimensions). REP-02 (table with 6 columns + sort). REP-03 (stacked bar chart + tick density + theme colors). REP-04 (CSV download with correct headers + body + filename + RFC 4180 quoting).
    - **CSV correctness audit:** Generated CSV passes a CSV parser smoke (e.g., open in Excel / Numbers / Google Sheets) — Title with embedded commas displays as a single cell; dates parse as dates.
    - **Pendo target audit:** `document.querySelectorAll('[data-pendo-id^="reports."]').length` ≥ 5 on a non-empty Reports page (csvExport + 3 filter controls + chart + table). Record exact count in SUMMARY.
    - **Dark mode no-leak audit:** `grep -rE 'fill="#[0-9a-fA-F]{3,6}"' src/reports/ src/routes/app/reports/` returns 0 matches.
  </acceptance_criteria>
  <done>
    `/app/reports` renders the real Reports UI: filter bar, stacked bar chart, read-only table, CSV export button. All four REP requirements satisfied: REP-01 (date range + assignee + status filters), REP-02 (TanStack table with 6 columns), REP-03 (SVG bar chart from filtered data), REP-04 (CSV export). Typecheck + build pass. Manual smoke recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReportsFiltersBar onChange → ReportsPage state | User-typed filter values control the visible data subset |
| Export CSV onClick → Blob download | User-triggered download bypasses normal HTTP — sandboxed via Blob URL |
| Recharts SVG → DOM | Chart renders as inline SVG; Pendo can target the wrapper but not individual <rect> per PEN-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | CSV cell content | mitigate | RFC 4180 quoter handles `,`, `\n`, `"` correctly. Double-quotes inside fields are doubled. Field with leading `=` or `+` could still be a CSV injection target in Excel — accept per scope (demo data, no executable injection risk; if Phase 5 adds polish, prefix risky cells with `'`). |
| T-04-05-02 | Information Disclosure | CSV exposes raw task data | accept | All Halo data is fake (faker-seeded). No PII. CSV is a deliberate user action; no covert channel. |
| T-04-05-03 | Denial of Service | Date range spanning thousands of days | mitigate | Tick density logic caps visible tick count at `dayCount / 10` for >31 days; below that, daily/every-3-days density. Chart degrades gracefully (Recharts handles 365+ buckets). Filter set is in-memory over ≤60 seeded rows. |
| T-04-05-04 | Tampering | Recharts chart color injection | mitigate | All color values resolve from `theme.colors.<token>[<shade>]` — no string interpolation of untrusted values. |
| T-04-05-05 | Information Disclosure | data-pendo-id leaves | accept | Same risk profile as Phase 3 dashboard.* leaves — semantically neutral. |
| T-04-05-06 | Denial of Service | Blob URL leak | mitigate | `URL.revokeObjectURL(url)` called after the `<a download>` click — Blob is GC-eligible immediately. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after both tasks.
- `npm run build` exits 0.
- All 4 REP-* requirements have a corresponding code path:
  - REP-01: ReportsFiltersBar with three controls + ReportsPage state.
  - REP-02: ReportsTable with 6 TanStack columns.
  - REP-03: ReportsChart with stacked BarChart.
  - REP-04: csvExport.ts + ReportsPage Export CSV button.
- `grep -rE "data-pendo-id=\"[a-z]" src/reports/ src/routes/app/reports/ReportsPage.tsx` returns 0 matches.
- `grep -rE 'fill="#[0-9a-fA-F]{3,6}"' src/reports/` returns 0 matches (no hardcoded chart colors).
- All chart colors flow through `useMantineTheme().colors.<token>[<shade>]`.
- `computeNowRef` is imported from `src/tasks/now-ref.ts` (NOT redefined locally), per the D-22 shared-module contract.
</verification>

<success_criteria>
- A user can filter task data three ways (date / assignee / status), see a stacked-bar visualization, inspect a sortable table, and export to CSV — all in one cohesive page.
- The page satisfies the locked UI design contract in 04-UI-SPEC.md verbatim (filter defaults, chart spec, table column list, CSV format).
- Pendo guides can target every Reports interactive control via stable `data-pendo-id` from the registry.
- Reports page renders cleanly in both light and dark schemes; chart bars meet contrast requirements via the `indigo[4]` dark-mode swap.
- CSV downloads work cross-browser (Chrome, Firefox, Safari) — Blob + URL.createObjectURL is universal.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages-lists-settings-reports/04-05-SUMMARY.md` capturing:
- The 8 manual smoke test results.
- Exact pendoId selector count on a non-empty Reports page.
- A note on CSV cross-browser smoke (Chrome at minimum; ideally Firefox + Safari).
- A note on chart dark-mode contrast (verify indigo[4] for in_progress reads cleanly on dark surface).
- Any deviations from UI-SPEC and why.
</output>
