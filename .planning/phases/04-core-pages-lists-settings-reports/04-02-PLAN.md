---
phase: 04-core-pages-lists-settings-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/authRepo.ts
  - src/tasks/tasksRepo.ts
  - src/tasks/schemas.ts
  - src/tasks/types.ts
  - src/tasks/labels.ts
  - src/tasks/now-ref.ts
  - src/tasks/assigneeOptions.ts
  - src/tasks/index.ts
  - src/dashboard/Dashboard.tsx
  - src/ui/primitives/Checkbox.tsx
  - src/ui/primitives/Textarea.tsx
  - src/ui/primitives/DatePickerInput.tsx
  - src/ui/primitives/index.ts
autonomous: true
requirements:
  - LIST-04
  - LIST-09
  - SET-02
  - SET-03
must_haves:
  truths:
    - "tasksRepo.updateTask owns the completedAt invariant: status→done stamps completedAt with new Date().toISOString(); status moving OFF done sets completedAt to null. UI code never thinks about completedAt (D-09)."
    - "tasksRepo.createTask stamps completedAt when input.status === 'done' (symmetry with updateTask)."
    - "authRepo.updateVisitor(id, patch) and authRepo.updateWorkspace(id, patch) exist with the same shape as tasksRepo.updateTask: read array via readWithSchema → find index → spread patch → writeJSON → return updated record or undefined when not found."
    - "updateVisitor's patch type structurally excludes id, passwordHash, createdAt (D-15 defense — Visitor.passwordHash cannot leak through Settings UI plumbing)."
    - "updateWorkspace's patch type structurally excludes id, ownerVisitorId, createdAt."
    - "TaskFormSchema is exported from src/tasks/schemas.ts with the 6 user-editable fields (title, description, status, priority, assignee, dueDate). TaskFormValues type derived via z.infer is exported from src/tasks/types.ts."
    - "TASK_STATUS_BADGE_COLOR and TASK_PRIORITY_BADGE_COLOR are exported from src/tasks/labels.ts as Record<TaskStatus, string> and Record<TaskPriority, string> respectively, with values matching the Phase 3 donut color story (D-02, UI-SPEC color table)."
    - "computeNowRef is extracted from src/dashboard/Dashboard.tsx into src/tasks/now-ref.ts as a pure exported function; Dashboard imports from the new module instead of inlining."
    - "getAssigneeOptions(workspaceId, visitor) exists in src/tasks/assigneeOptions.ts returning Array<{ value: string; label: string }> sorted by name asc, deduped by Assignee.id, always including the current visitor (D-10)."
    - "Three new primitive wrappers exist with the contract: pendoId: PendoId required, forwarded as data-pendo-id to the DOM. Checkbox additionally accepts taskId?: string forwarded as data-pendo-task-id."
    - "Existing primitives barrel re-exports the three new wrappers (Checkbox, Textarea, DatePickerInput) and their props types."
    - "D-26: Phase 4 does NOT bump SCHEMA_VERSION on these repo extensions (tasksRepo completedAt invariant, authRepo.updateVisitor/updateWorkspace) — the stored Visitor/Workspace/Task shapes are unchanged."
  artifacts:
    - path: "src/tasks/tasksRepo.ts"
      provides: "updateTask with completedAt invariant; createTask stamps completedAt on status==='done' input"
      contains: "completedAt"
    - path: "src/auth/authRepo.ts"
      provides: "updateVisitor + updateWorkspace functions"
      contains: "updateVisitor"
    - path: "src/tasks/schemas.ts"
      provides: "TaskFormSchema export"
      contains: "TaskFormSchema"
    - path: "src/tasks/types.ts"
      provides: "TaskFormValues type"
      contains: "TaskFormValues"
    - path: "src/tasks/labels.ts"
      provides: "TASK_STATUS_BADGE_COLOR + TASK_PRIORITY_BADGE_COLOR maps"
      contains: "TASK_STATUS_BADGE_COLOR"
    - path: "src/tasks/now-ref.ts"
      provides: "computeNowRef pure function extracted from Dashboard"
      contains: "computeNowRef"
    - path: "src/tasks/assigneeOptions.ts"
      provides: "getAssigneeOptions helper"
      contains: "getAssigneeOptions"
    - path: "src/ui/primitives/Checkbox.tsx"
      provides: "wrapped Mantine Checkbox forwarding pendoId + taskId"
      contains: "data-pendo-id"
    - path: "src/ui/primitives/Textarea.tsx"
      provides: "wrapped Mantine Textarea forwarding pendoId"
      contains: "data-pendo-id"
    - path: "src/ui/primitives/DatePickerInput.tsx"
      provides: "wrapped @mantine/dates DatePickerInput forwarding pendoId"
      contains: "@mantine/dates"
    - path: "src/ui/primitives/index.ts"
      provides: "Re-exports new wrappers + props types"
      contains: "Checkbox"
  key_links:
    - from: "src/tasks/tasksRepo.ts updateTask"
      to: "src/tasks/tasksRepo.ts writeJSON via K.tasks(workspaceId)"
      via: "FND-04 codec write path"
      pattern: "writeJSON\\(K\\.tasks"
    - from: "src/dashboard/Dashboard.tsx"
      to: "src/tasks/now-ref.ts"
      via: "import { computeNowRef } from '../tasks/now-ref'"
      pattern: "from '../tasks/now-ref'"
    - from: "src/tasks/assigneeOptions.ts"
      to: "src/tasks/tasksRepo.ts listTasks"
      via: "imports listTasks for assignee dedup"
      pattern: "from './tasksRepo'"
    - from: "src/ui/primitives/DatePickerInput.tsx"
      to: "@mantine/dates"
      via: "imports DatePickerInput from @mantine/dates"
      pattern: "from '@mantine/dates'"
---

<objective>
Wave-1 foundation plan that extends the task and auth repos with the four Phase 4 behavioral additions (`updateTask` completedAt invariant, `createTask` symmetry, `authRepo.updateVisitor`, `authRepo.updateWorkspace`), adds the `TaskFormSchema` co-located with `TaskSchema`, adds the badge-color maps to `src/tasks/labels.ts`, extracts `computeNowRef` from `Dashboard.tsx` into the shared `src/tasks/now-ref.ts` module (D-22), creates the `getAssigneeOptions` helper (D-10), and adds three new primitive wrappers (`Checkbox`, `Textarea`, `DatePickerInput`) to `src/ui/primitives/` with the existing pendoId-forwarding contract.

Purpose: All three page plans (04-03 Lists, 04-04 Settings, 04-05 Reports) consume one or more of these outputs. Without it: Lists checkbox toggle would not maintain `completedAt`, Settings save handlers would have no repo functions to call, Lists/Reports filter bars would have no shared assignee-options helper, and there would be no wrapped primitives for Checkbox / Textarea / DatePickerInput (forcing hand-typed `data-pendo-id` strings, violating CONVENTIONS.md §1).

Output: Extended `src/tasks/tasksRepo.ts`, `src/auth/authRepo.ts`, `src/tasks/schemas.ts`, `src/tasks/types.ts`, `src/tasks/labels.ts`. New files: `src/tasks/now-ref.ts`, `src/tasks/assigneeOptions.ts`, `src/ui/primitives/{Checkbox,Textarea,DatePickerInput}.tsx`. Updated `src/dashboard/Dashboard.tsx` (import-only refactor — no behavior change), `src/tasks/index.ts`, `src/ui/primitives/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages-lists-settings-reports/04-CONTEXT.md
@.planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md
@.planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md
@CLAUDE.md
@src/auth/authRepo.ts
@src/tasks/tasksRepo.ts
@src/tasks/schemas.ts
@src/tasks/types.ts
@src/tasks/labels.ts
@src/dashboard/Dashboard.tsx
@src/ui/primitives/index.ts
@src/ui/primitives/TextInput.tsx
@src/ui/primitives/PasswordInput.tsx
@src/ui/primitives/Select.tsx
@src/auth/types.ts
@src/auth/schemas.ts
@src/auth/authStore.ts

<interfaces>
<!-- From src/tasks/types.ts -->
`Task`, `TaskStatus`, `TaskPriority`, `Assignee` types. `TaskStatus = 'todo' | 'in_progress' | 'done'`. `Assignee = { id: string; name: string; avatar?: string | undefined }`.

<!-- From src/tasks/schemas.ts (existing) -->
`TaskSchema`, `TasksArraySchema`, `TaskStatusEnum`, `TaskPriorityEnum`, `AssigneeSchema`. The Task shape includes `id`, `title`, `description`, `status`, `priority`, `assignee`, `dueDate: string | null`, `completedAt: string | null`, `createdAt`, `updatedAt`.

<!-- From src/tasks/tasksRepo.ts (existing) -->
- `listTasks(workspaceId: string): Task[]` — reads via `readWithSchema(K.tasks(workspaceId), TasksArraySchema, [])`.
- `getTaskById(workspaceId: string, id: string): Task | undefined`.
- `createTask(workspaceId: string, input: CreateTaskInput): Task` — generates id/createdAt/updatedAt internally.
- `updateTask(workspaceId: string, id: string, patch: Partial<Omit<Task, 'id' | 'createdAt'>>): Task | undefined` — current shape (lines 70-83 of original file) — needs the completedAt invariant added.
- `deleteTask(workspaceId: string, id: string): boolean`.

<!-- From src/auth/authRepo.ts (existing) -->
- `listVisitors(): Visitor[]` reads via `readWithSchema(K.visitors(), VisitorsArraySchema, [])`.
- `listWorkspaces(): Workspace[]` reads via `readWithSchema(K.workspaces(), WorkspacesArraySchema, [])`.
- `createVisitor`, `createWorkspace` exist. NO updateVisitor / updateWorkspace yet — Phase 4 adds them after createWorkspace.

<!-- From src/auth/types.ts -->
`Visitor` has fields: id, email, firstName, lastName, username, jobTitle, role, yearsExperience, location, passwordHash, createdAt. (No updatedAt.)
`Workspace` has fields: id, ownerVisitorId, companyName, companySize, industry, planTier, createdAt.

<!-- From src/storage/keys.ts -->
`K.visitors()`, `K.workspaces()`, `K.tasks(workspaceId: string)`, `K.signupDraft()`, `K.meta()`.

<!-- From src/storage/codec.ts -->
`readWithSchema(key, schema, fallback)`, `writeJSON(key, value)`, `removeKey(key)`.

<!-- Existing primitive wrapper shape (TextInput.tsx) -->
```typescript
import { TextInput as MantineTextInput, type TextInputProps as MantineTextInputProps } from '@mantine/core'
import type { PendoId } from '../../pendo/PENDO_IDS'

export type TextInputProps = MantineTextInputProps & { pendoId: PendoId }

export function TextInput({ pendoId, ...rest }: TextInputProps) {
  return <MantineTextInput data-pendo-id={pendoId} {...rest} />
}
```
PasswordInput extends this shape with an additional `.pendo-sr-ignore` className merge. Phase 4 Checkbox follows the multi-prop forwarding precedent of PasswordInput (adds `taskId` forwarded as `data-pendo-task-id`).

<!-- From src/dashboard/Dashboard.tsx (lines ~58-75) -->
`computeNowRef(tasks: Task[]): Date` — currently inlined as a non-exported function inside Dashboard.tsx. Phase 4 extracts it verbatim into `src/tasks/now-ref.ts` (D-22) and updates Dashboard's body to import from there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo extensions (tasksRepo invariant + authRepo updateVisitor/Workspace) + schemas + labels</name>
  <files>src/tasks/tasksRepo.ts, src/tasks/schemas.ts, src/tasks/types.ts, src/tasks/labels.ts, src/auth/authRepo.ts</files>
  <read_first>
    - src/tasks/tasksRepo.ts (current updateTask + createTask implementations — lines that own the spread + writeJSON pattern)
    - src/tasks/schemas.ts (current TaskSchema + TaskStatusEnum + TaskPriorityEnum + AssigneeSchema — co-locating TaskFormSchema)
    - src/tasks/types.ts (current Task / Assignee type exports — adding TaskFormValues z.infer)
    - src/tasks/labels.ts (current TASK_STATUS_LABELS + TASK_PRIORITY_LABELS — appending TASK_STATUS_BADGE_COLOR + TASK_PRIORITY_BADGE_COLOR)
    - src/auth/authRepo.ts (current createVisitor + createWorkspace + list readers — pattern for new updateVisitor/updateWorkspace)
    - src/auth/types.ts (Visitor + Workspace field lists — for Omit<> in update patch types)
    - src/auth/schemas.ts (VisitorSchema + WorkspaceSchema — for downstream Settings tabs' .pick() usage, no edit needed here)
    - .planning/phases/04-core-pages-lists-settings-reports/04-CONTEXT.md D-09 (completedAt invariant), D-15 (authRepo extensions immutable fields), D-08 (TaskFormSchema fields), D-02 (status/priority badge colors)
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/tasks/tasksRepo.ts (extend updateTask)" lines 197-225, §"src/auth/authRepo.ts (add updateVisitor + updateWorkspace)" lines 227-265, §"src/tasks/schemas.ts (add TaskFormSchema)" lines 269-291, §"src/tasks/labels.ts (add badge color maps)" lines 294-314
    - .planning/phases/04-core-pages-lists-settings-reports/04-UI-SPEC.md §"Inline Validation Errors (Task Form)" lines 856-863 for exact Zod error messages, §"Color" badge rows lines 109-115 for badge color tokens
  </read_first>
  <action>
    1) **tasksRepo `updateTask` completedAt invariant (D-09):** In `src/tasks/tasksRepo.ts updateTask`, BEFORE constructing the `updated` record, compute the completedAt patch addition. Logic:
       - Let `existing = listTasks(workspaceId)`, `idx = existing.findIndex(...)`, return `undefined` if not found (current behavior preserved).
       - Let `prevStatus = existing[idx].status`.
       - If `patch.status !== undefined`:
         - If `patch.status === 'done' && prevStatus !== 'done'`: set `patch.completedAt = new Date().toISOString()`.
         - If `patch.status !== 'done' && prevStatus === 'done'`: set `patch.completedAt = null`.
       - Construct `updated = { ...existing[idx], ...patch, updatedAt: new Date().toISOString() }`.
       - Mutate patch locally (do not call mutate on caller's object — clone if linter flags this; the cleanest path is `const stamped: Partial<Omit<Task, 'id' | 'createdAt'>> = { ...patch }` then mutate `stamped`, then `const updated = { ...existing[idx], ...stamped, updatedAt: ... }`).
       Document the invariant with a leading code comment referencing D-09.

    2) **tasksRepo `createTask` symmetry:** In `createTask`, when `input.status === 'done'` and `input.completedAt == null`, stamp `completedAt = new Date().toISOString()`. Leave the rest of createTask's id/createdAt/updatedAt generation unchanged.

    3) **authRepo extensions (D-15):** Append after the existing `createWorkspace` in `src/auth/authRepo.ts`:
       ```
       export function updateVisitor(
         id: string,
         patch: Partial<Omit<Visitor, 'id' | 'passwordHash' | 'createdAt'>>,
       ): Visitor | undefined
       ```
       Implementation: read existing array via the existing private `listVisitors` (or inline `readWithSchema(K.visitors(), VisitorsArraySchema, [])` mirroring the existing pattern), find by id, return undefined if not found, spread `{ ...existing[idx], ...patch }` (no updatedAt — Visitor has no updatedAt field), splice into a new array, `writeJSON(K.visitors(), next)`, return updated.

       Append identically:
       ```
       export function updateWorkspace(
         id: string,
         patch: Partial<Omit<Workspace, 'id' | 'ownerVisitorId' | 'createdAt'>>,
       ): Workspace | undefined
       ```
       Same shape but against `K.workspaces()` / `WorkspacesArraySchema`.

       The `Omit` type on the patch arg is load-bearing — it makes TypeScript flag any UI plumbing that accidentally tries to pass `passwordHash` or `ownerVisitorId` (T-04-02-01 below). Document this in a leading JSDoc comment on each function.

       Add a top-of-function comment on `updateVisitor`: "Defense per D-15: passwordHash is omitted from the patch type so Settings UI cannot accidentally expose it. The crypto.subtle.digest hash is owned exclusively by createVisitor (Phase 2 D-09)."

    4) **TaskFormSchema (D-08, UI-SPEC validation table):** In `src/tasks/schemas.ts`, after `TaskSchema`, add:
       ```
       export const TaskFormSchema = z.object({
         title: z.string().min(1, 'Enter a task title.'),
         description: z.string(),
         status: TaskStatusEnum, // existing enum, error messages already attached or attach { message: 'Pick a status.' }
         priority: TaskPriorityEnum, // with { message: 'Pick a priority.' }
         assignee: AssigneeSchema,
         dueDate: z.iso.datetime().nullable(),
       })
       ```
       Use the exact error copy from UI-SPEC §"Inline Validation Errors (Task Form)" lines 856-863. For TaskStatusEnum / TaskPriorityEnum, wrap with `.refine` or pass `{ message: 'Pick a status.' }` to `z.enum([...], { message: '...' })` if those enums are constructed with a message slot per the Phase 2 idiom (look at the existing TaskStatusEnum definition to determine whether to wrap or to attach message inline — pick the path that matches the existing schemas.ts conventions; if the enum already has a message, leave it; otherwise wrap in `TaskStatusEnum.refine` or define a parallel `TaskStatusFormEnum`).

       Co-locate alongside `TaskSchema` in the same file — DO NOT create a new schemas file.

       In `src/tasks/types.ts`, append `export type TaskFormValues = z.infer<typeof TaskFormSchema>` (and add the import `import type { TaskFormSchema } from './schemas'` if not present; or use `import { TaskFormSchema } from './schemas'` with `z.infer` if `z` is already imported there — match the existing pattern at lines 32 of `src/auth/types.ts`).

    5) **Badge color maps (D-02, UI-SPEC color table):** Append to `src/tasks/labels.ts`:
       ```
       export const TASK_STATUS_BADGE_COLOR: Record<TaskStatus, string> = {
         todo: 'indigo.3',
         in_progress: 'indigo.6',
         done: 'gray.5',
       }
       export const TASK_PRIORITY_BADGE_COLOR: Record<TaskPriority, string> = {
         low: 'gray.5',
         medium: 'yellow.5',
         high: 'orange.5',
         urgent: 'red.6',
       }
       ```
       The `Record<TaskStatus, string>` and `Record<TaskPriority, string>` annotations enforce exhaustiveness — if a new status/priority is added to the enums, TypeScript fails compilation. Match the existing TASK_STATUS_LABELS annotation style in the same file.

    6) Run `npm run typecheck` after each edit; final pass must exit 0.
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      grep -E "completedAt.*new Date\(\)\.toISOString\(\)" src/tasks/tasksRepo.ts | grep -v '^#' | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "completedAt.*=.*null" src/tasks/tasksRepo.ts | grep -v '^#' | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "export function updateVisitor" src/auth/authRepo.ts | grep -c . | awk '$1 == 1' | grep -q '.' && \
      grep -E "export function updateWorkspace" src/auth/authRepo.ts | grep -c . | awk '$1 == 1' | grep -q '.' && \
      grep -E "Omit<Visitor.*passwordHash" src/auth/authRepo.ts | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "Omit<Workspace.*ownerVisitorId" src/auth/authRepo.ts | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "export const TaskFormSchema" src/tasks/schemas.ts | grep -c . | awk '$1 == 1' | grep -q '.' && \
      grep -E "Enter a task title\." src/tasks/schemas.ts | grep -c . | awk '$1 == 1' | grep -q '.' && \
      grep -E "export type TaskFormValues" src/tasks/types.ts | grep -c . | awk '$1 == 1' | grep -q '.' && \
      grep -E "TASK_STATUS_BADGE_COLOR" src/tasks/labels.ts | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "TASK_PRIORITY_BADGE_COLOR" src/tasks/labels.ts | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "'indigo\.3'" src/tasks/labels.ts | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      grep -E "'red\.6'" src/tasks/labels.ts | grep -c . | awk '$1 >= 1' | grep -q '.' && \
      npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - **tasksRepo invariant:** `grep -E "completedAt.*new Date" src/tasks/tasksRepo.ts | grep -v '^[[:space:]]*\\*\\|^[[:space:]]*//' | grep -c .` returns 1 or 2 (at least one in updateTask, optionally one in createTask).
    - **tasksRepo invariant — clear:** `grep -E "completedAt[[:space:]]*=[[:space:]]*null" src/tasks/tasksRepo.ts | grep -v '^[[:space:]]*\\*' | grep -c .` ≥ 1 (the `→ off-done clears completedAt` branch).
    - **authRepo additions:** `grep -c "^export function updateVisitor" src/auth/authRepo.ts` = 1; `grep -c "^export function updateWorkspace" src/auth/authRepo.ts` = 1.
    - **Patch type immutability:** `grep -c "Omit<Visitor, 'id' | 'passwordHash' | 'createdAt'>" src/auth/authRepo.ts` = 1; `grep -c "Omit<Workspace, 'id' | 'ownerVisitorId' | 'createdAt'>" src/auth/authRepo.ts` = 1.
    - **TaskFormSchema:** `grep -c "^export const TaskFormSchema" src/tasks/schemas.ts` = 1; the schema includes the keys `title`, `description`, `status`, `priority`, `assignee`, `dueDate` (verified by `grep -E "^[[:space:]]+(title|description|status|priority|assignee|dueDate):" src/tasks/schemas.ts | wc -l` ≥ 6).
    - **TaskFormValues:** `grep -c "export type TaskFormValues" src/tasks/types.ts` = 1.
    - **Badge color maps:** `grep -c "^export const TASK_STATUS_BADGE_COLOR" src/tasks/labels.ts` = 1; `grep -c "^export const TASK_PRIORITY_BADGE_COLOR" src/tasks/labels.ts` = 1; values match D-02 (`indigo.3`/`indigo.6`/`gray.5` for status, `gray.5`/`yellow.5`/`orange.5`/`red.6` for priority).
    - `npm run typecheck` exits 0.
    - **Behavior smoke (manual, recorded in SUMMARY):** In a dev-tools console with the app running, call `tasksRepo.updateTask(workspaceId, taskId, { status: 'done' })` on a non-done task and read the result — `completedAt` is a valid ISO timestamp; then call with `{ status: 'todo' }` and `completedAt` is null.
    - **D-15 defense (compile-time):** Attempting to pass `{ passwordHash: 'x' }` as the patch to `updateVisitor` triggers a TypeScript error (smoke this in plan execution, not in CI).
  </acceptance_criteria>
  <done>
    tasksRepo enforces the completedAt invariant in both update and create paths. authRepo exposes updateVisitor and updateWorkspace with immutable-field omits. TaskFormSchema + TaskFormValues exported. Badge color maps exported with exhaustive Record annotations. Typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: New primitive wrappers + helper modules + Dashboard refactor</name>
  <files>src/ui/primitives/Checkbox.tsx, src/ui/primitives/Textarea.tsx, src/ui/primitives/DatePickerInput.tsx, src/ui/primitives/index.ts, src/tasks/now-ref.ts, src/tasks/assigneeOptions.ts, src/tasks/index.ts, src/dashboard/Dashboard.tsx</files>
  <read_first>
    - src/ui/primitives/TextInput.tsx (verbatim wrapper template — copy for Textarea)
    - src/ui/primitives/Select.tsx (wrapper template — copy for DatePickerInput, swap import source to @mantine/dates)
    - src/ui/primitives/PasswordInput.tsx (multi-prop forwarding precedent — copy for Checkbox's taskId additional prop)
    - src/ui/primitives/index.ts (current barrel — append three new export pairs)
    - src/dashboard/Dashboard.tsx (extract computeNowRef function lines ~58-75 to new module + update import)
    - src/tasks/index.ts (current barrel — re-export computeNowRef + getAssigneeOptions + new helpers)
    - src/auth/authStore.ts (Visitor type + `useAuthStore.getState().currentVisitor` shape used by assigneeOptions caller — assigneeOptions itself only takes `visitor` as a param)
    - .planning/phases/04-core-pages-lists-settings-reports/04-PATTERNS.md §"src/ui/primitives/Checkbox.tsx" lines 75-100, §"src/ui/primitives/Textarea.tsx" lines 103-121, §"src/ui/primitives/DatePickerInput.tsx" lines 124-140, §"src/tasks/now-ref.ts" lines 318-343, §"src/tasks/assigneeOptions.ts" lines 347-376
  </read_first>
  <action>
    1) **Create `src/ui/primitives/Checkbox.tsx`:** Wraps `@mantine/core` `Checkbox`. Pattern from `PasswordInput.tsx` (the multi-prop forwarding precedent — Checkbox forwards both `pendoId` and an optional `taskId`):
       - Import `Checkbox as MantineCheckbox` and `CheckboxProps as MantineCheckboxProps` from `@mantine/core`.
       - Import `PendoId` type from `../../pendo/PENDO_IDS`.
       - Export `CheckboxProps = MantineCheckboxProps & { pendoId: PendoId; taskId?: string }`.
       - Export `Checkbox({ pendoId, taskId, ...rest })`: returns `<MantineCheckbox data-pendo-id={pendoId} data-pendo-task-id={taskId} {...rest} />`. When `taskId` is undefined, React omits the attribute entirely (verify: passing `data-pendo-task-id={undefined}` does NOT render the attribute — this is React's default behavior; smoke if uncertain).
       - The `data-pendo-task-id` lands on the same DOM node as `data-pendo-id` (the underlying `<input type="checkbox">` or Mantine's labeled wrapper — whichever Mantine forwards `...rest` to. For Mantine 9 Checkbox, `data-*` attrs forward to the root container element which Pendo can target; document this with a code comment).

    2) **Create `src/ui/primitives/Textarea.tsx`:** Verbatim copy of `TextInput.tsx` with type/component swapped:
       - Import `Textarea as MantineTextarea` and `TextareaProps as MantineTextareaProps` from `@mantine/core`.
       - Export `TextareaProps = MantineTextareaProps & { pendoId: PendoId }`.
       - Export `Textarea({ pendoId, ...rest })`: returns `<MantineTextarea data-pendo-id={pendoId} {...rest} />`.
       - No additional logic.

    3) **Create `src/ui/primitives/DatePickerInput.tsx`:** Verbatim copy of `Select.tsx` with import source swapped to `@mantine/dates`:
       - Import `DatePickerInput as MantineDatePickerInput` and `DatePickerInputProps as MantineDatePickerInputProps` from `@mantine/dates`.
       - Import `PendoId` from `../../pendo/PENDO_IDS`.
       - Export `DatePickerInputProps = MantineDatePickerInputProps & { pendoId: PendoId }`.
       - Export `DatePickerInput({ pendoId, ...rest })`: returns `<MantineDatePickerInput data-pendo-id={pendoId} {...rest} />`.
       - The wrapper passes through `type` ('default' | 'multiple' | 'range') unchanged — both Task form's `type='default'` (single date) and Reports filter's `type='range'` flow through the same wrapper.

    4) **Update `src/ui/primitives/index.ts`:** Append three new export pairs at the bottom of the file (preserving existing exports for Button/TextInput/Select/MultiSelect/PasswordInput/NumberInput/NavLink/Anchor):
       ```
       export { Checkbox } from './Checkbox'
       export type { CheckboxProps } from './Checkbox'

       export { Textarea } from './Textarea'
       export type { TextareaProps } from './Textarea'

       export { DatePickerInput } from './DatePickerInput'
       export type { DatePickerInputProps } from './DatePickerInput'
       ```

    5) **Create `src/tasks/now-ref.ts`** (D-22):
       - Copy the `computeNowRef` function verbatim from `src/dashboard/Dashboard.tsx` (currently inlined around lines 58-75 — find the function by `grep -n 'function computeNowRef' src/dashboard/Dashboard.tsx`).
       - Make it `export function computeNowRef(tasks: Task[]): Date`.
       - Add `import type { Task } from './types'` at the top.
       - Add a JSDoc header: "Anchor 'now' to the most recent task activity (createdAt/updatedAt/completedAt) so the demo never goes stale even if the seed/clock falls behind. Falls back to `new Date()` when tasks is empty. D-22 lock — shared between Dashboard.tsx and ReportsPage.tsx."
       - Match the file shape of `src/dashboard/relative-time.ts` (pure-function module, single export, JSDoc header).

    6) **Update `src/dashboard/Dashboard.tsx`:** Remove the inlined `computeNowRef` function definition and add the import `import { computeNowRef } from '../tasks/now-ref'`. All call sites (`computeNowRef(tasks)`) remain identical — they now resolve to the imported function. Verify no other code in Dashboard.tsx breaks (`npm run typecheck`).

    7) **Create `src/tasks/assigneeOptions.ts`** (D-10):
       - `import { listTasks } from './tasksRepo'`
       - `import type { Assignee } from './types'`
       - `import type { Visitor } from '../auth/types'`
       - Export:
         ```
         export function getAssigneeOptions(
           workspaceId: string,
           visitor: Visitor,
         ): Array<{ value: string; label: string }>
         ```
       - Implementation:
         - `const byId = new Map<string, Assignee>()`
         - For each task in `listTasks(workspaceId)`: `byId.set(task.assignee.id, task.assignee)`.
         - Ensure current visitor present: `byId.set(visitor.id, { id: visitor.id, name: \`${visitor.firstName} ${visitor.lastName}\` })`. (The visitor entry overwrites any prior task-assignee with the same id — fine, since the current visitor's name should reflect the up-to-date profile.)
         - Sort `[...byId.values()]` by `a.name.localeCompare(b.name)`.
         - Map to `{ value: a.id, label: a.name }`.
         - Return the array.
       - Caller will prepend `{ value: 'all', label: 'All' }` for filter selects (not this helper's responsibility — keep this returning only real assignees).

    8) **Update `src/tasks/index.ts`** (barrel):
       - Append `export { computeNowRef } from './now-ref'` and `export { getAssigneeOptions } from './assigneeOptions'`.
       - Verify TaskFormSchema / TaskFormValues / TASK_STATUS_BADGE_COLOR / TASK_PRIORITY_BADGE_COLOR are re-exported through the barrel if existing barrel re-exports their siblings (match the existing barrel pattern — if it re-exports TaskSchema then also re-export TaskFormSchema; if it re-exports TASK_STATUS_LABELS then also re-export TASK_STATUS_BADGE_COLOR).

    9) Run `npm run typecheck` and `npm run build`. Both must exit 0. Run `npm run dev` and load the Dashboard route — verify no visual regression (computeNowRef refactor is import-only, no behavior change).
  </action>
  <verify>
    <automated>
      cd /Users/colin.maxfield/testspace/test-sites/halo-app && \
      test -f src/ui/primitives/Checkbox.tsx && \
      test -f src/ui/primitives/Textarea.tsx && \
      test -f src/ui/primitives/DatePickerInput.tsx && \
      test -f src/tasks/now-ref.ts && \
      test -f src/tasks/assigneeOptions.ts && \
      grep -c "data-pendo-id={pendoId}" src/ui/primitives/Checkbox.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "data-pendo-task-id={taskId}" src/ui/primitives/Checkbox.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "data-pendo-id={pendoId}" src/ui/primitives/Textarea.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "from '@mantine/dates'" src/ui/primitives/DatePickerInput.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "data-pendo-id={pendoId}" src/ui/primitives/DatePickerInput.tsx | awk '$1 == 1' | grep -q '.' && \
      grep -c "export { Checkbox }" src/ui/primitives/index.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "export { Textarea }" src/ui/primitives/index.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "export { DatePickerInput }" src/ui/primitives/index.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "export function computeNowRef" src/tasks/now-ref.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "export function getAssigneeOptions" src/tasks/assigneeOptions.ts | awk '$1 == 1' | grep -q '.' && \
      grep -c "from '../tasks/now-ref'" src/dashboard/Dashboard.tsx | awk '$1 == 1' | grep -q '.' && \
      ! grep -E "^function computeNowRef" src/dashboard/Dashboard.tsx && \
      npm run typecheck && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - **Checkbox wrapper:** File exists; `grep -c "from '@mantine/core'" src/ui/primitives/Checkbox.tsx` ≥ 1; `grep -c "pendoId: PendoId" src/ui/primitives/Checkbox.tsx` = 1; `grep -c "taskId\\?: string" src/ui/primitives/Checkbox.tsx` = 1; `grep -c "data-pendo-id={pendoId}" src/ui/primitives/Checkbox.tsx` = 1; `grep -c "data-pendo-task-id={taskId}" src/ui/primitives/Checkbox.tsx` = 1.
    - **Textarea wrapper:** File exists; shape matches TextInput.tsx with `MantineTextarea` swap; `pendoId: PendoId` required.
    - **DatePickerInput wrapper:** File exists; `grep -c "from '@mantine/dates'" src/ui/primitives/DatePickerInput.tsx` = 1 (not `@mantine/core`); `pendoId: PendoId` required; props extend `MantineDatePickerInputProps`.
    - **Barrel exports:** All three new exports + their props types appended to `src/ui/primitives/index.ts`. `grep -cE "^export (\\{ Checkbox \\}|type \\{ CheckboxProps \\})" src/ui/primitives/index.ts` returns 2.
    - **now-ref.ts:** File exists; `computeNowRef` exported; function body matches the original Dashboard.tsx lines 58-75 byte-for-byte (modulo formatting). Verify by reading both and comparing.
    - **Dashboard refactor:** Inlined `function computeNowRef` REMOVED from `src/dashboard/Dashboard.tsx`. Import line `import { computeNowRef } from '../tasks/now-ref'` ADDED. All call sites still call `computeNowRef(...)` (no rename).
    - **assigneeOptions.ts:** File exists; `getAssigneeOptions(workspaceId, visitor)` exported with the exact signature `(workspaceId: string, visitor: Visitor): Array<{ value: string; label: string }>`. Body imports `listTasks` from `./tasksRepo` and dedupes by `Assignee.id`. Always includes the current visitor.
    - **Tasks barrel:** `grep -c "from './now-ref'" src/tasks/index.ts` = 1; `grep -c "from './assigneeOptions'" src/tasks/index.ts` = 1.
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
    - Dashboard route renders identically before and after (manual: `npm run dev`, sign in, visit `/app` — KPIs and charts populate per current behavior).
  </acceptance_criteria>
  <done>
    Three new wrapper primitives ship with the existing `pendoId` contract (Checkbox additionally forwards taskId). `computeNowRef` is shared via `src/tasks/now-ref.ts` (Dashboard updated to import). `getAssigneeOptions` ships in `src/tasks/assigneeOptions.ts`. Barrels updated. Typecheck + build pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → authRepo | User-typed form values cross into the persistence layer; structurally typed via Zod-validated form values |
| Lists checkbox onChange → tasksRepo.updateTask | Status toggle crosses into repo write path |
| Reports filter component → assigneeOptions | Workspace ID + visitor object cross into a read-only helper that scans tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | `authRepo.updateVisitor` patch type | mitigate | `Omit<Visitor, 'id' \| 'passwordHash' \| 'createdAt'>` STRUCTURALLY excludes the password hash from the patch type. TypeScript flags any caller that tries to pass `passwordHash` — UI plumbing cannot accidentally expose the secret (per D-15, defense lives in the type system per Phase 2 STATE.md `T-02-42` precedent). |
| T-04-02-02 | Tampering | `tasksRepo.updateTask` completedAt invariant | mitigate | The invariant is owned by the repo; UI code cannot bypass it. Even if a caller passes `{ status: 'done', completedAt: null }`, the repo overrides `completedAt` with `new Date().toISOString()`. |
| T-04-02-03 | Tampering | Zod TaskFormSchema | mitigate | All Lists task-create/edit input flows through `zodResolver(TaskFormSchema)` — invalid types are rejected at the validation boundary before reaching `tasksRepo.createTask` / `updateTask`. |
| T-04-02-04 | Information Disclosure | `getAssigneeOptions` output | accept | Returns only assignee IDs and display names that are already present in the workspace's task data — no new disclosure. Visitor's first/last name is the user's own; not a cross-tenant leak (single-user demo). |
| T-04-02-05 | Repudiation | `tasksRepo.updateTask` updatedAt stamp | accept | The `updatedAt: new Date().toISOString()` stamp is preserved; mutations are timestamped at the repo boundary. Single-user-local demo — no audit log requirement. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after both tasks.
- `npm run build` exits 0.
- Dashboard route renders identically before and after the `computeNowRef` extraction (manual smoke).
- All five primitives (`Checkbox`, `Textarea`, `DatePickerInput`, plus the existing `TextInput`/`Select`) compile against the new pendoId leaves added in plan 04-01.
- `grep -r 'localStorage\\.' src/auth/authRepo.ts src/tasks/tasksRepo.ts | grep -v 'codec\\.ts'` returns 0 matches — all storage I/O continues to route through `readWithSchema`/`writeJSON` (FND-04 lock).
</verification>

<success_criteria>
- Lists checkbox in plan 04-03 can call `tasksRepo.updateTask(workspaceId, taskId, { status: 'done' })` and trust that `completedAt` is stamped automatically.
- Settings save handlers in plan 04-04 can call `authRepo.updateVisitor(id, { firstName, lastName, ... })` without thinking about omitted fields — they're structurally enforced.
- Reports filter bar in plan 04-05 can call `getAssigneeOptions(workspaceId, visitor)` and use the result directly in a `<Select data={...} />`.
- All three Phase 4 pages can import wrapped `Checkbox`, `Textarea`, `DatePickerInput` from `@/ui/primitives` instead of using raw Mantine components (which would force hand-typed `data-pendo-id` strings).
- Dashboard.tsx and the new ReportsPage.tsx share `computeNowRef` from `src/tasks/now-ref.ts` — demo data anchor is consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages-lists-settings-reports/04-02-SUMMARY.md` capturing:
- Confirmation that the `completedAt` invariant fires in both directions (smoke-test result in dev console).
- File-line shifts in Dashboard.tsx after computeNowRef extraction (so future readers can locate the change).
- Final exported surface of the tasks/auth barrels.
</output>
